# Gaia — 世界地理主題（CLAUDE.md 的延伸）

> ⚠️ **這份檔案不會自動載入。** 它是 `CLAUDE.md` 切出來的一半，只收「世界地理」
> 主題（`/theme/world`，舊網址 `/theme/global`）各圖層的資料來源、取得邏輯、
> 踩過的坑與驗證方式。
>
> **要動世界主題的圖層之前先把這份讀完**：`src/map/registry/themes/world.ts`、
> `src/map/registry/generators.ts` 裡的 `windBelts()`／`oceanCurrents()`、
> `scripts/lib/` 底下對應的存取層（`plates.mjs`／`continents.mjs`／`mountains.mjs`／
> `biomes.mjs`／`koppen.mjs`／`volcanoes.mjs`）、`public/data/geo/` 底下非 `tw-` 的
> geojson、`src/content/geo/` 底下非 `tw-` 的說明檔，以及 `src/map/basemaps.ts`
> 的地名繁體化都算。
>
> 臺灣主題那一半在 **`CLAUDE_TW.md`**。跨主題的東西**仍然以 `CLAUDE.md` 為準**：
> 專案定位、技術棧與版本陷阱、資料源總表、硬性禁止事項、圖層註冊表的架構、
> 顏色系統與色票驗證規則、版面與彈出層、搜尋索引、圖表規範、開發指令、
> 驗證方式的通則與部署。三邊的章節標題刻意沒有改，互相引用時直接用標題找得到。

---

## 世界各圖層的資料來源、取得邏輯與坑

> 對應 `CLAUDE.md` 的「資料源」一章。端點總表、NLSC 路徑順序陷阱、氣候資料的
> build-time 產製、地理要素說明的延遲載入都留在 CLAUDE.md。

### 世界櫥窗：地表之最與編者選集

> ⚠️ **「世界之最」那兩層（`world-superlative-peaks`、`world-superlative-ranges`）
> 2026-08 起已從註冊表下架，等待重新設計。** 下面這一整節寫的是它們被下架前的設計與
> 實測結論，**刻意留著**：內容檔（`src/content/geo/world-superlatives/` 7 份）與兩份
> 幾何（`geo-manual/world-superlatives-peaks.geojson`、
> `geo/world-superlatives-ranges.geojson`）都原封不動，`build:geodata` 的
> `world-superlatives-ranges` 資料集也沒動——只有 `themes/world.ts` 裡那兩個圖層物件
> 被拿掉（在 git 上一版）。**重新設計時先把這一節讀完**，尤其是「為什麼是兩個圖層而
> 不是一個」「三項火山紀錄各有一個定義問題」與「講不出憑據的『最』不收」那三段：
> 那是這一層當初存在的理由，也是它值得重做的地方。
> 編者選集**不受影響，仍在線上**——但它 2026-08 從「作者精選」一層拆成了九層，見下。

「世界櫥窗」是 2026-08 新增的分組，排在**參考線之後、氣候與生物群系之前**——先讓學生
在地圖上找得到具體的地方，後面的氣候帶、洋流、板塊才有東西可以掛上去。底下有**兩種**
東西：

- **世界之最**（`world-superlative-peaks` 5 筆＋`world-superlative-ranges` 2 筆，
  **兩層都已下架**）：海拔最高的山峰（聖母峰）、海拔最低的山（日和山）、體積最大的
  火山（普哈胡努）、海拔最高的火山（奧霍斯－德爾薩拉多山）、海拔最高的活火山
  （尤耶亞科山）、最長的陸上山脈（安地斯）、最長的海底山脈（中洋脊）。
- **編者選集**（21 筆，**九個圖層**）：原本是「作者精選」（`world-picks` 點）＋
  「作者精選・範圍」（`world-picks-areas` 線）兩個核取方塊，2026-08 依原本的
  `category` 拆成九層——

  | 圖層 | label | 幾何 | 收錄 |
  |---|---|---|---|
  | `world-picks-volcanoes` | 火山與災害 | circle | 維蘇威火山（西元 79 年的龐貝城） |
  | `world-picks-landforms` | 地形與地質 | circle | 理查特結構（「撒哈拉之眼」） |
  | `world-picks-ocean` | 海洋 | circle | 尼莫點、馬里亞納海溝 |
  | `world-picks-straits` | 海峽 | circle | 直布羅陀、博斯普魯斯、曼德、荷姆茲、麻六甲、臺灣海峽、土渕 |
  | `world-picks-canals` | 運河 | circle | 巴拿馬、蘇伊士 |
  | `world-picks-capes` | 海角 | circle | 合恩角、好望角 |
  | `world-picks-lakes` | 湖泊 | circle | 蘇必略湖、死海、裏海、貝加爾湖 |
  | `world-picks-rift` | 板塊與地形 | line | 東非大裂谷 |
  | `world-picks-legend` | 傳說中的地方 | line | 百慕達三角 |

  拆的理由與三件綁在一起的事（幾何仍然只有兩份、內容 collection 九層共用、九層全部
  豁免圓點名額）寫在下面「編者選集為什麼是九個圖層」。

⚠️ **每一筆的重點都是「這個『最』是怎麼量、怎麼定義出來的」**，不是那個名字。三筆
火山紀錄尤其如此（見下面「三項火山紀錄各有一個定義問題」），內容檔的 `facts` 與圖層的
`notes` 都要把那件事寫出來——寫不出來的「最」不該收進這一層。

⚠️ **空的分組在抽屜上不會顯示**（`LayerPanel` 對 `layers.length === 0` 是 `return null`），
所以之後再開新分組時，標題要等第一個圖層進去才會出現，那不是漏掉。

#### 為什麼是兩個圖層而不是一個

「最高／最低／最大」是**點**、「最長」是**線**，而 `LayerRender` 一個圖層只能一種幾何
（`items` 也不行——`LayerItem` 沒有自己的 `render`）。所以是
`world-superlative-peaks`（circle，5 筆）與 `world-superlative-ranges`（line，2 筆）
兩個核取方塊。七筆圖徵**共用同一個內容 collection**（`src/content/geo/world-superlatives/`）：
id 在 collection 內唯一就夠，而這幾張卡本來就是一組要一起讀的東西。

⚠️ **三座火山併進點圖層、不另開一層**，那是圓點名額逼出來的：`MAX_ACTIVE_BY_KIND.circle`
是 4，而世界主題已經有四個 ready 的圓點圖層（全球地震帶、火山帶、世界重要城市、
這一層）。另開「世界之最・火山」會變成五個搶四個名額，而這一層要對照的正好就是
**火山帶**——那等於逼使用者在「櫥窗」與「要對照的圖層」之間二選一。

⚠️ 因此圖層 id 是 `world-superlative-peaks` 而 label 是「世界之最・**高山與火山**」。
**不要為了對齊而改 id**：它是 maplibre source/layer 的前綴（`world-superlative-peaks-points`），
改了等於改掉所有既有的驗證指令與文件。

⚠️ **圖徵 id 是「那個最」而不是那個地物的名字**（`highest-peak`／`lowest-mountain`／
`longest-land-range`／`longest-undersea-range`）。不叫 `andes` 是刻意的：那個字串已經
是「世界主要山脈」裡那一條的 id，而 `highlightIds` 是一份**跨圖層的扁平清單**——撞 id
的話點這一層的安地斯會連帶把另一層的也加粗（CLAUDE.md 早就記著「同時可見的兩層撞 id
實務上不會發生」，這一層正好是會發生的那種）。

#### 兩層都不用新顏色，那是刻意的

- **山峰沿用 `place` 藍**：POINT 色票已經是 all-pairs 全過的飽和狀態，而「藍點＝地圖上
  一個有詳情卡的地點」本來就是本站記錄在案的規則（附屬點一律用藍）。同框的另外兩個
  圓點圖層（火山帶洋紅、全球地震帶中性灰）都跟它驗過。

  ⚠️ 兩座安地斯火山的座標**逐位元取自 `volcanoes.geojson`**，所以兩層一起打開時，
  藍點與火山帶的洋紅點會疊在**同一個位置**（藍點半徑 6–7、洋紅 3.2，看起來是藍點
  中間一顆洋紅）。那是刻意的——同一顆火山就該在同一個座標上，跟聖母峰那一對
  「差 4 公里」的處理相反（那邊兩份資料量的不是同一個東西）。
- **山脈沿用 `mountain` 紫**：這兩條**就是**山脈，同一個角色本來就該同一個色（比照
  地形景點藍與水系藍共用同一個 hex）。安地斯疊在「世界主要山脈」上時因此看起來是
  同一條線，而不是兩條顏色打架的線。

⚠️ 中洋脊必然會壓在**張裂型板塊邊界**（橘、虛線）上——它們是同一件事的兩種說法。
紫對橘的分離度是 `MOUNTAIN_COLOR` 那次掃描驗過的，判讀靠線型與沿線名稱。

#### 幾何：兩條線都不能用抄的

`world-superlatives-ranges` 是 `build:geodata` 的一個資料集，**刻意跟既有圖層共用同一段
程式**：

- **安地斯**走 `polygonAxis()`、吃 `world-mountains` 同一個 `tolerance`（0.02），所以
  產出的 100 個座標與 `world-mountains.geojson` 裡那一條**逐位元相同**（建置後實測過）。
  兩層一起打開時那條線必須完全重合——手抄一份進 `geo-manual/`，上游或簡化參數一改就會
  漂開成兩條平行線。
- **中洋脊**取 Bird (2003) 的 **OSR** 段落。⚠️ **中洋脊 ≠ 張裂型邊界**：張裂型是
  OSR ＋ CRB，而 CRB 是**大陸**裂谷（東非、貝加爾、里約格蘭地），那些在陸地上、不是
  海底山脈。篩選常數在 `lib/plates.mjs` 的 `MID_OCEAN_RIDGE_CLASS`。

為此把 `plate-boundaries` 的串接邏輯抽成 `lib/plates.mjs` 的 `mergeStepRuns(raw, keyOf)`
（`keyOf` 回 `null` 就跳過那一段，順便切斷串接——中間隔了別種邊界的兩段 OSR 本來就不該
接成一條線），跨 ±180 的檢查抽成 `assertNoAntimeridianCrossing()`。⚠️ **重構之後
`plate-boundaries` 的產物實測逐位元相同**（只有 `generatedAt` 不同），改動這兩支函式後
請照樣重跑一次比對。10 MB 的 step 檔由 `fetchSteps()` 快取，兩個資料集共用同一次下載。

⚠️ **長度是自我檢查，不是產物**：`geometryLengthKm()` 算出中洋脊約 **67,000 公里**
（594 段），對得上 NOAA 公布的 65,000——落在 4 萬～10 萬以外就讓建置失敗，那是唯一
抓得到「STEPCLASS 篩選壞了」的檢查（比照板塊面積總和等於地球表面積）。但那個數字
**不寫進 geojson**：它是從模型化的邊界幾何量出來的，寫進卡片就是假精確（比照世界主要
山脈刻意不放長度）。卡片上寫的是 NOAA 的公布值。

#### 兩座山峰是人工轉錄的官方數字，放 `geo-manual/`

`public/data/geo-manual/world-superlatives-peaks.geojson`（比照臺灣海峽中線）：
聖母峰取峰頂實測位置與 2020 年中尼聯合公布的 **8,848.86 公尺（雪面）**，日和山取
國土地理院地形圖上的位置與 2014 年重新確認的 **3 公尺**。

普哈胡努取露出海面的加德納尖峰，體積 15 萬 km³ 出自 Garcia et al. (2020)；兩座安地斯
火山的**座標**取自 GVP（同上），**高度**則採兩國公布的常用值（GVP 記的是 6,879 與
6,739，差幾公尺）。

⚠️ **不要改成從 `world-mountain-peaks.geojson` 切出聖母峰。** 那一份是 Natural Earth
的 1:10m 高程點，位置差約 **4 公里**、高度是舊值 8,848——對「畫出喜馬拉雅的最高點在
哪一段」夠用，但這一層要講的正是那個數字本身。代價是兩層一起打開並放大之後會看到
兩顆藍點，這件事寫在圖層的 `notes` 裡。

⚠️ **取景寫在 geojson 的 `properties.zoom` 上、不是 `browse.zoom`**：兩筆的尺度差了
五級（聖母峰要看整條喜馬拉雅、日和山是一顆 3 公尺高的沙丘），而 `ThemeMapPage` 對點
圖層是 `feature.properties.zoom || browse.zoom || 11`——逐筆決定才對。

#### 三項火山紀錄各有一個定義問題，那正是收它們的理由

| 項目 | 收錄 | 要一起講的事 |
|---|---|---|
| 體積最大的火山 | 普哈胡努 約 15 萬 km³ | ⚠️ **2020 年才換人**。在那之前課本寫的是茂納羅亞（同一篇論文把它修正為 8.3 萬 km³），而茂納羅亞至今仍是體積最大的**活**火山。2013 年一度被認為最大的塔穆火山，2019 年重新分析後認定不是單一盾狀火山 |
| 海拔最高的火山 | 奧霍斯－德爾薩拉多山 6,893 m | 它**同時也是**全新世活火山（GVP 記最後噴發約西元 750 年），所以這一筆與下一筆是同一件事的兩種算法 |
| 海拔最高的活火山 | 尤耶亞科山 6,739 m | ⚠️ 取「**有文獻記載的噴發**」（1877 年）當判準。若採地質學界通用的「一萬年內噴發過」，答案會變成上一列那座——本站「火山帶」那一層（GVP 全新世名單）就是這樣列的 |

⚠️ **「最」講不出憑據就不要收，那種東西放編者選集那九層。** 「最有名的火山」「最危險
的火山」這類項目沒有人排得出來，硬掛在「世界之最」底下會讓那一層的判準失效——
維蘇威火山就是為此分出去的（見下）。

⚠️ **兩座安地斯火山不可以只留一座。** 「同一個問題因為標準不同而有兩個答案」正是這
兩筆的教學內容；合併成一筆等於把那件事刪掉。反過來，**也不可以兩筆都放奧霍斯**——
同一個座標疊兩顆點，點下去只開得了其中一張卡。

#### 編者選集是另一種東西，不要跟「世界之最」混在一起

那九層收的是**編者挑的地方**——沒有紀錄可言，但在地圖上指得出來、而且講得出
一個具體的故事。維蘇威火山就是為此分出來的：它不是最高、最大或最活躍的火山，硬掛在
「世界之最」底下會把那一層「量得出來、講得出憑據」的判準稀釋掉。

⚠️ **收錄界線要講得出口**（比照世界主要山脈的 39 條、GVP 火山中文名那 40 幾座）：
課本、新聞或科普讀物會直接叫出名字，在地圖上找得到明確的位置，而且那個地方本身
就是一堂課。**不是排名、也不會補完**——這件事寫在**九層每一層**的 `notes` 第一則裡，
拆層之後那句話更重要了：層名叫「海峽」而不再叫「作者精選」，讀者第一眼會以為它是一份
完整名錄，所以每一層的 `description` 也都以「編者挑出來…」開頭。

- 內容 collection 是 `world-picks`（不跟 `world-superlatives` 共用）；**圖徵 id 是那個
  地方本身**（`vesuvius`），不是某個「最」——那正是這一層與隔壁的差別。
- 座標逐位元取自 `volcanoes.geojson`，跟「火山帶」那一層完全重合（同上）。
- 維蘇威那張卡的重點是**龐貝城事件**（火山碎屑流、小普林尼的目擊記錄與「普林尼式
  噴發」、1748 年起的挖掘與石膏灌模、現在山腳下的三百萬人）。**改動這張卡時不要
  把龐貝拿掉**，那是它被收進來的理由。

⚠️ **理查特結構（「撒哈拉之眼」）的重點是「看起來像不等於就是」**，不是那個奇景。
那張卡先講撞擊坑說怎麼被推翻（找不到衝擊石英、高壓礦物與坑緣翻轉構造；2022 年 IUGS
的認定文件直接寫「撞擊說已被推翻，改採侵入說」，順帶連「這裡是亞特蘭提斯」一起交代），
再講那些環實際上是怎麼來的（白堊紀鹼性岩漿頂出穹丘 → 侵蝕削平 → 軟硬岩層交替出露，
越靠中心越老）。**改動這張卡時不要把「怎麼判斷它不是隕石坑」拿掉**——那是它被收進來
的理由，跟百慕達三角那張卡是同一種內容。

⚠️ **本站真的畫得出那圈同心圓，而且那正好是這一筆的教學高潮**：向量底圖在那裡什麼都
沒有（不是衛星影像），環是**等高線與地形陰影**從 DEM 算出來的。實測 zoom 9.5：兩個
疊圖都開著時同心圓清清楚楚，**只關掉地形陰影仍然看得到等高線畫出來的環**（所以卡片上
不能寫「關掉地形陰影就消失」——那是錯的），**兩個都關掉才會消失得一乾二淨**。
`properties.zoom` 因此是 **9.5**（整個結構四、五十公里寬，再近就框不住、再遠環就糊了）。

⚠️ 這一筆的座標是**結構中心**（21.1249/−11.4036，取自維基百科條目所載的度分秒），
不是某個指認得出來的地物——寫在圖層的 `notes` 裡。它跟維蘇威那筆「逐位元取自
`volcanoes.geojson`」是兩種不同的來源，不要互相照抄。

⚠️ **尼莫點（海洋難達極）的重點是「一個算出來的地方」**：它到三處最近的陸地都是
2,688 公里（迪西島、莫圖努伊島、馬厄島），1992 年由 Lukatela 從數位海岸線資料解出來，
海面上什麼都沒有。卡片的四個段落分別是那個計算、「最近的人類常常在四百公里高的太空站
上」、南太平洋無人區當太空船墓地（和平號 2001、國際太空站預定 2031），以及 **Bloop
那段被解開的傳說**——NOAA 判定是冰山碎裂的冰震，而且這種聲音能傳五千公里以上，所以
「聽起來來自附近」不等於來源在附近。**改動這張卡時不要把 Bloop 那一段拿掉**，它跟
百慕達三角、理查特結構是同一條線上的內容（怎麼查證，而不是獵奇）。

⚠️ **`properties.zoom` 是 2.6，不是「越近越好」**：這一筆的教學內容是**它離所有東西
都很遠**，所以取景必須同時看得到紐西蘭、南美與南極圈——實測 zoom 3.5 整個畫面沒有
任何陸地，看起來只是「一顆藍點浮在藍色裡」，反而讀不出 2,688 公里有多遠。

⚠️ **它其實是一個量得出來的紀錄**（最遠離陸地的海上點），嚴格說更接近「世界之最」的
判準；現在放在這一層是因為那兩層下架中。**重新設計「世界之最」時，這一筆是第一個該
考慮搬過去的**——搬的話記得 `world-picks` 與 `world-superlatives` 是兩個 collection，
內容檔要跟著換目錄。

⚠️ 座標沒有任何國際機構公告過（換一份海岸線資料重算會差幾公里），這件事寫在圖層的
`notes` 裡。

#### 馬里亞納海溝：「海洋」那一層的第二筆，收的是「最深是怎麼量出來的」

2026-08 加進 `world-picks-ocean`（`category: "海洋"`，在 geojson 裡排在尼莫點後面——
兩筆依經度由西往東，尼莫點 −123.4°、海溝 142.2°，比照「海峽」那一層的既有規則）。

⚠️ **它跟尼莫點是同一種東西，所以放同一層**：兩筆都是「一個算出來／量出來的地方」，
海面上什麼都看不到；尼莫點的重點是那個計算，海溝的重點是**那個數字每量一次就換一個**。
⚠️ **不要因為它是地形就搬去「地形與地質」或「板塊與地形」**：前者收的是理查特結構那種
「看得到、但看起來像的東西不是它真正的身分」，後者是**線**圖層（東非大裂谷、百慕達三角）。

⚠️ **它嚴格說也是一個量得出來的紀錄**（地表最深處），跟尼莫點一樣是「世界之最」重新
設計時第一批該考慮搬過去的——搬的話記得 `world-picks` 與 `world-superlatives` 是兩個
collection，內容檔要跟著換目錄。

⚠️ **卡片採 10,928 公尺（2019 年維斯科沃下潛實測），而維基百科那篇的資訊框寫 11,034。**
那不是抄錯，那正是這一筆的教學內容：條目**內文自己**就寫著 11,034（1957 年蘇聯維塔茲號）
「從未再測得，故不以為準確」。歷次數字一次列出來——1951 挑戰者二號 10,863（回聲測深，
提報時還主動減掉 20 噚）、1962 拜爾德號 10,915、1984 拓洋號 11,040.4（同一次也記成
10,920±10）、1995 海溝號 10,911（公認最精確）、2019 維斯科沃 10,928。⚠️ **改動這張卡時
不要把這串數字收斂成一個「乾淨」的深度**，那是它被收進來的理由（比照貝加爾湖深度刻意
跟中文維基的 infobox 不同、土渕海峽「最窄」是登記出來的那兩筆）。

⚠️ **距地心那一段不要刪**：條目寫著挑戰者深淵海床距地心 6,366.4 公里、只有四公里多深的
北冰洋海床距地心 6,352.8 公里——**淺的那個反而離地心近 13.6 公里**（赤道隆起）。
「最深」量的是從海平面往下多少，不是離地心多近，這跟本層一貫的「最取決於怎麼量」同一條線。

⚠️ **座標取條目資訊框的 11°21′N 142°12′E（142.2/11.35），不是挑戰者深淵的精確位置。**
比照臺灣海峽那一筆的既有處理（畫的是條目所載座標處的一個點、不是最窄處），圖層的
`notes` 寫明了這件事，也寫明挑戰者深淵在東邊數十公里、1951 年那次測深的位置是
11°19′N 142°15′E。⚠️ 這一層畫的是**一個點**，而海溝長 2,550 公里。

⚠️ **這個點落在海溝軸上，是實測的不是估的**（`queryRenderedFeatures` 打 `water` 圖層是 1，
點不在任何陸地上）：打開 3D 地形之後 `queryTerrainElevation([142.2,11.35])` 讀到
**約 −10,490 公尺**（挑戰者深淵座標處 −10,369、往西 0.4° 只有 −8,007）。
⚠️ **那個 API 回的是乘過 `exaggeration` 的值**（本站是 1.4），要自己除回去——不除的話會
讀成 −14,687 公尺，看起來像 DEM 壞了。⚠️ 這是 DEM 圖磚上化簡過的海底地形，**不是官方
深度**，只拿來確認「這個點真的在溝裡」。

⚠️ **這一層唯一一筆「底圖上真的看得見」的海底地形**：向量底圖在那裡什麼都沒有，但
terrarium DEM **含海底地形**，所以打開「地形陰影」就看得到那條弧形的溝與西邊的島弧
（實測 zoom 4.6 與 5.5 都清清楚楚，跟理查特結構那一筆是同一種「本站畫得出來」的驚喜）。
這件事寫在圖層的 `notes` 裡。

⚠️ **`properties.zoom` 是 4.6，實測調的**（畫布 1920×803，比既有那批 929 矮，所以垂直
方向是保守值）：實測視野 lon 114.37–170.03、lat −1.23 至 23.40，**25 座島弧火山全部在
畫面上**、聚合型邊界那條線整條弧都進得來。zoom 5.5 只框得到 13 座（北界 17.91），
弧的上半截被切掉，而這一筆的「對照重點」講的正是海溝與島弧平行排開那件事。
⚠️ **必須小於 8**（`plate-boundaries` 的 `maxzoom`），比照曼德海峽那條硬條件。

⚠️ **島弧離海溝多遠是量出來的**：拿本站自己的兩層（`plate-boundaries` 的聚合型邊界與
`volcanoes.geojson`）算 25 座島弧火山各自到邊界的最近距離，中位數 **214 公里**
（158–318）。卡片寫「大約 200 公里」就是這麼來的——**不要改寫成從別處抄的數字**。

⚠️ **上盤在本站的板塊圖層上是「菲律賓海板塊」，不是「馬里亞納板塊」，卡片有寫明。**
點在多邊形內實測：Bird (2003) 的馬里亞納板塊南界在 **11.49°N**，而這一筆在 11.35°N，
剛好落在南邊那一格。條目本身寫的是「太平洋板塊於此俯衝於菲律賓海板塊（或細分出的
馬里亞納板塊）之下」，兩邊對得上。⚠️ 這跟臺灣海峽那一筆「課本說歐亞、模型說揚子」
是同一類事情——**寫這種句子之前先拿 `plates.geojson` 實測**，不要照課本的印象寫。

⚠️ **`meta` 裡放了「又作瑪麗亞娜海溝」與「挑戰者深淵約 10,928 公尺」**（比照裏海／裡海、
麻六甲／馬六甲）：`meta` 在搜尋 haystack 裡，實測「馬里亞納」「瑪麗亞娜」「海溝」
「Mariana」「挑戰者深淵」五種都命中。

⚠️ **荷姆茲海峽是這一層第一筆「人文地理」的項目**，重點是**咽喉點**這個概念：一條
最窄處只有三、四十公里的水道，全球約五分之一的石油與五分之一的液化天然氣由此進出。
卡片刻意把它一路拉回臺灣——依經濟部能源署的年資料，**2025 年臺灣的原油有 61.7%
來自波斯灣裡的五個國家**，全都得從這條海峽出來。

⚠️ **那個 61.7% 是自己從官方 CSV 算的，不是抄新聞的**（data.gov.tw 資料集 163714 →
`wHandOpenData_File.ashx?set_id=242`）：沙烏地 28.9＋科威特 13.6＋阿聯 11.9＋卡達 6.0
＋伊拉克 1.3。⚠️ **阿曼那 7.9% 不能算進去**——它的油港在海峽外面，不必經過。
重算年度時記得這件事，也記得月資料跟年資料差很多（某月沙烏地可以到 40%）。

⚠️ **石油流量一律以 EIA 那一頁為準**（2022 年每天 2,100 萬桶、占全球石油液體消費
21%、海運石油四分之一以上），不要改用新聞轉述的數字——那些多半沒寫是哪一年、
哪一種口徑。

⚠️ **這張卡有一段是有時效的，而且這是全站唯一一處**：2026 年的衝突使通行量掉到平常
的一小部分（Lloyd's List Intelligence 2026-08-05 的簡報：一週 84 艘次，平時每天上百
艘）。內文**已經把日期與出處寫進句子裡**，圖層的 `notes` 也另外聲明「這個站是純靜態
的、不會自己更新」。⚠️ **要嘛維護它、要嘛整段拿掉，不要留一句沒有日期的現在式**——
純靜態站沒有任何機制會提醒你它過期了。

⚠️ **寬度有兩個常見數字**（33 公里與 21 海里／39 公里），差別在量的端點不同，卡片
兩個都寫出來——比照世界之最那幾張卡的既有做法（「最」取決於怎麼定義與怎麼量）。
座標取的是**主航道中線**上的一點（26.685/56.44，伊朗拉拉克島與阿曼奎恩群島之間），
`properties.zoom` 是 7.5：那個尺度同時看得到阿巴斯港、格什姆島、穆桑代姆半島與東邊
敞開的阿曼灣，「窄口」才讀得出來。

#### 七條海峽是一層，它從「海洋」分出來過兩次

2026-08 加進直布羅陀、博斯普魯斯、曼德（後來再加臺灣海峽）之後，海峽變成七條，於是先把
它們的 `category` 從 `"海洋"` 拆成 `"海峽"`（`尼莫點` 留在 `"海洋"`）；同月再把整個
「作者精選」拆成九層時，那個 `category` 就變成了 `world-picks-straits` 這一層的
`featureIds`。

⚠️ **`properties.category` 現在不驅動任何 UI 了**（`browse.groupBy` 跟著「作者精選」
一起拿掉），但**刻意留在 geojson 裡**：它記錄著每一筆屬於哪一層、也讓檔案的排列有意義。
⚠️ 它從來就不是 id、也不在搜尋 haystack 裡（haystack 是 name＋shortName＋en＋meta＋
內容檔別名），所以改它不會動到任何既有的搜尋或連結；⚠️ 但**真正的權威是註冊表的
`featureIds`**，兩邊不同步時以註冊表為準——而「geojson 裡有一筆沒被任何一層認領」會被
`validate-content.mjs` 擋下來（見 CLAUDE.md「一份 geojson 切給多個圖層」）。

⚠️ **geojson 裡七條海峽是由西往東依經度排的**（直布羅陀 −5.5°、博斯普魯斯 29.1°、
曼德 43.3°、荷姆茲 56.4°、麻六甲 103.5°、臺灣海峽 119.9°、土渕 134.2°），而
`splitByFeatureIds()` 是**過濾、不重排**，所以抽屜可點清單的順序仍然是檔案順序——
再加海峽時**要插在經度對的位置**，不能往檔案尾巴丟（`featureIds` 那一串也順手排成
一樣的順序，但那只是可讀性，不影響畫面）。前六條剛好串成從大西洋到東亞的那條海運航線，
土渕排最後是因為它經度最東，跟「它是異類」正好同向。

⚠️ **五張卡互相指涉，改一張要順手看其他幾張**（比照「湖泊」那一組的既有規則）：直布羅陀 →
曼德（地中海的門 vs 紅海的門，中間夾著蘇伊士運河）；博斯普魯斯 → 直布羅陀（黑海的船
要先出博斯普魯斯、再從直布羅陀才出得了大西洋）；曼德 → 荷姆茲（波斯灣的油往歐洲走的
同一條路）；麻六甲 → 荷姆茲（同一條路往東亞的那一半，而且直接引用了荷姆茲卡上的 61.7%）。

#### 兩條運河自成一層，而它們是拿來跟海峽對照的

2026-08 加進巴拿馬與蘇伊士，`category` 是 **`"運河"`**（拆層之後就是
`world-picks-canals` 那一層），在 geojson 裡排在「海峽」後面、「湖泊」前面
（抽屜裡的圖層順序也是 火山與災害／地形與地質／海洋／海峽／運河／海角／湖泊／
板塊與地形／傳說中的地方）。
⚠️ **不要併進「海峽」**：這兩層是同一個主題（咽喉點）的兩種來源——一個是地形逼出來的、
一個是人挖出來的，分開才講得出這件事，而兩張卡的「對照重點」都靠它。⚠️ 這條判準在
拆層之後**更硬**了：它從「為什麼是兩個 `category`」變成「為什麼是兩個核取方塊」。
層內依經度排（巴拿馬 −79.75° 在前）。

⚠️ **這兩張卡的核心是一組對比，改一張要順手看另一張**：蘇伊士 193 公里**一道船閘都
沒有**（地中海與紅海幾乎等高，中途還借用大苦湖），巴拿馬只有 82 公里卻**非有船閘不可**
（中間橫著海拔 26 公尺的加通湖）。⚠️ 更好的是，**拿破崙的測量團隊當年算出紅海比地中海
高、結論是「不建船閘就挖不成」而放棄**——那個結論是錯的，蘇伊士那張卡把它寫成「一個
量測誤差可以把一條全球航線推遲幾十年」。**不要把那一段刪成一句年表。**

⚠️ **巴拿馬那張卡有一個地圖上驗得到的反直覺點**：地峽在那一段是東西走向的 S 形，所以
**大西洋端（科隆）在西、太平洋端（巴拿馬城）在東**，從大西洋開往太平洋是往東南走。
實測 zoom 9.3 的畫面上科隆在左上、巴拿馬城在右下，一眼就看得出來——**取景就是為了讓
這件事成立**，改 zoom 之前先確認兩個港名還在同一個畫面裡。

⚠️ **兩條運河的 `properties.zoom` 都是實測的**（1920×873）：巴拿馬 **9.3**（123 m/px、
lat 8.639–9.600，82 公里的地峽橫躺在畫面上）；蘇伊士 **8.0**（263 m/px、lat 29.669–31.730，
北端塞德港 31.26 與南端蘇伊士 29.97 同時在框裡，尼羅河三角洲與西奈半島也進得來）。

⚠️ **PortWatch 的數字現在有六張卡在用**（曼德、臺灣海峽、巴拿馬、蘇伊士、合恩角、
好望角），而且**互相引用對方的數列**：蘇伊士 73.6 → 39.6 → 38.5 → 40.7 對好望角
48.9 → 91.4（船改繞非洲）；巴拿馬 31.7 → 20.7（2024 Q1 乾旱谷底）→ 32.2 對麥哲倫海峽
**一直是每天 4 艘上下、完全沒動**（船沒有改繞南美洲）。⚠️ **那個對比是每張卡各自的
重點，重算時六張要一起更新**，只改一張會讓另一張的論證斷掉。統計一律截到 2026-08-23。

⚠️ **好望角與麥哲倫海峽本身也是 PortWatch 追蹤的咽喉點**（那份清單 28 個，實測
`returnDistinctValues` 撈得到），所以那兩張海角卡引的是**它們自己那一列**，不是從別的
咽喉點推出來的：好望角 48.9（2023 1–10）→ 87.0（2024）→ 89.5（2025）→ 91.4（2026 1–8），
其中貨櫃船 5.9 → 20.2；麥哲倫海峽 4.1 → 4.7 → 4.0 → 4.2。⚠️ **合恩角不在那 28 個裡面**
——合恩角那張卡引的是麥哲倫海峽，卡片有寫明這件事。

⚠️ **蘇伊士那張卡刻意把「洲界」寫成事實、不叫讀者打開圖層**，理由跟博斯普魯斯完全相同
（`world-continents` 的 `maxzoom` 是 5，在 zoom 8 一個圖徵都不畫）。兩張卡的處理方式要
維持一致。

⚠️ **「雷賽布遷徙」在中文維基百科沒有條目**（`雷塞布遷徙`／`勒塞普遷徙` 實測都不存在），
所以那一筆來源連英文條目。同理 `托里霍斯-卡特條約` 也沒有中文條目，那一段併在
`維基百科 巴拿馬運河` 裡講，不另外登記。

#### 兩個海角自成一層，而它們是拿來跟運河對照的

2026-08 加進合恩角與好望角，`category` 是 **`"海角"`**（拆層之後就是
`world-picks-capes` 那一層），在 geojson 裡排在「運河」後面、「湖泊」前面。
⚠️ **不要併進「海峽」或「運河」**：海峽與運河是主要航線上的**咽喉點**，海角是那些航線
走不通時**剩下的那條路**——兩張卡的「對照重點」都靠這個分工，而拆層之後這條判準同樣
從「為什麼是兩個 `category`」升格成「為什麼是兩個核取方塊」。層內依經度由西往東排
（合恩角 −67.3° 在前）。

⚠️ **這兩張卡的核心也是一組對比，改一張要順手看另一張**：紅海危機讓繞好望角的船從每天
48.9 艘漲到 91.4 艘（那條十九世紀的老航線整條回來了），巴拿馬運河缺水時繞南美洲的船
**一艘也沒有增加**（麥哲倫海峽 4.1 → 4.2）。⚠️ 那個「一邊還原得回去、一邊還不回去」
的對比是兩張卡共同的收尾，也接著巴拿馬與蘇伊士那兩張卡既有的同一段論證——**四張要
一起維護**。

⚠️ **兩張卡共同的第二個重點是「大洋分界是一條經線」**，出自 IHO《海洋與海域的界限》
(S-23)：南大西洋／南太平洋的界線是**合恩角的經線（西經 67°16′）**，大西洋／印度洋的
界線是**厄加勒斯角的東經 20 度**。⚠️ 所以「兩大洋在好望角交會」這句常見的話**兩個角色
都錯**（不是好望角、也不是「交會」而是一條人訂的經線），而那正是收好望角進來的理由。
中文維基百科沒有對應條目（`海洋和海的界限`／`海與洋的界限` 實測都不存在），IHO 官方
PDF 的公開鏡像擋自動連線，所以來源連的是英文條目 `Borders_of_the_oceans`。

⚠️ **好望角的座標取的是英文條目那組（34°21′29″S 18°28′32″E），不是中文條目那組。**
中文維基所載的 34.3547/18.49077 偏東約 1.4 公里，已經靠近**開普角**（Cape Point，燈塔
與觀景台在那裡）——兩個岬頭相距只有 1.2 公里，是這張卡要拆穿的第二個混淆，拿錯座標
等於自己先站到錯的那個角上。實測 zoom 13：藍點落在西南側那個岬頭上（`water` 圖層打
不到），東邊那塊更高的懸崖才是開普角。

⚠️ **合恩角的座標中英文條目一致**（55°58′48″S 67°17′21″W），實測落在合恩島南端的岬頭上。
⚠️ 它**不是**南美洲大陸的最南端（那是麥哲倫海峽北岸的弗羅厄德角 53.93°S，北了兩個緯度），
也不是最南的陸地（迪亞哥·拉米雷斯群島更南）——卡片把「它被當成南端是**航海**的定義」
寫出來，不要簡化成「南美洲最南端」。

⚠️ **兩筆的 `properties.zoom` 都是實測 `m.getBounds()` 調出來的**（畫布 1920×772，比
既有那批 1920×929 的紀錄矮，所以垂直方向是保守值）：

| | zoom | 實測視野 | 為什麼是這個值 |
|---|---|---|---|
| 合恩角 | 5.6 | 903 m/px、lat −58.99 至 −52.72 | 蓬塔阿雷納斯與**麥哲倫海峽**（內側那條路）和合恩角（外側那條路）要同時在畫面上——那是卡片「麥哲倫走的是內側」那一段的憑據。⚠️ 而且它**必須小於 6**：卡片叫讀者勾「洋流」看西風漂流，那一層的 `maxzoom` 是 6（實測 5.6 時 `ocean-currents-cold-line` 6 段、標註 3 個，全是 `west-wind-drift`） |
| 好望角 | 7.8 | 290 m/px、lon 15.45–21.50、lat −35.36 至 −33.35 | 要讓**厄加勒斯角**（20.01°E／34.83°S）跟好望角同時在畫面上，「它不是非洲最南端」才在地圖上驗得到。⚠️ zoom 8.5 時東界只到 20.34，換一個窄一點的視窗厄加勒斯角就掉出去了；7.8 的東界是 21.50，留了 1.5° |

⚠️ **合恩角的取景刻意不追求「看得到南極」。** 實測 zoom 4.4 的南界已經到 −62.53、
4.2 到 −63.4，但**向量底圖在那個尺度上根本不畫南設得蘭群島與南極半島**（畫面下半整片
只有海與海底地形），把地圖拉那麼開只是換到一個什麼都沒有的畫面。「往南 650 公里就是
南極」因此只寫在卡片上，不當成取景條件。

⚠️ **好望角那張卡沒有叫讀者打開「洋流」而不加但書**：那一層的 `maxzoom` 是 6，在 zoom
7.8 一個圖徵都不畫，所以卡片把兩股洋流當事實講，並在同一段末尾說明「想看要縮到 zoom 6
以下」——處理方式跟博斯普魯斯／蘇伊士對「大洲分區」的做法一致。合恩角相反（5.6 < 6），
它的卡片就直接叫讀者勾起來。

#### 臺灣海峽：這一層唯一一條「自己家門口」的海峽

⚠️ **收它的理由是一個數字**：依 IMF PortWatch 的每日通行艘次，臺灣海峽是它追蹤的 **28
個咽喉點裡最繁忙的一條**——2025 年全年平均每天 238.8 艘次（麻六甲 234.5、朝鮮海峽
219.4、多佛 174.7、直布羅陀 135.4），貨櫃船 89.6 艘次更是第二名麻六甲（61.4）的將近
一倍半。2024 與 2026 年 1–8 月都是同一個排名（實測三年都跑過），所以「最繁忙」不是
一時的波動。⚠️ 那是各咽喉點在**各自劃定的範圍**內偵測到的「艘次」、不是噸位，卡片有
寫明這件事。⚠️ 統計截到 2026-08-23，跟曼德那張卡是同一份資料、同一個會過期的問題。

⚠️ **「冰期它是一片陸地」那一段有實物證據，不要簡化成一句推論**：漁民在澎湖水道水下
60–120 公尺撈起的下顎骨化石「澎湖 1 號」，2015 年發表於《自然．通訊》，2025 年再以
蛋白質定序確認是**丹尼索瓦人**（全世界第三件有分子證據的）。中文維基的條目名是
**「澎湖1號」**，「澎湖原人」是重新導向。

⚠️ **`properties.zoom` 是 6.3，要求是「整個臺灣島都要在畫面上」。** 實測 1920×929：
6.9 的南界只到 22.457°N，**鵝鑾鼻（21.90）會被切掉**；6.5 是 21.696（勉強，但換一個
800 px 高的視窗就掉出去）；6.3 是 **21.227–28.295**，臺灣、福建、呂宋海峽北段都在框裡。
這一筆的取景不是為了看海峽有多窄，而是看「北窄南寬」與兩岸的相對位置。

⚠️ **卡片原本寫「整條海峽都在歐亞板塊上」，那是錯的，已改。** 用點在多邊形內實測
`plates.geojson`：海峽中段、最窄處中點、澎湖、臺北、高雄**全部落在「揚子板塊」**
（Bird 2003 把歐亞板塊細分出來的子板塊），臺東才是菲律賓海板塊。這件事 `CLAUDE_TW.md`
的「六塊板塊裡有四塊在課本裡叫『歐亞板塊』」早就記著了——**寫世界主題的卡片時要記得
去讀臺灣那一份**，兩邊吃的是同一份 Bird 幾何。

⚠️ **「海峽本身沒有板塊邊界穿過」是實測過的，不是推論**：在 117.0–120.3°E、
22.4–25.6°N（開闊海面）這個框裡，三種邊界的節點數都是 **0**；模型的聚合型邊界走的是
**臺灣西部麓山帶**（120.36/23.198、120.546/23.979、120.981/24.654、121.335/24.963），
那已經在島上了。卡片因此把「模型畫在麓山帶、課本畫在花東縱谷」一起講出來。

⚠️ **搜「台灣海峽」（台不是臺）以前是零結果，那個洞已經在 `searchIndex.ts` 補掉了。**
`normalizeForMatch()` 現在會把「台」摺成「臺」再比對（只影響比對、不影響顯示），
所以兩種寫法都搜得到，`meta` 裡那句暫時繞路用的「也寫作台灣海峽」也跟著拿掉了。
⚠️ **這一筆因此變成那支正規化的回歸判準**：如果哪天搜「台灣海峽」又變成零結果，
問題在 `searchIndex.ts`，不在這一筆的 `meta`。細節與為什麼只摺這一對見 CLAUDE.md
的「主題頁搜尋」。

#### 三條海峽各自的取景與坑（2026-08）

⚠️ **`properties.zoom` 全部是實測 `m.getBounds()` 調出來的，不是算的**（見上面那條
512 圖磚的警告）。實測畫布 1920×929：

| | zoom | 實測視野 | 為什麼是這個值 |
|---|---|---|---|
| 直布羅陀 | 9 | 124 m/px、lat 35.43–36.47 | 14.2 公里的最窄處在畫面上有 115 px，塔里法、直布羅陀、丹吉爾、休達同時在框裡 |
| 博斯普魯斯 | 10.5 | 41 m/px、lat 40.91–41.25 | 整條 31 公里的海峽剛好進得來（⚠️ **zoom 11 會把北口切掉**，實測北界只到 41.203，而海峽北端在 41.24） |
| 曼德 | 7.2 | 520 m/px、lat 10.41–14.74 | 要讓「板塊邊界」與「東非大裂谷」兩層同時進畫面（⚠️ **zoom 7.5 時裂谷北端 14.2°N 離上緣只剩 0.14°**，換一個矮一點的視窗就會掉出去） |

⚠️ **曼德那張卡的「對照重點」叫讀者打開「板塊邊界」與「板塊與地形」（東非大裂谷），那兩層的
`maxzoom` 都是 8**——所以 `properties.zoom` 必須小於 8，這是硬條件不是偏好。實測 7.2
兩層都畫得出來（`張裂型邊界`＋`錯動型邊界`＋`東非大裂谷` 都查得到圖徵）。**改這個 zoom
之前先確認那兩層還畫得出來**，比照麻六甲那次「行星風系 maxzoom 4」的教訓。

⚠️ **博斯普魯斯那張卡刻意不叫讀者打開「大洲分區」**：那一層的 `maxzoom` 是 5，在 zoom
10.5 一個圖徵都不畫。卡片改成把洲界當成一件事實講，並在「對照重點」括號說明想看洲界
要先縮到 zoom 5 以下。⚠️ 洲界那條切線是 `scripts/lib/continents.mjs` 裡的
「達達尼爾（26.2°E, 40.05°N）到博斯普魯斯（29.02°E, 41.05°N）」，卡片說的「本站用的
也是一條簡化的直線」指的就是它。

⚠️ **三個座標都實測落在水上**：除了在地圖上用 `queryRenderedFeatures` 打 `water` 圖層，
也拿 `world-continents.geojson`（Natural Earth 1:50m）做過點在多邊形內測試，三筆都不在
任何大洲多邊形裡——連 700 公尺寬的博斯普魯斯在 1:50m 的資料上都是通的。

⚠️ **博斯普魯斯取的是最窄處（約 700 公尺，魯梅利堡與安納托利亞堡之間）水道中間的
41.083N/29.061E，不是維基百科條目所載的北口座標**（41.11944/29.07528）——那張卡講的就是
那 700 公尺。實測 zoom 10.5 橫向掃過去是 20 個像素（約 814 公尺，帶子略斜所以比 700 大）。

⚠️ **曼德海峽的 PortWatch 那一段是全站第二處有時效的內容**（第一處是荷姆茲那張卡）。
數字是用 IMF PortWatch 的 ArcGIS FeatureServer 對 `n_total` 取期間平均算出來的，
統計截到 **2026-08-23**：曼德 74.6（2023-01～10）→ 32.5（2024）→ 28.2（2026-07-25～08-23），
同期好望角 48.9 → 87.0 → 89.9、蘇伊士運河 73.6 → 39.6 → 41.1。⚠️ **三個數字要一起
更新**，只改曼德那一個會讓「船沒有消失，只是改走遠路」這句話失去證據。重算方式寫在
`sourceLinks.ts` 那一筆的註解裡。

⚠️ **麻六甲海峽跟荷姆茲海峽是當成一組收的**（都是 `category: "海洋"`）：它們不是兩個
平行的例子，而是**同一條供應線上前後相接的兩個窄口**——臺灣 2025 年 61.7% 的原油出了
荷姆茲之後，絕大多數就是穿過麻六甲再北上。⚠️ **改動其中一張卡時要順手看另一張**，
比照「湖泊」那一組的既有規則；麻六甲那張卡直接引用了荷姆茲卡上的 61.7%。

⚠️ **這一筆最值得教的是「『最窄處』是 38 公里還是 2.8 公里，看界線畫到哪裡」。**
海峽本身最窄約 38 公里；常被引用的 2.8 公里是**菲利普水道**（Phillip Channel），它在
東南邊的**新加坡海峽**裡。美國能源資訊署把它當成整個麻六甲咽喉點的最窄處來寫，嚴格說
已經是另一條海峽了。**改動這張卡時不要把這一段化簡成單一個數字**——那正是它被收進來
的理由（比照世界之最那幾張卡「最取決於怎麼定義與怎麼量」的既有判準）。

⚠️ **座標取海峽本身最窄處主航道中間的一點（1.157N/103.455E，馬來西亞丹絨比艾與印尼
卡里門群島之間），不是菲利普水道。** 實測那個點落在水上（`queryRenderedFeatures` 打
`water` 圖層是 1）。⚠️ **不要改標到 2.8 公里那個瓶頸**：那會讓一個叫「麻六甲海峽」的
圖徵落在新加坡海峽裡，等於把卡片上剛講清楚的界線問題自己弄混。

⚠️ **`properties.zoom` 是 5.9，那是為了讓 930 公里的整條漏斗進得了畫面，實測調的。**
這一層的取景一向逐筆決定，而這一筆的難處是**點在東南端、海峽卻往西北延伸 930 公里**，
框整條就一定會在南邊浪費一片海。實測（1920×873 畫布）：6.5 只框得到東南三分之一
（北界 4.54°N）；5.9 的北界是 **6.28°N**，蘇門答臘北端與檳城一帶都進得來，畫面上讀得出
「西北寬、東南收窄」的漏斗，同時看得到吉隆坡、馬六甲市與新加坡沿著海峽排開。

⚠️ **卡片的「對照重點」叫讀者看的是「赤道」那條參考線，不是「行星風系」那一層。**
海峽正好橫在赤道無風帶上（實測赤道線就在藍點下方 98 px），而**`wind-belts` 的
`maxzoom` 是 4**——在這一筆的預設視角 zoom 5.9 那一層一個圖徵都不畫。⚠️ 初稿寫成
「把行星風系打開」，那是一句**畫面上兌現不了的話**，已改成先講赤道參考線、再括號說明
想看整個風系要縮到 zoom 4 以下。加任何「打開某一層對照」的句子之前，**先確認那一層在
這一筆的 `properties.zoom` 上真的畫得出來**。

⚠️ **石油流量是 EIA 2016 年的數字（每天 1,600 萬桶）**，來源是 EIA 專講麻六甲的那一篇
（2017-08-11）。⚠️ **不要拿它跟荷姆茲卡上的 2022 年 2,100 萬桶直接相減**——不同年份、
不同口徑，卡片已經把這件事寫進內文。荷姆茲那一篇整篇沒有提到麻六甲，兩張卡連的是
兩個不同的 EIA 頁面。

⚠️ **`name.zh` 用臺灣慣用的「麻六甲海峽」，「馬六甲海峽」寫進 `meta`**（比照裏海／裡海、
土渕／土淵）。中文維基的條目名是「马六甲海峡」，`sourceLinks.ts` 連的就是那個網址；
「麻六甲海峽」在維基上是重新導向。實測「麻六甲」「馬六甲海峽」「Malacca」三種都搜得到。

⚠️ **「馬六甲困境」在中文維基百科沒有條目**（`马六甲困局`／`馬六甲困境` 兩個標題實測
都不存在），所以那一筆來源連英文條目——胡錦濤 2003 年提出、中國進口原油約八成經過
這裡，只有那一篇寫得完整。

⚠️ **土渕海峽（世界最窄的海峽，9.93 公尺）收的是「一個『最』是怎麼被認定下來的」**，
不是那個數字本身。日本香川縣土庄町 1990 年第一次向金氏世界紀錄申請就被退回，理由**不是
量錯了，而是「沒有國家機關的證明」**；町方於是設法讓「土渕海峽」這個名稱登載到國土地理院
的地形圖上，再重新申請，1996 年 1 月才通過。連名字本身都是為了申請才取的——「土庄」的
土加上對岸「渕崎」的渕（不過 1921 年的《小豆郡誌》裡已經出現過）。**改動這張卡時不要把
退件與登載地形圖那一段拿掉**，那是它被收進來的理由。

⚠️ **它跟已下架的「世界之最」那組是同一條線上的內容，判準卻不同**：日和山那一筆講的是
「怎樣才算一座山，日本的答案是有沒有登載在國土地理院的地形圖上」，這一筆講的是**同一個
機關、同一份地形圖，決定了一條水道算不算海峽**。卡片裡因此有一句話直接指向日和山——
⚠️ 但那是**當成一件世界上的事實在講**，沒有叫讀者去打開任何圖層（那兩層已經下架）。
重新設計「世界之最」時這兩筆值得放在一起讀。

⚠️ **`properties.zoom` 是 16，那正好是 `MapView` 寫死的 `maxZoom`，不是隨手填的。**
那個緯度下 zoom 16 **實測是每像素約 1 公尺**（1920 px 寬的畫布量到 2 公里），而那條水道
橫向只掃得到 **5 個像素**（沿著西北－東南向斜穿過藍點，用 `queryRenderedFeatures` 逐像素
打 `water` 圖層量出來的）。⚠️ **5 公尺比真實的 9.93 公尺還窄，那不是量錯**：向量圖磚的
海岸線是化簡過的，最窄處被畫得比實際更緊；而且帶子是斜的，垂直方向的寬度比橫向掃到的
5 px 更小。再遠一級就完全看不出有水，而 16 也剛好讓 2.5 公里長的整條海峽橫躺在畫面上。

⚠️ **算尺度時不要用 256 圖磚的那條公式。** maplibre 的 `zoom` 是以 **512 px 圖磚**定義的
（世界寬度＝`512 × 2^zoom` px），所以每像素公尺數是 `78271.5 × cos(緯度) / 2^zoom`，
不是課本上常見的 `156543 × cos(緯度) / 2^zoom`——用錯的那條會把視野算成兩倍寬。這裡踩過
一次（土渕那段原本寫「約 2 公尺一個像素」，就是這麼來的）。**本檔案所有的取景結論一律以
`m.getBounds()` 實測為準**，不要用公式回推。

⚠️ **「看起來像水道在那裡斷掉」是預期的，不是底圖壞了。** 最窄處正好架著永代橋（香川縣道
26 號），而這一層自己的藍點半徑 6–7 px 又壓在正上方，所以那 5 個像素的藍色細線在點的位置
被蓋掉了——`queryRenderedFeatures` 打在正中心仍然查得到 `water`。這件事寫在圖層的
`notes` 裡。⚠️ **不要為了「讓它看得見」去改成別的幾何或加一條線**：那等於用畫的去補一個
底圖本來就畫得出來的東西。

⚠️ **座標取最窄處（永代橋一帶）34°29′11.30″N 134°11′10.26″E，取自維基百科條目所載的
度分秒**（實測 OpenStreetMap 標在同一處的「土渕海峡」展望點是 34.48641/134.18631，
相差不到 10 公尺）。⚠️ **不要改用國土地理院地名檢索回傳的那個座標**
（`msearch.gsi.go.jp/address-search` 打「土渕海峡」回 34.4912/134.1744）——那是整條
2.5 公里長海峽的**標註錨點**，往北約 1 公里，不是最窄處。那支 API 另有用處：它證明那筆
地形圖登載至今仍在，也就是這一筆的官方依據還成立。

⚠️ **`name.zh` 用「土渕海峽」（中文維基的條目名），「土淵海峽」寫進 `meta`**：渕是淵的
異體字，兩種寫法都在流通。比照裏海「也寫作裡海」的既有做法——`meta` 在搜尋 haystack 裡，
所以打哪一種都找得到（實測「土淵海峽」「土渕海峽」「Dofuchi」「世界最窄」四種都命中）。

⚠️ **死海、裏海、貝加爾湖是當成一組收的**（`category: "湖泊"`，2026-09 加入蘇必略湖
之後是四張），每張卡各自把「湖」推到一個極端，而且互相指來指去——**改動其中一張時要
順手看另外三張**：

| | 這一張的極端 | 內容重點 |
|---|---|---|
| 蘇必略湖 | 最大的**淡水**湖面（82,100 km²） | 那個「最大」量的是**面積**，而且要先講好五大湖算幾個湖 |
| 死海 | 最低的**湖面**（約 −440 m） | 「陸地最低點」是一個**每年在變**的數字 |
| 裏海 | 最大的湖（37 萬 km²） | 「算海還是算湖」直接決定五國怎麼分石油 |
| 貝加爾湖 | 最深（1,642 m）、最老、水最多 | 它是**大陸裂谷**，跟東非大裂谷同一類 |

⚠️ **死海那張卡的數字一定要帶年份**：1930 年代 −390 公尺、2026 年約 −440 公尺，
每年還在掉一公尺以上。卡片的第一段講的就是這件事（「遇到最低最高，先問哪一年量的」），
所以**不要把年份拿掉去換一個看起來乾淨的數字**。來源連 USGS 的 Earthshots：那一頁把
1973–2024 的衛星影像排在一起，湖在縮這件事看得見，而且不會過期。

⚠️ **貝加爾湖的深度有好幾個流傳的數字**（1,642／1,700／1,741 公尺），本站採 1,642
（現行測深值，英文維基與多數工具書一致）。⚠️ **中文維基的 infobox 寫 1,742，那是舊值**
——所以那張卡的數字**刻意跟中文維基不同**，不是抄錯了。

⚠️ 四筆都只畫湖**中央的一點**（本站沒有世界湖泊的面圖層），逐筆取景：蘇必略湖 5.6
（見下）、死海 8.4（整個湖含南端蒸發池 ＋ 耶路撒冷與安曼）、裏海 4.8（五個沿岸國同時
在畫面上）、貝加爾湖 5.8（整條新月形 ＋ 伊爾庫茨克）。四個都是實測調的。

#### 蘇必略湖：「湖泊」那一層的第四筆，收的是「最大淡水湖要先講好怎麼算」

2026-09 加進 `world-picks-lakes`（`category: "湖泊"`，在 geojson 裡排在死海**前面**——
四筆依經度由西往東，蘇必略湖 −87.1°、死海 35.5°、裏海 50.7°、貝加爾湖 108.2°，
比照「海峽」那一層的既有規則）。

⚠️ **這張卡的重點是三道「先決條件」，不是那 82,100 平方公里**，三件事缺一不可：
（a）「最大」量的是**面積**——論蓄水量它只有 12,100 km³，排在貝加爾湖（23,600）與
坦干伊喀湖之後，因為它平均只有 147 公尺深；（b）要先**排除鹹水湖**，否則第一名是
裏海（37 萬 km²，那是同一層隔壁那張卡）；（c）要先接受「密西根湖與休倫湖分開算」
這個**習慣**——兩者由麥基諾水道相連（寬 8 公里、深 90 公尺）、水位始終相同，水文學上
是同一個湖，合計 117,400 km² 比它大得多。**改動這張卡時不要把（c）拿掉**，那是它被
收進來的理由，跟同層另外三張「最取決於怎麼量」是同一條線。

⚠️ **面積採美國環保署那張〈五大湖物理特徵〉表的 82,100 km²，不是中文維基的 82,414。**
比照本站「數值型資料以主管機關為準」的既有規則（也比照貝加爾湖深度刻意跟中文維基的
infobox 不同）。⚠️ **那張表同時是蓄水量 12,100 km³、最大深度 406 m、滯留時間 191 年
與卡片裡 117,400 km²（57,800 ＋ 59,600）的出處**，四個數字全部來自同一份，換來源
就要整組換。

⚠️ **座標取條目資訊框的 47°7′N 87°5′W（−87.08333/47.11667）**，落在基威諾半島東南方的
開闊水面上——實測 `queryRenderedFeatures` 打向量底圖的 `water` 圖層是命中的。

⚠️ **`properties.zoom` 是 5.6，那是取景在兩件事之間權衡出來的**（1920×803 實測）：
往下調到 5.4 整個五大湖水系都進得來但蘇必略湖只剩 440 px 寬、讀不出主角是誰；往上調到
5.9 湖是漂亮了，**但麥基諾水道與休倫湖掉出畫面**，而那正好是卡片第三段講的東西。
5.6 兩件事同時成立：湖身 535 px（西端德盧斯在 x=614，清得過詳情面板的 360）、
蘇聖瑪麗、密西根湖與休倫湖的名字都在畫面上。**改這個數字之前先把第三段讀一次。**

⚠️ **`name.zh` 用「蘇必略湖」（中文維基 zh-tw 的條目名，也是教育部教育百科的詞條），
「蘇必利爾湖」寫進 `meta`**——比照裏海／裡海、麻六甲／馬六甲、土渕／土淵的既有做法。
`meta` 在搜尋 haystack 裡，實測「蘇必略湖」「蘇必利爾湖」「Lake Superior」三種都命中。

⚠️ **常見的別譯寫進 `meta`，那是刻意的**：裏海「也寫作裡海」、荷姆茲海峽「又譯霍爾木茲
海峽」。`meta` 在搜尋 haystack 裡（見「搜尋索引」那節），所以打哪一種寫法都找得到，
而讀者也剛好需要知道這件事。⚠️ **不要改用 `shortName` 塞別名**——那個欄位的語意是
「地圖上實際印出來的短名」，這一層根本沒有標註。

#### 編者選集為什麼是九個圖層

2026-08 之前是兩個核取方塊：`world-picks`（17 個點）與 `world-picks-areas`（2 條線），
類別只是 `browse: { groupBy: "category" }` 從 `properties.category` 切出來的**清單分組**。
那個擺法有兩個問題——**要看海峽就得連湖泊、火山一起打開**（分組不是開關），而那一大段
`description` 從維蘇威一路講到貝加爾湖，勾任何一類的人都得讀完整段。現在每個類別自己
就是一個核取方塊，`description`、`notes` 與 `sources` 也各自歸位（原本 24 則 notes
逐則搬到對應的那一層，只有第一則「這是編輯選集、不是排名」九層都留）。

三件事跟這個拆法綁在一起，改動前請一起看：

1. **幾何仍然只有兩份**（`world-picks.geojson` 19 筆、`world-picks-areas.geojson` 2 筆）。
   九層靠**圖層層級的 `featureIds`** 從那兩份切出來（機制與完整判準見 CLAUDE.md 的
   「一份 geojson 切給多個圖層」），共用 `resolveLayerData` 的同一個快取項目——
   **九層全開實測只有兩次 geojson 請求**。⚠️ 也因此 `searchIndex.ts` 必須自己套同一份
   `featureIds`：它是全站唯一直接讀 `layer.source` 而繞過切分結果的地方。
2. **內容 collection 九層共用 `world-picks`**（拆層當下那 19 份內容檔與它的分片一個字
   都沒動，id 在 collection 內唯一就夠；同月加的馬里亞納海溝與 2026-09 加的蘇必略湖
   直接放進同一個 collection，現在是 21 份）。實測九層每一層點第一筆，卡片、來源連結與面板標題
   都跟拆之前逐字相同。
3. **九層全部 `exemptFromMaxActive`**（見下一節）。

⚠️ **有範圍的那兩筆仍然是兩個獨立的線圖層，不是「點圖層的範圍版」。** 百慕達三角是
一片海域、東非大裂谷是一條橫跨半個非洲的裂谷，畫成點就等於把它們唯一的地理內容丟掉；
而 `LayerRender` 一個圖層只能一種幾何。

⚠️ **兩層都是 `reference` 中性灰虛線，不是分類色**，而且那條虛線是必要的。
⚠️ 但**兩層的理由相反，`notes` 要分開講**：百慕達三角**沒有官方界線**（美國海軍與
海岸防衛隊明講它不在任何官方海圖上），實線會讓它看起來像一條真實的界線，而那正好是
這張卡要拆穿的東西；東非大裂谷**是真實的地形**，但真正的裂谷是一組數十公里寬的地塹
與斷層帶，把它畫成一條線本身就已經是示意（比照世界主要山脈的中軸線）。**不要把這兩層
的語意收斂成「這裡畫的東西都不存在」**——那對東非大裂谷是錯的。拆成兩層之後這件事反而
好講了：兩則 `notes` 各自寫自己的理由，而且互相點名對方。

因此它們跟同主題的緯度參考線、國際換日線長得一樣——那是刻意的，它們確實是同一類東西，
區辨靠沿線標註（所以 `label` 在這兩層也是必要條件）。

⚠️ **東非大裂谷的東支在「板塊邊界」那一層上一條線都沒有，那不是漏畫。** 實測 Bird
(2003) 在 34–38.5°E／−6–4.5°N 這個框裡**三種邊界各 0 個點**：那份模型把努比亞／
索馬利亞的界線放在**西支**（艾伯丁裂谷），肯亞的格雷戈里裂谷不在上面。這正是這一筆
值得收的理由之一——它補上了模型層看不到的另一半，而東支那一串火山由「火山帶」那一層
補齊。**這件事同時寫在圖層的 `notes` 與卡片的「對照重點」裡，不要當成資料缺漏去「修」。**

⚠️ **幾何是人工描的，控制點一律用公開的地名座標**（達納基爾窪地、圖爾卡納湖、奈瓦沙湖、
納特龍湖、艾伯特湖、坦干伊喀湖、馬拉威湖…），比照百慕達三角的三個頂點。其中爾塔阿雷
火山（40.666/13.601）與尼拉貢戈火山（29.25/−1.52）**逐位元取自 `volcanoes.geojson`**，
所以跟「火山帶」那一層完全重合。⚠️ 產物是 **2 段的 MultiLineString**（東支、西支），
不是一條線——兩支中間夾著的維多利亞湖並不在裂谷裡，接成一條就等於把那件事講錯了。
⚠️ **不要把量出來的長度寫進卡片**（實測東支 2,432 km、西支 2,621 km）：那是從示意
幾何量出來的，比照中洋脊的既有規則，卡片上寫的是公開的常用值。

⚠️ **這兩層不搶 `MAX_ACTIVE_BY_KIND.line` 的三個名額**（`exemptFromMaxActive`，
見下一節）。拆層之前它們合起來吃一個名額，現在是零。

⚠️ 百慕達三角那張卡的重點**不是「哪裡有怪事」，而是怎麼查證**：先問「這個地方有界線
嗎」（沒有，各家畫法面積差三倍），再問「事故率跟別的地方比呢」（NOAA：沒有證據顯示
比其他繁忙海域更常發生）。**改動這張卡時不要把這兩個問題拿掉**，那是它被收進來的
理由；把它寫成獵奇故事就違反本站的內容誠信。

#### 九層全部豁免名額上限，那是拆層的**前提**不是附帶效果

拆之前這一組吃掉一個圓點名額與一個線名額；照原樣拆下去會變成七個圓點圖層搶
`MAX_ACTIVE_BY_KIND.circle` 的四個名額——**一次只看得到四類，比拆之前（一個勾選項
看得到全部 17 筆）更難用**。所以九層一律宣告 `exemptFromMaxActive: true`。

判準只有一條，寫在 `registry/types.ts` 上：**這九層用的是非分類的固定角色色**
（點 `place` 藍、線 `reference` 中性灰），多開一層畫面上一個新色相都沒有增加，而
`MAX_ACTIVE_BY_KIND` 封的正是「同時顯示幾個**分類色**還讀得動」——那條論證對它們
根本不成立。

⚠️ **那是豁免，不是上限失效。** 實測：九層全開的同時，另外四個圓點圖層（世界重要城市、
世界人口分布、古文明發源地、火山帶）仍然勾滿四個，第五個（全球地震帶）照樣 `disabled`。
⚠️ **不要因為想多開幾層就去調高 `MAX_ACTIVE_BY_KIND`**（本檔舊版寫的「不要為此把上限
調到 5」仍然有效，而且理由沒變）：那個數字等於色票長度，是 all-pairs 驗證的前提。
⚠️ 也**不要把這個旗標掛到吃分類色票的圖層上**（items 的 palette、水庫／古蹟／人口那
幾條色階都算）。

⚠️ 附帶的代價要知道：兩個線層跟緯度參考線、國際換日線**長得一模一樣**，四條灰虛線
現在可以同框。那不是新問題（那三層本來就同樣式、同時開得起來），區辨一律靠沿線標註
——所以那兩層的 `label` 是必要條件，不是裝飾。

#### ⚠️ 「世界最低的山」沒有權威名單，這件事必須寫出來

怎樣才算一座山，各國定義並不相同：日本以「有沒有登載在國土地理院的地形圖上」為準，
多數國家沒有這種制度。所以這一筆取的是**有官方測量、又正式登載在地形圖上的最低者**，
而且它還是**人工築山**、高度還是**海嘯削掉山頭之後**重測的（震災前 6.05 公尺，當時
日本最低的是大阪天保山 4.53 公尺）。這幾件事全部寫在圖層的 `notes` 與卡片的 `facts`
裡——那正是這一筆的教學價值（「最」取決於怎麼定義與怎麼量），不是要遮起來的雜訊。

#### ⚠️ 示意警語掛在內容檔上，不在圖層上

`world-superlative-ranges` **沒有** `schematic: true`：兩筆的性質不同——安地斯是由範圍
面算出來的中軸線（示意），中洋脊是 Bird (2003) 的模型幾何（跟「板塊邊界」一樣不標
示意）。`FeatureCard` 是 `feature?.schematic ?? fallback.schematic`，所以警語寫在
`src/content/geo/world-superlatives/longest-land-range.json` 的 `schematic` 上，只出現在
該出現的那一張卡。代價是抽屜那一列不會顯示「（教學示意圖，非精確界線）」，改成寫在
`notes` 裡。

#### 這一組又把線圖層的名額擠得更緊

世界主題現在有**七個**線圖層在搶 `MAX_ACTIVE_BY_KIND.line` 的三個名額（緯度參考線、
國際換日線、行星風系、洋流、板塊邊界、世界主要河流、世界主要山脈）。
**這是預期行為，不要為此把上限調到 4**——理由見上面「合併之後線圖層變成七個」那一節，
整段色彩分析都建立在「最多三條線同時出現」的前提上。

⚠️ **有兩層在這個數之外：**「世界之最・山脈」已經下架；而世界櫥窗的「板塊與地形」與
「傳說中的地方」宣告了 `exemptFromMaxActive`（它們是 `reference` 中性灰虛線，判準見
上面那一節），所以它們既不佔名額、也不會被擠掉。

### 板塊與板塊邊界：全站第一個「面帶名字」的圖層

> ⚠️ **臺灣主題也有這兩層**（`tw-plates`／`tw-plate-boundaries`，2026-08 新增）：
> 同一份 Bird (2003) 模型裁到臺灣周邊，六塊板塊、三種邊界。
> 那兩層的裁切、色彩衝突（三個顏色在臺灣主題各撞一個既有色）與內容決定
> **全部寫在 `CLAUDE_TW.md`**，這一節只講世界主題那兩層。

「地體構造」群組底下的 `plates`（52 塊板塊，面）與 `plate-boundaries`（三種邊界，線）
是同一份資料的兩層，取得邏輯關在 `scripts/lib/plates.mjs`。

#### 為什麼是 Bird (2003) 而不是 USGS 或 Natural Earth

`plate-boundaries` 原本掛著 `status: "planned"`、`sources: ["USGS"]`，但 USGS 只有靜態
圖片、Natural Earth 沒有板塊。**Bird (2003) 的 PB2002 是唯一一份公開、帶完整幾何、
而且每一小段邊界都標了種類的全球模型**——課本講的「聚合、張裂、錯動三種板塊邊界」
要畫得出來就得有那個分類欄位。幾何取自 Hugo Ahlenius / Nordpil 轉製的 GeoJSON 版。

⚠️ **授權是 ODC-BY 1.0，要求標示出處**，所以兩層的 `sources` 都必須同時列
`Peter Bird (2003) 板塊模型` 與 `Nordpil 板塊資料集`，少一個就違反授權（比照
`tw-rivers` 對 OpenStreetMap 的 ODbL 署名義務）。

#### ⚠️ 不要改用 `PB2002_boundaries.json`，那份分不出三種邊界

同一個資料夾裡的 `PB2002_boundaries.json` 只有 241 條乾淨的線、檔案也小得多，很容易
以為它才是對的來源。但它的 `Type` 欄位**只分得出 subduction（65 條）與空字串
（176 條）**，畫不出三分類。三分類在 10 MB 的 `PB2002_steps.json` 裡（5,824 段，
每段帶 `STEPCLASS`：OSR／CRB → 張裂、SUB／OCB／CCB → 聚合、OTF／CTF → 錯動）。
10 MB 只在建置期下載，產物是 87 KB。

#### ⚠️ 邊界的產物**刻意只有三筆圖徵**

每一種邊界是一個 MultiLineString，裡面有幾百段（張裂 661、聚合 205、錯動 716）。
一段一筆會壞掉一件事：註冊表用 `LayerItem.featureIds` 把子項目從母圖層切出來，而那是
一份**寫在 `themes/global.ts` 裡的 id 清單**——1,582 行 id 是不能接受的。而逐段點選也
沒有教學意義（要點開的是「這是哪一種邊界」不是「這是哪一小段」），所以三筆剛剛好。

#### 52 塊板塊各有一張說明卡

原本 `plates` 的 `detail` 就宣告成 `{ type: "geo", collection: "plates" }`，但那個目錄
一直是空的，於是 52 塊全部走 `FeatureCard` 的 fallback——每一張卡上只有名字、
`meta`（「次要板塊・544 萬 km²」）與**逐字相同**的圖層說明。也就是說點哪一塊板塊，
讀到的都是同一段「地球表面分成 52 塊板塊」，等於沒有逐筆內容。2026-08 補齊
`src/content/geo/plates/plate-<代碼>.json` 共 52 份。

每一份的骨架跟世界主題其他內容檔一致：`subtitle` ＋ 四格 `stats`（面積、分類，加上
兩格逐筆不同的欄位）＋ 三到五條 `facts`，**最後一條一律是「對照重點」**，明講該跟
哪一個圖層疊著看（火山帶、全球地震帶、板塊邊界、世界主要山脈、大洲分區、編者選集那幾層…）。

⚠️ **敘述性事實逐塊取自維基百科條目，數值一律不抄**：面積與分類仍然來自 Bird (2003)
的幾何與 `lib/plates.mjs` 的對照表（比照五大山脈與颱風的既有做法）。歐亞、北美這種
「模型值跟課本值差很多」的板塊，卡片上**兩個數字都寫出來並說明差在哪裡**（比照
`tw-plates` 的既有做法），不要只留一個看起來乾淨的數字。

⚠️ **`sources` 一律三筆**：`維基百科 <條目名>` ＋ Bird ＋ Nordpil。後兩筆是 ODC-BY
要求的署名，少一個就違反授權；第一筆要逐條登記進 `sourceLinks.ts`（泛稱的「維基百科」
不在那份表裡，會渲染成沒有連結的純文字）。

⚠️ **維基百科的條目名與本站的板塊中文名不一定相同**（本站的「湯加板塊」在維基百科是
〈東加板塊〉、「揚子板塊」是〈華南板塊〉、「新海布里地板塊」是〈新赫布里底板塊〉、
「加拉巴哥板塊」是〈加拉帕戈斯板塊〉、「索馬利亞板塊」是〈索馬里板塊〉…）。
`sourceLinks.ts` 裡一律用**本站的名字**當標籤與網址，靠維基百科 `/zh-tw/` 路徑自己
重新導向與做字形轉換——52 條實測全部回 200 而且落在正確的條目上。改動時重跑那個
檢查，不要只看有沒有拼錯字。

⚠️ **菲律賓海、揚子、巽他、阿穆爾、沖繩五塊的來源標籤與臺灣主題共用**（早就登記在
`sourceLinks.ts` 的臺灣那一段），所以世界主題這一段刻意不重複登記那五筆。

⚠️ **這 52 份一度讓主 bundle 多背 gzip 28.7 KB**（`content/index.ts` 當時對
`./geo/*/*.json` 是 `eager: true`）。**現在不會了**——地理要素說明整組改成
`public/data/geo-content/` 的延遲載入分片，主 chunk 反而比補這 52 張卡之前還小了
一半（375.2 → 165.2 KB）。做法與踩過的坑見上面「地理要素說明為什麼要延遲載入」。
⚠️ 連帶的規矩：**改完 `src/content/geo/plates/` 底下任何一份就要跑
`npm run build:geo-content`**，忘了跑的話卡片顯示的是上一版的文字（`npm run validate`
會擋）。

#### 三種邊界各有一張說明卡，而三個 id 必須是同一個字串

課本講的「三種板塊邊界」要講的是**各自怎麼動、造出什麼地形、哪裡看得到**，圖層說明
只塞得下一句話，所以三種各有一份內容檔：`src/content/geo/plate-boundaries/{divergent,
convergent,transform}.json`，圖層的 `detail` 因此是 `{ type: "geo", collection:
"plate-boundaries" }`。點地圖上的線、或點抽屜裡的子項目名稱，開的是同一張卡。

⚠️ **`detail` 不可以退回 `"none"`。** 除了說明沒地方放之外還會壞掉一件事：
`handleItemNameClick` 照樣會 `setSelected`，而 `DetailCard` 對 `none` 回 `null`——
點抽屜裡的「張裂型邊界」會開出一張**空白面板**（`data-detail-open` 仍是 true），
完全靜默。垂直植被帶那一層也踩過同一個坑。

⚠️ **geojson 的 `properties.id` 不帶 `boundary-` 前綴**（早期版本帶，2026-08 拿掉並
重跑了 `build:geodata`）。比照交通軸線的既有規則：**geojson 的 id、註冊表的 item id、
內容檔的檔名必須是同一個字串**。不一致的話點子項目名稱傳的是 item id、點地圖上的線
傳的是圖徵 id，兩條路徑會開出不同的卡；而且「只顯示這一筆」的 `setFilter` 會一筆都
比對不到、整層消失。

串接規則：相同 `PLATEBOUND`、相同分類、而且前一段的終點等於後一段的起點，就併成一條線
（實測 5,613 對接得上、只有 8 對接不上）。不併的話會得到 5,824 條各三十幾個點的碎線，
Douglas–Peucker 幾乎砍不掉任何東西。

⚠️ 跟國際換日線同一個坑：**每一段都不可以跨越 ±180**，跨了 maplibre 會畫一條繞過整個
地球的橫線而且不報錯。transform 逐段檢查經度跨距（實測最長的一段只跨 57.5°）。

#### ⚠️ 面積一定要用球面公式，而且總和就是自我檢查

`geometryAreaKm2()` 用的是球面多邊形面積（standard spherical excess）。**不可以用平面
shoelace**：那算出來的單位是「平方度」，高緯度會嚴重高估，南極板塊會變得比太平洋板塊
還大。

**52 塊板塊鋪滿整個地球，所以面積總和必須等於地球表面積**——實測 510.1 百萬 km²，
誤差 -0.01%。build 時對不上就直接失敗。那是唯一能抓到「投影或幾何弄錯」的檢查，
比照國家公園的 `officialHa` 交叉比對。

⚠️ **算出來的面積跟課本／維基百科的數字對不起來是正常的**，不要「修正」它：那些數字
多半把子板塊算進母板塊（北美板塊 7,590 萬 km² 含鄂霍次克與格陵蘭，Bird 分開算是
5,543 萬；歐亞 6,780 萬 vs 4,856 萬）。圖層的 `notes` 有交代。

#### ⚠️ 主要板塊是 8 塊，不是課本說的 7 塊

Bird 把課本合稱的「印澳板塊」分成印度板塊與澳洲板塊兩塊。**不要為了湊 7 塊把其中一塊
降級**——那會讓清單跟地圖上的界線對不起來。這件事寫在 `notes` 裡。

中文名與「主要／次要／微板塊」的分類逐筆取自維基百科〈板塊列表〉（zh-tw），對照表在
`lib/plates.mjs` 的 `PLATES`（52 筆，代碼對不到就讓建置失敗）。比照五大山脈與颱風的
既有做法：維基百科是次級來源，只拿來查名稱，數值一律自己算。

⚠️ 分類是**依維基百科的傳統分法**，不是依面積——索馬利亞板塊（次要，1,915 萬 km²）比
印度板塊（主要，1,243 萬）還大。那不是排序錯了。

#### 面圖層的標註是新加的算繪能力

`LayerRender` 的 fill 變體多了 `label`，`geo.ts` 的 fill 分支因此會多開一個
`${instanceId}-label` 的 symbol 圖層（`symbol-placement: point`）。板塊是第一個
用到它的圖層——「標出各板塊」如果沒有名字就等於沒做；第二個是臺灣主題的
`tw-eez`（四片專屬經濟海域）。

- **不需要在建置期算形心**：maplibre 對多邊形會自己算錨點。
- ⚠️ **但那個錨點是逐圖磚算的，所以同一塊面放大之後會被重複標註。** 實測 `tw-eez`
  沒設上限時 zoom 5 每片 1 個、zoom 6 變 4 個、**zoom 8 的臺灣那一片有 6 個「臺灣」
  散在海上**。板塊沒踩到只是因為它 `maxzoom: 8` 而且實際只在 zoom 1–3 看。
  `label.maxzoom` 就是為此加的（**只擋標註、不擋面**）；不要改成在建置期算形心去繞過它。
- ⚠️ **MultiPolygon 的每一塊都會拿到一個標註。** 實測只有 4 塊板塊是 MultiPolygon
  （太平洋 3 塊、澳洲 2 塊、克馬德克與巴爾莫勒爾礁各 2 塊），其中太平洋的兩大塊是
  ±180 兩側的真實兩半、標兩次是對的，真正多餘的只有兩個小碎塊。**這是已知行為，
  不是 bug。**
- ⚠️ **文字色刻意不用圖層色**（`geo.ts` 的 `FILL_LABEL_COLOR`）：面已經是那個色相的
  半透明色塊，字再同色就糊進自己的底色裡。線與圓點沒有這個問題。
- `label.minzoom: 2` 是實測調的：zoom 1 的全球視角上 52 個板塊名互相碰撞到只剩幾個。
- ⚠️ `geoHitLayerIds()` **要把面的標註一起綁**：標註的文字方塊會溢出到鄰接板塊上，
  不綁的話點在字上會開到旁邊那一塊的卡片。

#### ⚠️ `plates` 的 `fillOpacity` 是 0，那不是忘了設

52 塊板塊**鋪滿每一個像素**，任何均勻的面染都只是把整張圖壓暗一階，資訊量是零
（跟縣市界那種「面染標出範圍」的處境完全相反）。這一層畫出來的是**外框與名字**；
面留給「選取時 0.38」那個互動——點一下才看得出這塊板塊有多大，那才是重點。
`fill-opacity: 0` 不影響點擊命中（maplibre 的 hit test 不看不透明度，實測過）。

⚠️ 兩層的簡化容差**必須一致**（都是 0.02）：不一致的話兩層一起打開時，板塊外框與
彩色的邊界線會被各自簡化到差幾個像素，沿線露出一圈暖褐色毛邊。

#### 顏色

`plates` 用 `PLATE_COLOR`（暖褐 `#7d6b4f`），跟 reference／hazard 同屬**非分類的固定
角色**，不參與色票驗證——52 塊板塊全同色，它不是一個要跟別人比色相的類別。

三種邊界用 `PLATE_BOUNDARY_COLORS`（橘 `#c95c1c`／藍 `#2f74c9`／綠 `#159c6b`），
**比照交通軸線宣告不參與線／面色票的 all-pairs**。組內 all-pairs 實測明暗兩
模式**五項全數 PASS、零 WARN**：

```bash
node <dataviz-skill>/scripts/validate_palette.js "#c95c1c,#2f74c9,#159c6b" --pairs all --mode light|dark
# → CVD 最差 8.8（綠↔橘，deutan）、一般視覺最差 21.2（綠↔藍）
```

⚠️ 綠色在臺灣主題是禁忌（NLSC 底圖的山區底色就是綠的），這裡可以，理由與交通軸線相同
——**地理分佈相反**：板塊邊界絕大多數在洋底，而世界底圖在海上是藍的。

#### ⚠️ 這一層是虛線，那是被「世界主要河流」逼出來的

當初宣告豁免的理由是「同框對象一個彩色分類色都沒有」。**2026-08 把全球地理形貌併進
世界地理之後，那句話不成立了**：這一層第一次跟世界主要河流同框，而**聚合藍 `#2f74c9`
對水系藍 `#2a78d6` 的一般視覺 ΔE 只有 2.1**——同一個顏色，而且喜馬拉雅、安地斯、
阿爾卑斯正好是大河的源頭。換色救不了（掃描結論見下面「⚠️ 「全球地理形貌」在 2026-08 併進「世界地理」」
那一節），所以改用線型當第二通道：`render.dash = [4, 1.5]`。

⚠️ **不要「順手」改回實線**，也不要因為畫了虛線就以為這一層變成示意圖——它沒有標
`schematic`，幾何是 Bird (2003) 的實測模型；虛線在這裡的語意是「模型化的界線」，
跟 `notes` 早就寫著的「真實的變形區可以寬達數百公里」一致。

### 大洲分區：把國界併成七大洲

取得邏輯在 `scripts/lib/continents.mjs`，產物是 `public/data/geo/world-continents.geojson`
（7 筆、326 KB）。

#### 沒有可用的「大洲多邊形」，所以自己併

公開又可自由使用的大洲圖資其實不存在（Esri Living Atlas 的 World Continents 不是
開放資料），Natural Earth 只有國界，每個國家帶一個 `CONTINENT` 欄位。而「把同一洲的
國家併起來」正好是本站已經有工具的事——`lib/dissolve.mjs` 的有向邊相消（當初為國家
公園的分區圖寫的）。實測 Natural Earth 的國界**確實共用逐位元相同的邊**，七大洲一次
就併起來了。

⚠️ **但 NE 的環是順時針**（shapefile 老慣例，跟 GeoJSON 相反），不先反轉的話
`dissolveRings` 會把每一個環都判成內環，然後丟「合併後沒有任何外環」——那個錯誤訊息
完全不會讓人想到繞行方向。

#### ⚠️ 上游「整個國家算一洲」，所以跨洲國家一定要自己切開

Natural Earth 把**整個俄羅斯算成歐洲**。照單全收的話歐洲會變成 2,290 萬 km²、亞洲
剩 3,120 萬——課本寫的是歐洲 1,018 萬、亞洲 4,458 萬，那不是「定義不同」而是錯的。

所以 `DIVIDES` 依課本講的洲界把四個國家切開（烏拉山＝東經 60°、烏拉河、土耳其海峽、
蘇伊士運河與蘇伊士灣），切完的數字對得上：歐洲 1,023 萬（歐俄 392 萬）、亞洲 4,386 萬、
土耳其歐洲部分 2.5 萬（東色雷斯 2.4 萬）、西奈半島 5.6 萬（約 6 萬）。夏威夷另外依
位置改判大洋洲（玻里尼西亞）。

⚠️ **每一條分界線只切它該切的那一個國家（`countries`）。** 這一條踩過，症狀很好認：
**地圖上冒出幾條橫貫大陸的直線**。半平面裁切（Sutherland–Hodgman）把環切兩半時會沿
切線補「連接邊」；環只跨切線兩次時兩半的連接邊剛好相消，但跨**四次以上**時左半補的是
X1→X2、X3→X4、右半補的是 X2→X3、X4→X1，**消不掉**，整條切線就被畫出來。實測把土耳其
海峽那條線套用到全部國家時，它延伸出去橫掃俄羅斯，畫出一條從土耳其拉到西伯利亞的紫線。

⚠️ 代價是切線與國界的交會處會留下一小片雙重覆蓋的三角形（被切的那一國多了一個交點、
鄰國沒有）。它遠小於一個像素，而且 dissolve 仍然串得起來——比整條假線好得多。

#### 面積總和就是自我檢查

比照板塊那一層：七大洲的面積總和必須接近陸地總面積（1.49 億 km²），實測 1.466 億、
誤差 -1.7%，超過 5% 就讓建置失敗。⚠️ 面積要在**切開之後、簡化與濾島之前**算。

⚠️ 南極洲會少 12%（本層 1,226 萬 vs 常見的 1,400 萬）：NE 那份畫的是**岩床海岸線、
不含冰棚**。這件事寫在圖層的 `notes` 與內容卡上，不要「修正」它。

⚠️ 小於 0.02 度²（≈250 km²）的島在**簡化之前**濾掉（1,450 → 871 塊），但面積仍然算
進所屬的洲。門檻不要再往上調：0.15 度²（生物群系那一層的值）會開始咬到新加坡、馬爾他、
澎湖與大半個太平洋島群。

#### ⚠️ 洲名是另外一層點，不是面的標註

maplibre 對多邊形是**逐塊、逐圖磚**算標註錨點的，而這一層的亞洲有 240 塊——把 `label`
掛在面上，實測全球視角會在菲律賓、印尼、日本、千島群島上各印一次「亞洲」，一個畫面
六十幾個洲名。板塊那一層沒踩到只是因為一塊板塊通常就是一塊多邊形。

修法是把洲名做成 `attach` 的一層點（`world-continent-labels`，`radius: 0` ＋ 標註）：
名稱與 id 讀自母圖層那份 geojson（**共用 `resolveLayerData` 的快取，不會多抓一次**），
七個錨點座標寫在 `resolve.ts`。⚠️ 錨點**刻意不用形心**——形心會把「北美洲」丟進加拿大
北部、「大洋洲」丟進太平洋、「南極洲」丟到麥卡托投影上的無限遠。

id 跟母圖徵**是同一個字串**（比照颱風的中心定位點），所以點洲名與點那一洲開的是同一
張卡，`parentProperty` 讓兩邊互相連動強調。

#### 顏色：掃描選色相、實測定明暗

`CONTINENT_COLOR`（梅紫 `#a05a80`）是**非分類的固定角色**，比照板塊的暖褐——七大洲
同色，畫出來的是外框與名字，「這一洲有多大」交給選取時那層 0.38 的面染（所以
`fillOpacity` 是 0，跟板塊同一個理由）。七個分類色本來就不存在（本站掃出來的上限是六色）。

掃遍 OKLCH 之後，能同時離世界主題所有同框色（板塊暖褐、參考線灰、風系板岩藍、面色票
11 色、線點色）都遠的**只剩色相 320–350° 的紫紅那一族**。⚠️ **亮度不是取掃描最佳值**：
最佳解在 L≈0.67（`#b581b0`，最差 ΔE 15.3），但那個亮度實際疊在 Liberty 上**讀不出來**
——外框看起來只是「海岸線被染了一點粉紅」，烏拉山與蘇伊士那幾條洲界幾乎看不見。取
L≈0.56 之後最差值掉到 11.5（針葉林），那是**刻意的**，判例是 `WIND_COLOR`（對參考線灰
11.1）與保護區紫。詳細量測與補償見 `thematicColors.ts` 的 `CONTINENT_COLOR`。

⚠️ 要改色的話兩件事都要做：整份同框名單重掃，**而且**在 Liberty 底圖的世界視角實際
疊一次。

### 火山帶：1,214 座全新世活火山

取得邏輯在 `scripts/lib/volcanoes.mjs`（端點、19 個火山區與 17 種火山類型的中文對照、
40 幾座知名火山的中文名）。

#### 為什麼是 GVP 的「全新世火山」

課本說的活火山＝**全新世（約一萬年）以來噴發過**，而史密森尼學會全球火山計畫的
Volcanoes of the World 就是那份權威名單。USGS 只有靜態圖、Natural Earth 沒有火山圖層，
所以這是唯一一份公開、帶座標與最後噴發年代的全球資料。WFS 一次回 2.4 MB，產物 285 KB。

⚠️ **黃石與多巴這類「超級火山」不在名單裡，那不是漏掉**——它們上次噴發在 7 萬年前，
早於全新世，按定義不算活火山。這件事寫在圖層的 `notes` 裡。

⚠️ 三成（366 座）的最後噴發年代是「不詳」：靠地層或碳定年判定為全新世噴發，但定不出
年份。`formatEruption()` 因此把 `null` 寫成「最後噴發年代不詳」而不是留白。

⚠️ 海拔**可以是負的**（最深 -5,700 公尺的海底火山），所以 `formatElevation()` 對負值
寫「海面下 N 公尺」——寫成「海拔 -5700 公尺」是讀不懂的。

#### ⚠️ 產物只留卡片會用到的四個欄位

上游每筆還帶著幾百字的英文地質沿革（`Geological_Summary`）、照片網址與岩性。全帶著的
話產物會從 285 KB 膨脹到 2 MB 以上、直接撞穿大小預算，而卡片一個字都用不到。留下的是
`id`／`name`／`meta`／`detail`，後兩者是建置期就組好的字串（比照水庫與河川的既有做法）。

中文名的收錄界線是「**臺灣的兩座（龜山島、大屯火山群），加上課本、新聞與科普讀物會
直接叫出名字的知名火山**」，40 幾座；其餘沿用 GVP 原名。⚠️ 這份表**不可能補完**，
理由與 `ZH_HANT_OVERRIDES`、`RIVER_NAMES_ZH` 相同。key 用 `Volcano_Number` 不用名稱
——GVP 會修訂拼寫（`Fuji` → `Fujisan`、`White Island` → `Whakaari/White Island`），
編號則穩定；表裡的編號在上游找不到就讓建置失敗。

#### ⚠️ 有卡片但沒有 `browse`，這是兩個分開的決定

- **有卡片**：跟地震帶不同，每一座火山都有名字、類型、海拔與最後噴發年代，點下去讀得到
  具體的東西（比照 `tw-quakes` 那 612 個震央）。
- **沒有清單**：1,214 列不是人掃得完的清單。既有判準就擺在那裡——`tw-quakes-major`
  （92 筆）有 browse、`tw-quakes`（612 筆）沒有。
- **連帶後果是搜尋也找不到**（`searchIndex` 的 `indexesFeatures()` 看的就是 `browse`），
  所以**搜「富士山」不會有結果**。那是刻意的取捨：進索引等於每個學生一聚焦搜尋框就多付
  285 KB。搜「火山」仍然找得到**圖層本身**。

#### ⚠️ 顏色不能用紅色，而且這一層的跨幾何豁免不成立

`VOLCANO_COLOR` 是洋紅 `#c0259c`。本站的規則是「三組獨立色票、跨幾何不驗」（古蹟赭紅的
圓點與斷層磚紅的線就是這樣共存的），但那條豁免的前提是**地理分佈相反**。火山正好相反
——它們幾乎全部長在板塊邊界上，那是這一層要教的第一件事，所以必須跟三種邊界一起驗：

```bash
node <dataviz-skill>/scripts/validate_palette.js "#c95c1c,#2f74c9,#159c6b,#c0259c" --pairs all --mode light|dark
# → 兩模式五項全數 PASS、零 WARN；CVD 最差仍是既有的綠↔橘 8.8、一般視覺最差 21.2
```

紅色（製圖慣例）**過不了**：`#d1352b` 對張裂型邊界的橘 `#c95c1c` 一般視覺 ΔE 只有
**7.3（hard FAIL，下限 15）**、deutan 3.2——冰島與東非大裂谷那一帶的紅點會直接融進橘線。
掃過整個 OKLCH 色域（色相每 2.5°、L 0.48–0.67＝明暗兩模式亮度帶的交集、彩度 0.10–0.26）
之後，零 WARN 的候選**只剩色相 285–355° 的紫／洋紅那一族**。深紅 `#ba054a` 過得了
all-pairs 但深色模式對比只有 2.66，而這裡有乾淨的替代品可選（比照保護區紫那次的判準：
有乾淨替代品就不要帶 WARN 上線）。顏色與「火山」的對應靠圖例文字，比照茶葉配藍。

⚠️ 它跟山脈洋紅 `#c23f8f` 同一族是**刻意可以**的（不同幾何、不同主題，比照地形景點藍與
水系藍共用同一個 hex）；但 `#c23f8f` 本身在這裡**不夠好**——它對錯動型邊界的綠只有
deutan 6.7，落在「只有搭配次要編碼才合法」的 6–8 band。

#### ⚠️ 半徑 3.2／不透明度 0.9／不畫外框，三個值都是被主題預設視角逼出來的

這一層第一眼看到的是 `center [0,10]、zoom 1.8`——正中央是全球最沒有火山的非洲與大西洋。
全球地震帶踩過那個坑（1.5 px + 0.35 的灰點＝「勾了圖層但什麼都沒有」），這裡直接沿用它
修好之後的量級。半徑**不隨 zoom 變**：放大時靠點與點拉開，不是點變大。**不畫白色外框**
——1,214 個亮外框在投影機上會糊成一片白雜訊，而這一層常常跟地震帶疊著看。

### 規模最強地震：18 次，收錄名單是抄的、規模是查的

2026-09 加在「地體構造」底下、緊接在「全球地震帶」後面。收錄名單來自
**維基百科〈地震列表〉的「震級最強地震」那張表**（使用者指定的來源），存取層是
`scripts/lib/quakes-strongest.mjs`，資料集 id 與圖層 id 都是 `quakes-strongest`。

#### 為什麼名單用抄的、數字用查的

那張表是人手維護的維基表格，沒有 API；而且它自己就混用了當地日期與 UTC 日期。
所以名單（哪 18 次、叫什麼名字、通稱哪一天）抄成常數，**位置、規模與震源深度一律查
USGS 的地震目錄**（比照本站「數值型資料以主管機關為準」的既有規則）。抄成常數的附帶
好處是新增或移除一筆一定出現在 git diff 上。

⚠️ **實測有四筆對不起來，而那正是這一層的教學內容：**

| 地震 | 那張表 | USGS（本站採用） |
|---|---|---|
| 2004 印度洋 | 9.3 | **9.1**（那張表自己的註腳就寫「USGS 修正為 9.1」；9.3 是西北大學團隊用長週期波形算的） |
| 1700 卡斯凱迪亞 | 9.2 | **9.0**（兩個都是推估值，區間 8.7–9.2） |
| 2005 蘇門答臘（尼亞斯） | 8.7 | **8.6**（USGS 初評 8.7 後下修） |
| 2007 蘇門答臘（明古魯） | 8.5 | **8.4** |

對不上的那幾筆會把維基值一併寫進 `properties.magWiki`，卡片兩個都講。
`fetchStrongestQuakes()` 每次建置都會把差異印成警告——**那四行是預期會出現的，不是壞掉**。

⚠️ **因此清單順序（規模由大到小）跟那張表的排名不完全一樣**：2007 年那次以 8.4 排到
最後、2004 年從第 2 掉到跟 2011 並列。要照原表排就得改用維基的數字當主值，那跟上面
那條規則相牴觸。這件事寫在圖層的第二則 `notes` 裡。

#### ⚠️ 四筆 USGS 完全沒有，那是人工轉錄的

1575 瓦爾迪維亞、1668 郯城、1755 里斯本、1833 蘇門答臘都在儀器地震學誕生之前，
ComCat 查不到（**實測那四個時間窗各回 0 筆**）。它們的座標與規模推估取自維基百科條目，
產物裡帶 `source: "維基百科"`（其餘十四筆是 `"USGS"`），而卡片的「資料來源」那一行
本來就會顯示各自的出處——混合來源就要說出來，比照 `tw-quakes-major` 的 `RECENT_QUAKES`。

⚠️ **1755 里斯本的經度一定是 −11，不是 +11。** 中文維基的座標模板漏了 W，
Wikipedia 的 `prop=coordinates` API 回的就是 `lon: 11`——照抄會把震央放到地中海的
突尼西亞外海去，而且畫面上看起來「也還算合理」（一樣是海、一樣在歐非之間）。
英文條目是 36.0N/11.0W，本站採後者，實測那個點打在大西洋的 `water` 圖層上。

#### ⚠️ 那張表的 wikitext 有第 19 列，但它不會顯示

1707 年寶永地震（估計 8.6–9.3）那一列的語法寫壞了（`|-` 那一行後面直接接內容），
MediaWiki 把它當成列屬性吃掉，**渲染出來的頁面上根本沒有這一列**。本站照「讀者看得到
的那張表」收 18 筆。上游修好那一列再補——**不要以為是漏抄了**。

#### 顏色、半徑與白框：逐字沿用 `tw-quakes-major`

同一個 `hazard` 中性色，但不透明度 0.9、半徑 `mag 8.4→5、9.5→12`、`strokeWidth: 1.5`。
使用者要的是「把這 18 次標出來」而不是另一個類別，所以刻意不給新色相——那也省下一次
色票驗證。

⚠️ **白框在這一層是必要的，母圖層刻意不畫外框那條規則不適用。** 這 18 顆點常常正好
落在「全球地震帶」最密的那幾段上（日本海溝、蘇門答臘外海），沒有外框就融進那團灰裡。
反過來母圖層有 2,800 個點，加白框會變成一片白雜訊——**兩層的判準相反，不要互相照抄**。
⚠️ 半徑下限 5 是「看不看得見」的門檻：這一層要在主題預設視角（zoom 1.8）就讀得出
「最強的幾次全部沿著隱沒帶排列」，而母圖層踩過那個坑（1.5 px 的灰點＝「勾了圖層但
畫面上沒東西」）。

#### ⚠️ 全站第二個「非世界櫥窗」的 `exemptFromMaxActive`

判準跟「世界紀錄熱帶氣旋路徑」那一條一模一樣：**它用的是非分類的固定角色色 `hazard`，
多開一層畫面上一個新色相都沒有增加**。而世界主題已經有五個吃色票的圓點圖層在搶四個
名額（全球地震帶、火山帶、世界重要城市、世界人口分布、古文明發源地），不豁免的話
「把最強的幾次標出來」就得跟它要對照的「全球地震帶」「火山帶」二選一——那正是這一層
存在的理由（比照當初「世界之最・火山」併進點圖層的同一個論證）。

⚠️ **那是豁免，不是上限失效**（實測見驗證清單第 41 項）。

#### ⚠️ 不共用 `QuakeCard`，這 18 筆各自有內容檔

`detail` 是 `{ type: "geo", collection: "quakes-strongest" }`，不是 `tw-quakes` 那一組的
`{ type: "quake" }`。那張卡只講規模、震源深度與震央經緯度，而這 18 筆每一筆的重點都是
**它教了我們什麼**：矩震級是為了 1960 智利才發明的、卡斯凱迪亞是靠日本的「孤兒海嘯」
加上枯樹年輪定年的、2012 那次因為是走向滑移所以規模 8.6 卻幾乎沒有海嘯、1965 拉特
群島 8.7 卻一個人也沒死。那些只有內容檔寫得下。

⚠️ **`deaths` 與 `tsunami` 刻意不寫進 geojson**：`FeatureCard` 不會去讀 properties 的
任意欄位，存進來就是沒有人看得到的重複資料（比照 `tw-quakes` 那次把顯示字串存進
properties 讓檔案從 190 KB 漲到 400 KB 的教訓）。它們留在 `lib/quakes-strongest.mjs`
的表上，當寫卡片時的依據。⚠️ `source` 相反，**要留**——那是這一筆的位置與規模到底
來自哪裡，屬於資料本身的來源標示。

#### 卡片互相指涉，改一張要順手看被它點名的那幾張

這 18 張刻意串成幾條線索，**不要單獨改掉其中一段**：

| 線索 | 涉及的卡片 |
|---|---|
| 同一段隱沒帶的復發 | 1575 ↔ 1960（瓦爾迪維亞，相隔 385 年）、1952 ↔ 2025（堪察加，震央相距約 35 公里）、1964 ↔ 1965（阿留申弧相鄰段落） |
| 分段破裂 | 1906 厄瓜多（1942／1958／1979 各破一段）→ 2004 → 2005 → 2007（巽他隱沒帶三年內由北往南） |
| 沒有儀器怎麼定規模 | 1700（日本文獻＋樹輪＋口傳）、1833（珊瑚微環礁）、1668（四百多份地方志的等震線）、1755（龐巴爾侯爵的問卷） |
| 規模不等於災情 | 1965（8.7、0 人）↔ 1668（8.5、四五萬人）↔ 2004（9.1、22 萬人）↔ 2010（8.8、525 人） |
| 斷層型態決定有沒有海嘯 | 2012（走向滑移、板塊內部、幾乎沒有海嘯）↔ 2005（隱沒帶逆衝）↔ 1950（大陸碰撞帶、山崩與堰塞湖） |

⚠️ **`browse.zoom` 是 5，那是實測調的**：這些震央幾乎都在外海，取景要同時看得到震央
與最近的那段海岸，才看得出「它為什麼會淹到那裡」（實測 1960 智利看得到瓦爾迪維亞與
康塞普西翁、1755 里斯本看得到里斯本與直布羅陀）。

### 世界主要山脈：從「面」算出來的中軸線，配一座最高峰

取得邏輯在 `scripts/lib/mountains.mjs`，產物是 `public/data/geo/world-mountains.geojson`
（39 條線、30 KB）與 `public/data/geo/world-mountain-peaks.geojson`（39 個點、10 KB）。

#### 上游是「面」，這一層畫的是「線」——中軸線是算出來的

山脈沒有像國界那樣的官方界線圖資（「阿爾卑斯山脈到哪裡為止」本來就是製圖判斷）。
公開、免金鑰、涵蓋全球又逐筆標了種類的只有 Natural Earth 的
`ne_10m_geography_regions_polys`（`FEATURECLA === "Range/mtn"`，222 筆），而它收的是
**範圍面**。這一層要教的是**走向**，而且面在這個站上還有兩個硬問題：

1. **第 14 個面色不存在。** 掃遍 OKLCH（做法同大洲那次），要跟大洲梅紫、生物群系
   六色、柯本五色、板塊暖褐都分得開的面色**一組都沒有**——本站的分類色上限是六色，
   面色票早就滿了。實測最好的候選（玫瑰 `#c47581`、青綠 `#1aa67a`）一般視覺最差值
   只有 9.1／10.7，而且分別撞上「B 乾燥氣候」與「溫帶林」——雨影沙漠與森林正好是
   山脈旁邊那一格。
2. **面會吃掉 `MAX_ACTIVE_BY_KIND.fill` 的兩個名額之一**，而「山脈擋住水氣 → 背風側
   是沙漠」正好要跟柯本氣候分區或生物群系疊著看。

所以 `polygonAxis()` 把每一塊範圍面化成一條**中軸線**：掃描線點陣化 → chamfer 距離
變換 → 每個連通分量做兩次 BFS 找出兩端 → Dijkstra（離邊界越近越貴，路徑因此貼著中軸
而不是抄捷徑貼著邊）→ 移動平均 → Douglas–Peucker。

⚠️ **經緯度要先做 cos(緯度) 校正再進網格**，否則高緯度的山脈（烏拉、斯堪地那維亞）
在網格上會被縱向拉長，中軸線會偏。

⚠️ **移動平均那一步不能省。** Douglas–Peucker 只會**刪點**，而階梯狀的轉角剛好是它
認定「不能刪」的那種點——不先平滑，簡化完仍然是一條鋸齒線，點數也降不下來。

實測長度對得上常識（喜馬拉雅 2,165 km／常見值約 2,400、阿爾卑斯 1,006／約 1,200、
安地斯 8,907／7,000–8,900、大分水嶺 3,498／約 3,500），但**產物刻意不放長度**：
那是這一層最容易產生假精確的地方。圖層與 39 條全部標 `schematic`。

#### ⚠️ 收錄名單是人工挑的，`SCALERANK` 篩不出來

39 條的界線是「臺灣課本、新聞與科普讀物會直接叫出名字的山脈，七大洲都要有」。
機械篩選會同時出兩種錯：`SCALERANK <= 3` 漏掉**秦嶺**（rank 4，中國南北的自然界線）
與**托魯斯山脈**（rank 5），卻收進雅布羅諾維嶺、塔爾巴哈台山這種課本不會提的。

⚠️ **上游的 `NAME_ZHT` 不能用**：欄位名字叫 zht，內容卻簡繁混雜（阿爾卑斯寫
「阿尔卑斯山」、烏拉寫「乌拉尔山脉」、喜馬拉雅寫「喜马拉雅山脉」），跟世界底圖
`name:zh-Hant` 是同一個坑。中文名、洲別、成因說明與最高峰全部人工整理在 `RANGES`，
key 用 **`NE_ID`**（上游會改拼寫，數值 id 穩定；橫貫南極山脈上游拆成兩筆但共用同一個
NE_ID，正好自動併回一條，產物是 2 段的 MultiLineString）。

#### ⚠️ 最高峰不可以用「面內海拔最高的那個高程點」自動決定

那個做法實測會出兩種錯，而且都不會報錯：

- **上游的興都庫什面蓋到了南迦帕爾巴特峰**（那是喜馬拉雅的西端），自動選會選到
  8,125 公尺的它，而不是興都庫什真正的最高峰蒂里奇米爾峰 7,708。
- **西高止山脈真正的最高峰阿奈穆迪山落在面的外面**，自動選會退而選到多達貝塔山。

所以 39 座逐條指名（`RANGES[].peak.en`，必須對得到上游 `featurecla === "mountain"`
的某一筆，對不到就讓建置失敗），面內面外都不管。**高度取自上游、不自己抄數字**，
只有庫克山例外（`ELEVATION_OVERRIDES`：上游仍是 1991 年山頂崩落前的 3,754，
現行公告值是 3,724）。聖母峰 8,848 → 8,848.86 這種小數位差異不列，`notes` 有交代。

#### ⚠️ 顏色不是臺灣五大山脈的 `relief` 洋紅

直覺上兩層都是山脈該共用同一個角色，**實測不行**：洋紅 `#c23f8f` 對火山帶的洋紅
`#c0259c` 一般視覺 ΔE 只有 **4.6**——同一個顏色。「跨幾何不驗」那條豁免在這裡不成立，
因為它的前提是**地理分佈相反**，而這兩層正好相同：安地斯、喀斯開、巴里桑、新幾內亞
高地全都是火山密集的山脈。從前兩層分屬不同主題碰不到，山脈進了世界主題就碰得到了。

`MOUNTAIN_COLOR`（紫 `#8e26ff`）是**非分類的固定角色**（39 條同色），比照板塊暖褐與
大洲梅紫。條件是「跟山脈在**陸地上**同框的每一個顏色都分得開」：三種板塊邊界、火山
洋紅、水系藍（＝世界城市與本層最高峰的 `place` 藍）、地震灰、參考線灰、風系板岩藍。
洋流的紅與藍**不列**——洋流全在海上、山脈全在陸上，一個像素都不會疊（比照已記錄的
寒流藍↔水系藍豁免）。

```bash
node <dataviz-skill>/scripts/validate_palette.js "#8e26ff,#c95c1c,#2f74c9,#159c6b" --pairs all --mode light|dark
# → 兩模式五項全數 PASS、零 WARN（CVD 最差 8.3、一般視覺最差 19.7，都對聚合型邊界藍）
node <dataviz-skill>/scripts/validate_palette.js "#8e26ff,#c0259c,#2a78d6" --pairs all --mode light|dark
# → 兩模式五項 PASS，一個 WARN：對藍的 CVD 6.6（deutan）
```

⚠️ 掃遍 OKLCH 之後，能讓一般視覺最差值站上 15 的**只剩色相 295–302° 的紫**，而且
**彩度必須拉到 0.28**（同色相降到 0.20，對藍的 CVD 就從 6.6 掉到 2.8）。看起來偏鮮豔
是掃描的結果，不是隨手挑的。那個 CVD WARN 依驗證器的規則「只有搭配次要編碼才合法」，
補償有兩層：每一條山脈都有**沿線標註**（畫面上寫著「安地斯山脈」），而藍色的對象是
**圓點**——線與點的形狀差異本身就是區辨通道（判例是洋流那一組）。

#### 最高峰是附屬圖層，而且它**不是**示意的

比照臺灣五大山脈的 `tw-range-peaks`：一條山脈配一座最高峰、跟母圖層同一個核取方塊、
在可點清單裡巢狀排在各自的山脈底下、母子雙向連動強調（`parentProperty: "rangeId"`）。
顏色沿用 `place` 藍（附屬點一律用藍，見「附屬圖層」那節）。

⚠️ 這一層是 `remote` 而不是 `derived`（臺灣那層是在 `resolve.ts` join 的）：這裡的
join 需要 NE 的高程點，那是一份 843 KB、只在建置期該碰的檔案。

⚠️ **`attach.schematic: false` 不是多餘的，那個欄位就是為它加的。** `findGeoOwner()`
本來一律把母圖層的 `schematic` 傳給附屬圖徵，而母圖層的中軸線是**算出來的**、最高峰
卻是上游的真實座標與高度——不覆蓋的話，聖母峰的卡片底下會印一行「這是簡化的教學示意
幾何」，那是對讀者說謊。

⚠️ **`attach.browse.zoom` 是 5.5，不是 8，這是實測改回來的。** 母圖層的 `maxzoom`
是 6，所以 `zoom: 8` 時點「聖母峰」會飛到一個**看不到喜馬拉雅山脈**的畫面：詳情卡、
相機、強調表達式全都正常，只有那條線 `queryRenderedFeatures` 回 0，畫面上只剩一顆
藍點。判準同縣市政府那一層——取景必須讓山峰與山脈同時在畫面上。

#### 其餘幾個實測值

- **39 條都有內容檔**（`src/content/geo/world-mountains/`）：主標與副標之外，卡片上是
  「最高峰＋高度／主要國家／長度或走向」四格數據，加上三到五條 `facts`（成因、對氣候
  或水系的影響、人文，最後一條一律是「**對照重點**」——明講該跟哪一個圖層疊著看）。
  每一份的 `sources` 都指名那條山脈的維基百科條目（39 條實測全部 200），幾何與高度
  的來源接在後面。⚠️ 條目名要用維基百科**實際的**標題：`海岸山脈 (北美)` 與
  `內華達山脈 (美國)` 那兩個括號是消歧義後綴，少了會連到臺灣的海岸山脈與西班牙的
  內華達山脈。
- ⚠️ 圖層仍然掛著 `hideLayerDescription`，但**今天是 no-op**（有內容檔的圖徵根本不走
  fallback），比照 `tw-rivers` 與 `tw-protected-areas`：留著是為了規則一致，之後補收
  一條山脈而內容檔還沒寫時，卡片會是「名稱＋洲＋最高峰＋成因」而不是整片圖層說明。
- `maxzoom: 6`：一個像素在 zoom 6 約 0.04°，正好是產物簡化容差（0.02°）的兩倍。
  再放大，中軸線就會變成一條假的精確稜線。
- **標註的 `maxAngle: 150`** 照抄臺灣河川那次的教訓（真實曲線用預設的 60 會被靜默
  拒絕大半）。實測主題預設視角（`[0,10]`、zoom 1.8、1920×873）38 條畫得出來、12 個
  標註，沒有同一條線重複出現名字。
- `browse.groupBy: "category"` 依**洲**分組。⚠️ 不依「新褶曲／古老褶曲」是刻意的：
  後者在教學上更有力，但對半數的山脈說不清楚（衣索比亞高原是熔岩高原、西高止山脈是
  斷層崖、帕米爾是山結），硬分等於製造一堆查不到出處的斷言。成因逐條寫在 `formation`
  裡，那是講得出來也標得出來源的層級。
- **搜尋索引多抓 40 KB**（30 + 10），兩層都有 `browse` 所以都會進索引。實測搜「安地斯」
  會同時命中山脈、阿空加瓜山（它的 `meta` 裡有「安地斯山脈主峰」）與板塊；搜「Andes」
  「聖母峰」「庫克山」都找得到。

### 森林與沙漠帶：六個生物群系大類

取得邏輯在 `scripts/lib/biomes.mjs`；六類各一個檔（`public/data/geo/biomes-<class>.geojson`，
合計 731 KB）。

#### 為什麼不是手繪示意圖

這一層原本掛著 `schematic: true`，打算畫幾條橫的緯度帶。**現在是真實資料，所以不標
schematic。** 理由是這一層要教的不是「有哪幾種植被」而是「**為什麼**沙漠帶落在南北緯 30°」
——手繪長方形只是把結論畫出來，而真實分布會露出反例：同樣在 30° 附近，撒哈拉與阿拉伯連成
一氣，東亞卻是森林（季風）。那個對比才是這一層的價值。

資料是 RESOLVE Ecoregions 2017（Dinerstein et al.，WWF 陸域生態區的現行版本），847 個
生態區各自標了 14 個生物群系之一。⚠️ **授權 CC-BY 4.0，要求標示出處**，所以 `sources`
必須同時列原始資料集與取得管道（比照板塊那份 ODC-BY）。

#### ⚠️ 走 Esri Living Atlas，不要去抓官方那份 149 MB 的 shapefile

官方 `Ecoregions2017.zip` 是 149 MB，解開更大——`lib/shp.mjs` 是自己寫的純 JS 讀取器，
不該讓它扛那個量級。Living Atlas 代管的同一份資料支援伺服器端的 `maxAllowableOffset`
（幾何綜合）與 `geometryPrecision`（小數位），一次要求就拿到化簡過的 GeoJSON。

⚠️ **連續打會收到一個會騙人的 400**：第二輪之後開始回
`'maxAllowableOffset' parameter is invalid`，**但同一個網址單獨打是好的、參數也沒錯**
（0.2／0.4／0.8 都出現過同一句話）。看起來是流量控管，錯誤訊息卻指著參數，很容易讓人
跑去改 offset。`fetchBiomeClass()` 因此自己做間隔與退避重試——`fetchWithRetry` 只認
429／5xx，這裡是 HTTP 400 帶著一個 JSON 錯誤物件。**六類不要連著跑。**

#### 14 類併成 6 類是色票逼出來的，併法要說得出口

十四個分類色不可能通過驗證（本站掃過整個色域，六色已經是 all-pairs 全過的上限），而課本
講的本來就是「熱帶雨林、莽原、沙漠、溫帶林、針葉林、苔原」這幾條帶。併法寫在
`BIOME_CLASSES`，四個判斷值得記住：紅樹林併入熱帶林（窄帶，世界尺度下幾乎全被面積門檻
濾掉）、**溫帶草原與熱帶莽原合成「莽原與草原」**（所以類名不叫「莽原」——不能說謊）、
地中海型併入溫帶林、高山草原併入苔原（同一個低溫限制，只是換成用海拔達成）。
⚠️ `BIOME_CLASSES` 有一道建置期檢查：`BIOME_NUM` 1–14 少涵蓋一個就直接拋錯。

#### ⚠️ 小碎塊過濾是這一層塞得進預算的關鍵

上游把每個小島與湖心島都收了進來：**光是苔原那一類就有 50,680 個環**，全留下來那一類
自己就 3.5 MB。`MIN_POLYGON_AREA = 0.15` 度²（≈1,850 km²）之後是 169 KB。
⚠️ **門檻不能再往上調**：0.3 度² 會開始咬到峇里島、宿霧這種島，而熱帶雨林這一類的教學
重點正好在印尼與菲律賓的群島上。⚠️ 跟離島那條規則一樣，**必須在簡化之前**做。

#### ⚠️ 外框是拿來「補縫」的，不是畫界線

六類彼此相鄰，而幾何是上游**逐一**化簡的（不保拓樸），共用邊界因此對不齊，撒哈拉／薩赫爾
之間會露出一條一條的白縫（zoom 3.6 就看得到）。修法是畫一圈**跟面同色、同樣半透明**的
外框把縫蓋掉——這就是 `LayerRender.fill` 新增 `outlineOpacity` 的唯一理由（預設仍是 0.9，
既有圖層行為不變）。

⚠️ **不要改回預設的 0.9**：1,900 多塊多邊形在中亞、安地斯與北極群島會織成一張線網，
色帶本身反而讀不出來（0.6 寬 × 0.9 實測就是那個樣子）。現在這組 **1.0 寬 × 0.15**
是在 zoom 1.8 的全球視角與 zoom 3.6 的撒哈拉交界**兩邊都看過**才定的。

`maxzoom: 5` 同理：0.4° 的綜合精度在更近的尺度會露出折線與縫。

#### 六色是在語意色相窗內最佳化出來的

`BIOME_COLORS`（熱帶林 `#25744c`／莽原 `#989401`／沙漠 `#b4410d`／溫帶林 `#369db8`／
針葉林 `#695ba9`／苔原 `#c754f4`），明暗兩模式 `--pairs all` **五項全數 PASS、零 WARN**：

```bash
node <dataviz-skill>/scripts/validate_palette.js "#25744c,#989401,#b4410d,#369db8,#695ba9,#c754f4" --pairs all --mode light|dark
# → CVD 最差 8.6（沙漠↔熱帶林，protan）、一般視覺最差 18.1（溫帶林↔熱帶林）
```

方法：每一類先框一段語意色相窗，亮度限制在明暗兩模式亮度帶的交集 L 0.48–0.67，再以
「CVD ≥ 8.5 的前提下**最大化一般視覺最差值**」跑爬山法。⚠️ 目標函數把餘裕留在一般視覺
那一項是刻意的——15 是不能用次要編碼豁免的硬下限（另一組解是 CVD 10.0／一般視覺 15.4，
就卡在下限旁邊）。

⚠️ **溫帶林是藍綠、針葉林是靛紫，那不是配錯**：三種森林的慣例色都是綠，但綠只夠給一個，
三個綠的 CVD 分離度不可能同時達標。比照茶葉配藍——對應靠圖例文字，不是靠「像不像」。

### 柯本氣候分區：五個顏色、30 個亞型

取得邏輯在 `scripts/lib/koppen.mjs`；五大類各一個檔（`public/data/geo/koppen-zones-<a–e>.geojson`，
合計 336 KB）。

#### 為什麼是維也納那份 0.5° ASCII 網格

柯本分類是**用氣溫與雨量的門檻算出來的**，沒有官方界線圖，能拿到的都是別人算好的網格。
維也納獸醫大學（Kottek et al. 2006）那份是課本與百科用得最多的一版，而且它提供
**純文字的 `Lat Lon Cls` 三欄網格**（92,416 個陸地格、2.6 MB），不必寫 GeoTIFF 解碼器。

⚠️ 另外兩條路都被否掉了：Beck et al.（2018／2023）的新版**只發布 GeoTIFF**；Esri Living
Atlas 上的柯本圖層是 **Image Service（點陣）**。同一個網站的 1986–2010 版**只有 KMZ，
而 KMZ 裡是一張 720×360 的 PNG 疊圖**（實測解開來確認過），不是向量。

⚠️ 所以期距是 **1951–2000**，寫在 `notes` 裡。

#### 網格 dissolve 的兩條硬規則

同一個亞型的格子用 `lib/dissolve.mjs`（有向邊相消）併成一個 MultiPolygon——網格的邊
逐位元相同，正好是那支模組的前提（它本來是為國家公園的分區圖寫的）。

⚠️ **`tolerance` 必須是 0。** 容差 0 的 Douglas–Peucker 只刪掉**完全共線**的點（無損），
而座標都是 0.5 的倍數，所以 `digits: 1` 也是無損的。容差一旦大於 0，階梯狀邊界會被切角，
而相鄰兩類是**各自**簡化的、切完就對不齊——那正是生物群系那一層要用半透明外框補縫的
原因。這一層**不需要補縫**，所以 `outlineWidth: 0`（順帶讓同一大類裡的亞型界線不會被
畫成一堆內部線條）。

⚠️ 產物是 **5 個檔**但只下載**一次**：五大類吃的是同一份 2.6 MB 文字檔，`fetchKoppenGrid()`
有 module-level 快取（比照古蹟三級與作物三種）。

#### 顏色是大類，圖徵是亞型

30 個亞型不可能各給一個顏色（本站掃出來的分類色上限是六色），但「這一塊到底是 Cfa 還是
Cwa」正是這一層存在的理由——每一張地點卡上都有 `koppen` 欄位。所以：**五個核取方塊＝
五個顏色**，30 個亞型各是一筆圖徵，名稱／判準／代表地點在建置期就寫進 geojson
（`SUBTYPES`，30 筆一個都不能少），卡片走 `FeatureCard` 的 fallback ＋
`hideLayerDescription`。

⚠️ **亞型代碼一定要放進 `items[].keywords`**：這一層沒有可點清單，也**刻意沒有開
`indexFeatures`**（開了等於讓每個學生一聚焦搜尋框就多付 336 KB），所以搜「Cfa」能不能
找到東西，完全靠那份 keywords。實測搜「Cfa」會同時出現「C 溫帶氣候」與 koppen 欄位是
Cfa 的那些地點（後者是 `contentKeywords` 本來就有的行為）。

五色 `KOPPEN_COLORS` 明暗兩模式 `--pairs all` **五項全數 PASS、零 WARN**：

```bash
node <dataviz-skill>/scripts/validate_palette.js "#1d9dc8,#d8783d,#147811,#ba229e,#5e42fe" --pairs all --mode light|dark
# → CVD 最差 8.4（C 綠↔B 橙，protan）、一般視覺最差 23.3（E↔D）
```

色相窗刻意貼近 Kottek 原圖的慣例（A 藍、B 橙黃、C 綠、D 紫、E 冷色），選法同生物群系。

#### ⚠️ 這一層與「森林與沙漠帶」會互相蓋住

兩層都是 fill、都蓋滿陸地，而 `MAX_ACTIVE_BY_KIND.fill` 是 2 ——所以它們**可以**同時
打開，但兩片 0.25 的半透明面疊起來就是一團糊。這件事寫在 `notes` 裡提醒使用者一次看
一層；**不要為了讓兩層「能一起看」去調亮度或不透明度**，那只會讓兩層都變難讀。
兩組色票也不可能一起驗（11 個分類色）。

#### 0.5° 對臺灣意味著什麼

臺灣整座島只有三、四格。實測臺北是 Cfa（跟本站地點資料一致）、東京 Cfa、倫敦 Cfb、
開羅 BWh 都對得上，但**高雄一帶算成 Am**，而本站地點卡採中央氣象署的分區寫 Aw。
兩者不一定逐格相同，這件事寫在 `notes` 裡——不要為了「一致」去改任何一邊。

### 行星風系：全站第二個「程式產生」的圖層

幾何在 `src/map/registry/generators.ts` 的 `windBelts()`，**沒有任何資料檔**。

#### 為什麼是程式產生，而且應該是

行星風系不是測出來的界線，而是**理想化的模型**（氣壓帶在 0°／±30°／±60°，風帶夾在
中間），實際大氣還會隨季節南北移動好幾度。所以沒有「權威資料檔」可抓，只有課本的
示意圖——而示意圖的參數（帶的緯度、箭頭間隔、畫面上的斜度）該寫成程式碼裡的常數，
不是一份手抄的幾千個座標。**這一層因此必須標 `schematic: true`**（`geo-manual/` 那條
路也可以走，但參數會變成一堆改不動的座標）。

#### ⚠️ 箭頭的斜度要依緯度校正，否則極地東風會變成垂直的

Web Mercator 的縱向拉伸是 1/cos(緯度)：同樣 10° 的緯度差在 70°N 看起來比赤道長將近
三倍。所以緯度差是由「畫面上要幾度斜」反推的——`tan(30°) × 經度長度 × cos(緯度)`，
六條帶的箭頭在畫面上才會是同一個斜度。

⚠️ 極地東風的箭頭放在 **70°** 而不是 60–90 帶的正中央（75°）：主題預設視角
（zoom 1.8）的上緣只到約 66°N，往北每 5° 就要多縮小一級才看得到。**即使放在 70°，
預設視角仍然看不到極地東風**（實測 0 個標註）——要縮到 zoom 1.2 左右整套環流才會
同時出現，這件事寫在 `notes` 裡。

⚠️ 每一支箭頭與每一段氣壓帶線都**不跨越 ±180**（中心經度留了半個長度的餘裕），
理由同國際換日線。

#### 氣壓帶畫成「間斷的短線段」，不是橫貫全圖的長線

三條氣壓帶的緯度（0°／±30°／±60°）跟「緯度參考線」**完全重疊**。畫成長線就會變成
兩條疊在一起、只差顏色的線；短線段配上自己的標註（「副熱帶高壓帶」）才看得出是兩種
不同的東西。同理，這一層的顏色**必須跟參考線灰分得開**（實測 ΔE 11.1，見下）。

#### ⚠️ 整層只有一個顏色，那不是漏填

四個核取方塊（氣壓帶／信風／西風／極地東風）共用 `WIND_COLOR`（板岩藍 `#5a6f96`）
——它們是**同一個系統的四個部位**，不是四個要互相比較的類別。區辨靠形狀（點線 vs
箭頭）、位置（各自的緯度帶）與沿線標註，比照三條橫貫公路共用一個青的既有判例。
`items.palette` 因此是四個重複值。

⚠️ **為什麼不用課本的紅（高壓）／藍（低壓）**：`MAX_ACTIVE_BY_KIND.line` 是 3，
這一層可以跟緯度參考線＋板塊邊界（橘／藍／綠）同時打開；掃過整個 OKLCH 色域，能跟
那三個邊界色一起 all-pairs 全過又零 WARN 的**只剩色相 290–360° 的紫／洋紅**，而那一
族已經被火山帶的洋紅 `#c0259c` 佔走。所以改走「非分類固定角色」那條路（比照板塊的
暖褐與地震帶的中性灰）：彩度只有 0.066、**低於分類色下限 0.10**，本來就不是拿來跟人
比色相的顏色。明暗兩模式對面板底色是 4.93:1 與 3.44:1（都過 3:1，不必欠 relief）。

⚠️ 這一層是 `items` 變體，所以**不能有 `colorRole`**（型別擋著：`colorRole?: never`），
顏色寫在每個子項目的 `color` 上。

#### ⚠️ 標註是必要條件，而參數是實測出來的

四個部位同色，畫面上唯一寫出「這是西風」的東西就是沿線標註。實測（1512×772）：
`spacing: 60` 會讓氣壓帶的同一段短線重複放兩次（zoom 1.1 共 20 個），250 之後是 16 個；
`size: 11` 時「極地東風」四個字排不進箭頭，10 才進得去。**不可以照抄緯度參考線的
320**——箭頭在 zoom 1.8 下只有約 50 px，再高就一個都放不出來。

#### 驗證器也跟著補了一條：程式產生的圖層現在會被交叉檢查

`validate-content.mjs` 以前只對 `source.type === "remote"` 檢查 `featureIds`，而內容檔
的「id 必須在 geojson 裡找得到」對沒有 geojson 的圖層一律報錯。現在它會**直接跑一次
`generateLayer()`** 拿真正的圖徵 id 來比對（跟垂直植被帶那個 elevation 例外同一個形狀），
兩個方向都擋住：`featureIds` 打錯字 → 子項目變空圖層；內容檔檔名打錯 → 卡片退回只有
圖層說明。兩者在執行期都是**完全靜默**的。

### 洋流：全站第三個「程式產生」的圖層，也是唯一為了語意而放棄 all-pairs 的一組

幾何在 `generators.ts` 的 `oceanCurrents()`（18 條，控制點寫在 `OCEAN_CURRENTS`），
**沒有任何資料檔**。

#### 為什麼是 generator 而不是 `geo-manual/` 的一份手繪 geojson

CLAUDE.md 原本把洋流歸在「手繪教學示意幾何」，而它的**路徑**確實是手訂的（洋流沿著
海岸與海盆走，推不出來）。改走 generator 的理由是另外三件**手抄座標一定會出事**的事：

1. **箭頭。** 方向就是這一層的教學內容（北半球順時針、南半球逆時針的環流），而箭羽
   要在 Web Mercator 上看起來一樣大就得依緯度校正（同 `windBelts()`）。
2. **跨 ±180。** 北太平洋暖流、兩條赤道暖流與西風漂流**一定**會跨越換日線，跨了
   maplibre 會畫一條繞過整個地球的橫線**而且不報錯**（見國際換日線那節）。
   `splitAntimeridian()` 負責切段並在換日線上補端點——不補的話線會在畫面邊緣前幾度
   就停住，看起來像資料缺了一塊。
3. **控制點旁邊寫得下「這條是什麼」。** geojson 沒有註解，而 18 條每一條都有一句話
   要交代（黑潮怎麼繞過臺灣、祕魯寒流為什麼要貼著海岸）。

⚠️ 路徑仍然是**示意**的，所以圖層與 18 份內容檔都標 `schematic: true`。

⚠️ **控制點的經度刻意寫成「連續、可以超過 ±180」的形式**（北太平洋暖流從 145 寫到
232、西風漂流從 20 寫到 380），先在連續空間裡平滑與算箭頭，最後才一次 wrap＋切段。
反過來寫是踩過的坑：北赤道暖流原本寫成 wrap 過的 `…, -160, 190, 165, 143`，連續空間
裡那是一個 **+350° 的跳躍**，平滑與切段全部失效，產出 **29 段碎線與一條 44° 的橫線**。
往西流的洋流經度必須**一路遞減**（`-166 → -196 → -217`）。

#### ⚠️ 這一組色是全站唯一「為了保住語意而宣告不參與 all-pairs」的分類色

`MAX_ACTIVE_BY_KIND.line` 是 3，所以洋流**可以**跟板塊邊界同時打開，照理該把五個線色
一起驗。問題是**那樣就畫不出暖流／寒流**——掃過整個 OKLCH 色域（色相每 2.5°、
L 0.48–0.67、彩度 0.10–0.32，10,046 × 7,691 組），固定 `#c95c1c,#2f74c9,#159c6b` 之後
能讓五色 all-pairs 全過的只剩一族：**暖 = 洋紅／緋紅（h 0–10）、寒 = 藍紫（h 275–280）**，
而且每一組都還帶著對比 WARN。也就是暖流不能是紅的、寒流不能是藍的——而「紅暖藍寒」
正是這一層唯一要教的事（圖層名字就叫「洋流（暖流／寒流）」）。

所以走**柯本氣候分區 ↔ 森林與沙漠帶**那條既有判例：兩層可以同開，但兩組色票不可能
一起驗，於是把事實寫進 `notes` 請使用者一次看一層。組內驗證本身是全站最寬鬆的一組：

```bash
node <dataviz-skill>/scripts/validate_palette.js "#b00a1d,#133eec" --pairs all --mode light|dark
# → 兩模式五項全數 PASS、零 WARN；一般視覺 ΔE 39.1、CVD 32.9
# 對真的會一起讀的兩個中性線色（參考線灰、風系板岩藍）最小 ΔE 18.8
```

⚠️ **已知且接受的兩對衝突**（換色時要重量的就是這兩個數字）：寒流藍 ↔ 聚合型邊界藍
**ΔE 14.0**（CVD 10.0）、暖流紅 ↔ 張裂型邊界橘 **ΔE 14.1**（CVD 12.1），都低於 15 的
硬下限。而且是真的會碰到：**祕魯寒流貼著祕魯－智利海溝、親潮貼著千島海溝**，兩段都是
「藍線平行壓在藍線上」（已實測截圖確認）。⚠️ 但**色盲下反而不是問題**（CVD 都 ≥10），
短的是一般色覺那一項，補償是**洋流有沿線標註寫出自己的名字、板塊邊界沒有**——所以
`render.label` 在這一層是必要條件，不是裝飾。

⚠️ **不要把寒流改成更「正統」的藍**（h 245–255）：那正是聚合型邊界的色相，ΔE 會從
14.0 掉到 7.7。`#133eec` 的 h 265 是「還讀得出是藍、又離得最遠」的位置。

#### `indexFeatures` 在這一層是零成本的，而且是唯一的檢索入口

`items` 圖層沒有可點清單（`ThemeMapPage` 的 `!l.items`），所以搜「黑潮」「祕魯寒流」
只能靠搜尋索引。開 `indexFeatures` 在別的圖層要付一份 geojson 的流量（那正是它必須
明確開啟的原因），這一層**連 fetch 都不會發**——資料是程式產生的。

⚠️ 為此補了 `searchIndex.ts` 一個洞：它原本只認得「子項目自己有 `source`」那一種
（`if (!item.source) continue`），用 `featureIds` 從母圖層切分的圖層**即使宣告了
`indexFeatures` 也一條都索引不到**，而且完全靜默——搜「黑潮」只會靠 keywords 命中
「暖流」那個子項目，看起來像「本來就只索引到這一層」。

#### 其餘幾個實測值

- **18 條在主題預設視角（zoom 1.8）全部畫得出來，而且 18 個名字全部標得出來**
  （1800×953 實測 warm 11 + cold 9 = 20 個標註，長線會拿到兩個）。
  `maxAngle: 150` 是照抄臺灣河川那次的教訓——真實的彎曲路徑用預設的 60 會讓放置
  演算法**靜默拒絕**掉大半標註。
- `maxzoom: 6`：控制點只有幾十個，再放大那條「流軸」會變成一條假的精確曲線。
- 顏色**固定綁在暖／寒上**（`LayerItem.color`），不是依勾選順序指派：先勾寒流再勾
  暖流時暖流仍然必須是紅的，否則「紅暖藍寒」當場失效（比照古蹟三級與板塊邊界）。

### 世界紀錄熱帶氣旋路徑：33 個紀錄保持者，掛在「海洋」底下

`world-cyclones`（線）＋ `world-cyclone-centers`（附屬的圓點），取得邏輯全部關在
`scripts/lib/cyclones.mjs`，說明卡 33 份在 `src/content/geo/world-cyclones/`。

#### 收錄名單逐字對應維基百科〈熱帶氣旋〉的「紀錄」表

不是編者挑的，也不是「史上最強的 33 個」。那張表每一列附有原始出處（NHC 的颱風報告、
AOML 的 FAQ、各機構的新聞稿），一列一個紀錄：氣壓最低（狄普 870 hPa）、風速最高
（帕翠莎 185 節）、致死最多（波拉 50 萬人）、持續最久（弗雷迪 36 天）、行進最遠
（約翰 13,280 公里）、風眼最大（卡門 370 公里）與最小（葳瑪 3.7 公里）、覆蓋範圍最小
（馬可，烈風半徑 18.5 公里）、最靠近赤道（阿耆尼 0.7°N）、風暴潮最高（馬希納 13 公尺）、
陣風最強（奧里維亞 113.2 m/s）、總降雨最大（亞森特 6,083 mm）…

**加一筆或少一筆都會讓「這張表在地圖上長什麼樣子」對不起來**，所以 `CYCLONES` 那張表
的長度就是名單本身。要更新的時機只有一個：維基百科那張表因為新的紀錄而改了。

#### 為什麼掛在「海洋」而不是自成一個「天然災害」組

熱帶氣旋是海洋餵出來的（海面水溫 26.5 °C 以上、暖水層要夠深），所以 33 條路徑的起點
全部落在低緯的暖洋面上——唯一的例外卡塔琳娜正是因為「南大西洋不該有」才成為紀錄。
跟同組的「洋流」疊起來看，暖流流過的海域正是氣旋走廊。

⚠️ 它跟臺灣主題的 `tw-typhoons` 是**不同的東西**，不要合併也不要互相照抄參數：那 14 個
是「侵臺並造成重大災害」的颱風、依氣象署的侵臺路徑分類分組、時間換算成臺灣時間；這
33 個是全球紀錄保持者、依生成洋盆分組、時間是 UTC，分級單位也不同（見下）。

#### 資料源：IBTrACS v04r01，一份 331 MB 的 CSV，而我們只要 33 個氣旋

`ibtracs.ALL.list.v04r01.csv`（NOAA NCEI，免金鑰、`ACAO: *`）收錄 1842 年以來全球
13,584 個氣旋、逐 3 小時一筆。

- ⚠️ **不可以 `await res.text()`**——那是把 331 MB 一次讀進記憶體。`streamRows()` 逐塊
  解碼、逐行過濾，先比對前 13 個字元（SID 是定長）再決定要不要 `split(",")` 那 174 欄。
- ⚠️ **不要改抓 `ibtracs.since1980.*`**（小很多）：卡門(1960)、南施(1961)、波拉(1970)、
  狄普(1979)、馬希納(1899) 五個都在 1980 年以前，而它們正是最重要的幾筆。分成
  「since1980 ＋ 幾個洋盆檔」反而更大——光是 WP 一個洋盆就有 114 MB。
- ⚠️ 上游支援 Range（實測回 206），理論上可以拿 SID 排序做二分搜尋把下載量壓到幾 MB。
  **刻意不做**：那要依賴「列一定依 SID 排序」這個沒有任何承諾的性質。
- **序號（SID）是人工對出來的**，來源是官方的 `IBTrACS_SerialNumber_NameMapping_*.txt`
  （1 MB，⚠️ 檔名帶日期、每次改版都會變，所以**不由程式抓**）。SID 的格式是
  `<年><年積日><半球><起始緯度><起始經度>`，肉眼就能粗核：狄普的 `1979275N06159`
  ＝ 1979 年第 275 天、北緯 6 度、東經 159 度生成。⚠️ **五筆上游沒有名字**（波拉、
  奧里薩、阿耆尼、馬希納、卡塔琳娜）是靠生成時間與位置對出來的，最危險。

#### ⚠️ 三個踩過的坑

1. **`Number("  ")` 是 0，不是 NaN。** IBTrACS 的缺值不是空字串，是**空白填滿的欄位**。
   `num()` 少了 `trim()` 的那一版，狄普的「中心氣壓最低」變成 **0 hPa**、波拉的風速變成
   **0 節**，而且不報錯——地圖上只會看到色階整片落在最淺那一階。交叉檢查是靠
   「最低氣壓 0 vs 紀錄表 870」那行警告才抓到的。
2. **氣壓可以退回 `WMO_PRES`，風速不行。** `USA_PRES` 對 1990 年代以前幾乎整欄是空的
   ——**狄普那個 870 hPa 的世界紀錄只存在於 `WMO_PRES`**。氣壓沒有「平均時距」的問題，
   退回是安全的；風速有（見下），所以只取 `USA_WIND`，缺了就是缺了。
3. **8 條跨越 ±180，而且 IBTrACS 的經度是連續的（>180 不 wrap）。** 約翰 1994 從
   墨西哥外海一路西行到 172.9°E，上游寫成 266.9 一路遞減。不切段的話 maplibre 會畫一條
   繞過整個地球的橫線**而且不報錯**（跟洋流、國際換日線同一個坑）。南施、狄普、佛瑞特、
   舒力基、佐伊各切 2 段，**約翰、伊歐佳、溫斯頓來回跨兩次、各切 3 段**。
   實測驗法：每條線的最大經度跨距都必須 `< 180`。

#### 風速一律取 `USA_WIND`（一分鐘平均），色階因此不是氣象署那一套

各機構的平均時距不同：美系（NHC／JTWC）一分鐘、日本氣象廳與澳洲氣象局十分鐘、
印度氣象局三分鐘，同一個颱風用十分鐘平均會低 10–15%。混著用等於在同一條色階上比兩把
不同的尺，所以只取 `USA_WIND`——那也是維基百科紀錄表「一分鐘平均風速」欄的來源。

`CYCLONE_INTENSITY_RAMP`（`thematicColors.ts`）因此用**薩菲爾—辛普森**的門檻（節）：
34／64／113，對應熱帶性低氣壓／熱帶風暴／一～三級／四～五級。

⚠️ **顏色逐字沿用 `TYPHOON_INTENSITY_RAMP`**（`#e79e80,#d35f2a,#af4611,#873203`，
已跑過 `--ordinal` 明暗兩模式四項全過），**沿用等於不新增任何未驗證的顏色**。
換色時**兩條 ramp 要一起換**，否則兩張地圖的圖例從此不一致。

⚠️ **這一條要宣告 `nodata`，臺灣那條刻意不宣告。** 判準是「真的會有缺值嗎」：臺灣那
757 個定位點每一筆都有風速；這一層收了 **1899 年的馬希納與 1970 年的波拉，整條路徑
一筆風速都沒有**（那個年代沒有衛星也沒有飛機偵察）。畫成最淺那一階等於謊報「它很弱」
——馬希納保持的紀錄是 13 公尺的風暴潮。灰 `#7d7c76` 取自水庫那條 ramp 的「暫無資料」。

#### ⚠️ 這是全站第一個「非世界櫥窗」的 `exemptFromMaxActive`

> ⚠️ **2026-09 起有第二個：「規模最強地震」**（同樣是 `hazard`，判準逐字相同，
> 見那一節）。這一節寫的論證仍然是原始出處。

`MAX_ACTIVE_BY_KIND.line` 是 3，而世界主題已經有七個線圖層在搶（現在八個）。判準仍然是
types.ts 那一條——**它用的是「非分類的固定角色色」`hazard`，多開一層畫面上一個新色相
都沒有增加**——但它跟世界櫥窗那九層**不完全同型**：那九層跟同組其他層本來就同色，
這一層是「海洋」組裡唯一的 `hazard`。差別在於「同色」不是判準的目的，**「不新增分類
色相」才是**；中性灰本來就不參與 `LINE_PALETTE` 的 all-pairs，把它算進名額等於用一個
它不屬於的預算去限制它。而它最該疊的兩層——同組的「洋流」與「板塊邊界」——正好都是
吃色票的線圖層，不豁免的話「暖流餵出氣旋」這件事永遠看不到。

⚠️ **不要因此把 `MAX_ACTIVE_BY_KIND.line` 調高**：那個數字等於色票長度，是 all-pairs
驗證的前提。實測（勾滿緯度參考線＋國際換日線＋世界主要河流之後）：世界主要山脈與洋流
的核取方塊是 disabled，**熱帶氣旋那一格仍然可勾，而且勾了 33 條全部畫得出來**。

#### 沿線標註的 `spacing: 600` 是掃出來的

1920×859 的 CSS 視窗，五個視角各數「標到名字的線／畫面上的線」與「同一個名字最多重複
幾次」：

| spacing | z1.8 全球 | z4 西北太平洋 | z5 加勒比海 | z4 孟加拉灣 | z4 南太平洋 |
|---|---|---|---|---|---|
| 400 | 24/33・重複 3 | 11/11・重複 4 | 7/7・重複 3 | 14/14・重複 2 | 6/6・重複 3 |
| **600** | **24/33・重複 2** | **11/11・重複 3** | **7/7・重複 3** | **12/14・重複 2** | **6/6・重複 2** |
| 900 | 24/33 | 10/11 | 7/7 | 9/14 | 6/6 |
| 1200 | 24/33 | 9/11 | 4/7 | 5/14 | 6/6 |

⚠️ **z1.8 的 24/33 不是參數調得不好，四個值全都是 24。** 全球視角下有九條路徑在畫面上
太短（馬可只有 577 公里、卡塔琳娜 856 公里），放不下三個字。`maxAngle: 150` 照抄臺灣
河川那次的教訓（預設的 60 會靜默拒絕整條線）。

#### 定位點沿用臺灣颱風那一層的兩個結論

- **共用母圖層的 `properties.id`**（1,729 個點只有 33 個不重複的 id，`sharedIds: true`，
  `scripts/validate-content.mjs` 的白名單也要補一筆）。共用之後點定位點會開出那個氣旋的
  卡片、選取時整條路徑連同它的定位點一起加粗。⚠️ **不要改成唯一 id ＋ `detail: none`**，
  臺灣那一層已經驗證過那條路是壞的。
- **半徑同時吃 zoom 與風速。** 只吃風速的話，世界視角（zoom 1.8，比臺灣那層的 4.2 更遠）
  下 1,729 個點會糊成一片。
- ⚠️ **`attach` 沒有宣告 `browse`**（比照颱風的中心定位點）：1,729 個沒有名字的點灌進
  抽屜會把清單變成一千七百多列，而且搜尋索引也不該去抓那 268 KB。實測聚焦搜尋框之後
  只多抓 `world-cyclones.geojson`（33 KB），`world-cyclone-centers.geojson` **零次**。

#### `digits: 3`，`tolerance: 0`

⚠️ **不要降到 2 位**（≈1.1 km）。世界尺度看起來一樣，但學生會放大去看卡崔娜怎麼進
紐奧良——zoom 12 時 0.01° 就是一百多個像素，路徑會變成樓梯狀而且定位點會落在轉角上。
⚠️ **`tolerance: 0`**：最佳路徑本來就是每 6 小時一個定位點，Douglas–Peucker 會把轉向點
抹平，而那正是這一層要教的東西。33 條合計只有 1,729 個點、產物 33 KB／268 KB。

#### ⚠️ 卡片上的數字與地圖上的顏色來自兩套重新分析，這件事要寫進 `notes`

紀錄表引的是**各洋盆負責機構**的值，地圖的色階用的是 IBTrACS 裡**美系機構**的重新分析。
實測 33 筆裡最大的落差是**莫妮卡 37 hPa**（澳洲氣象局 916 vs JTWC 879），其次法尼 32、
阿耆尼 30、溫斯頓 23、古努 22、天鵝 21。**這些不是錯，是同一個氣旋的兩個官方答案。**
`fetchCyclones()` 的交叉檢查門檻因此放到 40 hPa／30 節——對錯氣旋的話落差是三位數
（把缺值當 0 那次是 870–998 hPa），這個門檻擋得住。

### 世界底圖的地名一律改成繁體中文（只換表達式，不換資料源）

「世界地圖」（OpenFreeMap Liberty）原本的 `text-field` 是 `拉丁名\n當地文字`，於是德國寫
`Deutschland`、埃及寫 `مصر`——一張給國中小學生看的地圖上有一半的地名讀不出來。

修法**不需要另一份資料**：OpenMapTiles schema（OpenFreeMap 與 Carto 都是這個 schema）
本來就把 OSM 的 `name:*` 多語名稱原樣放進圖磚，包含 `name:zh-Hant`／`name:zh`／
`name:zh-Hans`。所以 `basemaps.ts` 的 `localizeStyle()` 在樣式套用前，把每一個「用地名
當文字」的 symbol 圖層換成一條繁體中文優先的表達式（判準是原本的 `text-field` 有沒有
提到 `name`——公路盾牌用 `ref`、門牌用 `{housenumber}`，兩者都不該被改寫）。

⚠️ **順序不能改成先 `name:zh`**：那一欄在 OSM 上簡繁混雜，只有 `name:zh-Hant` 保證是
繁體。實測世界尺度的 171 個國家有 154 個帶 `name:zh-Hant`。

⚠️ **`index-of` 的參數順序是 `[要找的東西, 被搜尋的字串]`**，跟 `slice` 與 JS 的
`indexOf` 相反。寫反了 `index-of` 一律回 -1，而 `slice` 的 end 吃到負數是**從尾巴倒數**
——結果是**每一個地名都少掉最後一個字**（德國→德、埃及→埃），`tsc` 抓不到（表達式是
斷言進型別的）、console 也一個字都不會印。實測踩過，只有把地圖畫出來看才發現得了。

⚠️ **多值名稱要切掉第二段以後**。`name:zh-Hant` 會出現「德拉瓦州;特拉華州」
「斯威士兰 / 史瓦蒂尼」這種一格塞好幾個譯名的寫法（世界尺度約 1.5% 的標籤）。
`NAME_SEPARATORS` 把帶空白的 `" ;"`／`" /"` 排在前面一起比，否則會留下尾隨空白。

⚠️ **`ZH_HANT_OVERRIDES` 是逐筆改寫，不是簡繁轉換器**——maplibre 的表達式沒有辦法逐字
換字形，所以這份表**不可能補完**，收錄範圍必須是一條講得出來的界線。現在的界線是機械掃
出來的：`place` 圖層的 `country`／`continent` 加上整個 `water_name` 圖層，在 **zoom 2–5**
（攤開整個世界時字最大的那一層標籤）的所有圖磚裡，逐筆看過選出來的名稱。實測 171 國 +
9 大洲 + 116 個水體，其中 47 筆要改（簡體殘留，外加危地馬拉→瓜地馬拉這種非臺灣譯名）。

⚠️ **省、州與城市刻意不收**：那個尺度有上萬筆、長尾沒有盡頭（悉尼／孟买／奥克兰……），
硬編一份維護不起來。**放大到單一國家之後仍會看到簡體城市名，那是已知限制，不是漏掉。**

⚠️ 備援底圖（Carto Positron）**也要改寫，所以它不能再只回網址字串**——`loadBasemapStyle`
現在兩份向量樣式都先抓成 JSON。Positron 用的是舊式的 `"{name_en}"` 字串樣板（甚至有
stops 物件），JSON 化之後一樣比對得到 `name`。實測它的 28 個 symbol 圖層改寫了 26 個，
沒被動到的是門牌與本站自己的 `contour-labels`。

⚠️ 中文字形靠 `MapView` 既有的 `localIdeographFontFamily`（OpenFreeMap 的 glyph 端點沒有
中日韓字）。**那個選項不能拿掉**，拿掉之後標註會整片變成空白方塊。

三種 NLSC 底圖是 raster 的臺灣官方地圖，本來就是中文，不經過這條路徑。

---

## 主題地圖頁：世界主題的部分

> 對應 `CLAUDE.md` 的「主題地圖頁與圖層註冊表」。註冊表本身的架構、`attach`、
> 圖層 id 的組合方式、三個關鍵坑、顏色策略與 `MAX_ACTIVE_BY_KIND` 的意義
> 都留在 CLAUDE.md。

### 世界重要城市：2026-08 從 5 個補到 31 個

`world-places` 圖層讀的是 `src/content/places/` 裡 `region === "world"` 的那些
（`resolve.ts` 的 `places-world`），所以**補城市不必動註冊表，加內容檔就好**——那句話
本來就寫在圖層的註解裡，這一批就是照著做的。

#### 挑選判準：每一個城市要能單獨當一堂課

不是「大城市排行榜」，也不會補完。收錄的條件是**它身上有一件別的城市講不了的事**，
而且那件事在本站的圖層上找得到對照：

| 收的東西 | 例子 |
|---|---|
| 一個氣候型的標準代表 | 新加坡（Af）、羅馬（Csa）、莫斯科（Dfb）、努克（ET） |
| 一組只差一個變因的對照 | 東京↔洛杉磯（東西岸）、新加坡↔奈洛比（海拔）、雷克雅維克↔努克（洋流） |
| 一個人地關係的經典案例 | 杜拜（海水淡化）、開普敦（Day Zero）、雅加達（地層下陷與遷都）、拉哥斯（過度都市化） |
| 一個已經在站上的圖層需要的落腳點 | 伊斯坦堡→博斯普魯斯海峽、杜拜→荷姆茲海峽、開普敦→好望角、雷克雅維克→中洋脊 |

⚠️ **氣候型的覆蓋是刻意排出來的**，A／B／C／D／E 五大類都要有代表，而且同一類裡
盡量收「成因不同」的：三個 BWh（杜拜＝副熱帶高壓、利馬＝寒流海岸、愛麗絲泉＝大陸
內部）講的是三件事。⚠️ **不要為了湊「每洲幾個」而收**——這一層的價值在對照，不在
均勻分布。

#### ⚠️ 三筆的 ERA5 數字跟課本／測站差很多，內容檔要自己交代

比照既有的希洛（實測年雨量 3,300 mm、ERA5 只給 1,590）。這一批實測下來有三筆要注意，
**寫卡片時不可以只抄課本數字了事**（圖表畫的是 ERA5，讀者兩邊一對就會發現不一致）：

| 地點 | 課本／測站 | ERA5（本站圖表） | 卡片怎麼寫 |
|---|---|---|---|
| 利馬 | 年雨量約 13 mm | **85 mm** | 內文明寫「測站量到的是十幾公釐，本站圖表用的 25 km 網格把周邊安地斯山區的雨平滑了進來」 |
| 雪梨 | 年雨量約 1,150 mm | **681 mm** | 改寫成「一千公釐上下」，不引測站的精確值 |
| 努克 | 一月均溫約 −8 °C | **−14.4 °C** | 改寫成「比雷克雅維克低了十幾度」，兩個數字都成立 |

⚠️ 其餘的城市實測都對得上（北京 −3.7／26.9／545 mm、倫敦 4.8／17.7／649 mm、
杜拜 69 mm、愛麗絲泉 232 mm…），**新增地點之後請照這個方式逐筆核對一次**：
`node -e` 讀 `public/data/climate/<id>.json` 把一月／七月／年雨量印出來，跟卡片上寫的
數字對。這件事只能人工做——`validate-content.mjs` 只檢查「有 JSON 的必須對得到地點」。

#### 柯本代碼一律照公認的分類填，不要自己從 ERA5 算

比照臺灣那半邊的既有規則（`CLAUDE.md`「⚠️ `koppen` 不可以拿 ERA5 自己算」）。
幾筆值得記下來的判斷：

- **德里**填 `BSh`。它落在 BSh 與 Cwa 的交界上，兩種寫法在文獻裡都看得到；卡片內文
  把「半乾燥與濕潤只有一線之隔」直接寫出來，而不是假裝沒有爭議。
- **馬瑙斯**填 `Am` 不是 `Af`（最乾月低於 60 mm，ERA5 因為平滑而算出 96 mm——又一個
  「不要拿 ERA5 當判準」的例子）。
- **雅加達**同理是 `Am`；本站的 `Af` 代表是新加坡與（雨林區的）亞馬遜。

#### 五組新的 `/compare` 配對

| 組合 | 緯度 | 變因 | 實測 |
|---|---|---|---|
| 東京 ↔ 洛杉磯 | 35 | 大陸東岸／西岸 | 東京年雨量 1,465 mm 且夏雨；洛杉磯 347 mm 且夏季近乎零 |
| 新加坡 ↔ 奈洛比 | 0 | 海拔（15 m ↔ 1,795 m） | 年均溫 26.4 °C ↔ 18.7 °C |
| 聖保羅 ↔ 愛麗絲泉 | −23.5 | 南回歸線上的海陸位置 | 1,289 mm ↔ 232 mm |
| 開普敦 ↔ 雪梨 | −33.9 | 南半球東西岸（夏乾／全年有雨） | 兩地最暖月相近，雨量的季節分配相反 |
| 雷克雅維克 ↔ 努克 | 64.2 | 洋流（暖流／寒流） | 一月 −0.9 °C ↔ −14.4 °C，最暖月 11.8 ↔ 8.9 |

⚠️ **雅庫茨克現在有配對了**（雷克雅維克，62°N vs 64°N）。`CLAUDE.md` 舊版寫「62°N
在本站找不到同緯度的另一個地點」，那句話已經被這一批補掉——**但它背後的規則沒變**：
硬湊緯度差太多的兩地會讓 `lat`（兩張地圖共用）落在誰身上都不對。

### 柯本氣候分區：30 個亞型 2026-08 全部補上說明卡

在那之前這一層走的是 `FeatureCard` 的 fallback（名稱＋判準＋「代表地點：…」），
點下去只知道自己站在哪一格，不知道那一格為什麼在那裡。現在
`src/content/geo/koppen-zones/` 有 30 份，`stats` 四格固定是**判準／氣溫／雨量／
代表地點**——⚠️ 最後那一格**必須保留**，因為有內容檔之後 `FeatureCard` 就不再畫
geojson 的 `meta` 與 `detail` 了（那兩行原本就是判準與代表地點），不接住等於刪掉資訊。

⚠️ **判準要照 Kottek et al. (2006) 那一版寫**（本站的網格就是那一份）：C／D 的分界
是最冷月 **−3 °C**，不是有些課本用的 0 °C。差異寫進 `cfa` 那張卡，不要在三十張卡上
各寫一次。

⚠️ **搜尋是這一層唯一的檢索入口**（沒有 `browse`、`indexFeatures` 也刻意不開，開了
每個學生一聚焦搜尋框就多付 332 KB），所以 30 個亞型的中文名要**進 `items.keywords`**。
補完卡片之後把 keywords 一起加長了：實測搜「熱帶雨林氣候」「地中海型氣候」「針葉林
氣候」「苔原氣候」「沙漠化」在那之前**全部是零結果**（舊的 keywords 只有「熱帶雨林」
這種不含「氣候」二字的詞，而比對是子字串比對，查詢比 haystack 長就沒得配）。
⚠️ 搜到的是**大類那個子項目**（例如「A 熱帶氣候」），點下去是把那一類打開——這是
`items` 圖層的既有行為，不是漏掉。

#### 五個大類自己也要一張卡（`items.detail`）

30 個亞型補完之後還缺一半：抽屜裡「A 熱帶氣候」那五個字是可以點的，而它預設開的是
**母圖層的** `detail`（`koppen-zones`）配上 item id `a`——`koppen-zones` 裡只有
`af`／`am`／`as`／`aw`，永遠查不到。⚠️ **實測開出來的整張卡只有「柯本氣候分區」五個字
加一行資料來源**（`hideLayerDescription` 又把圖層說明擋掉，所以比一般的 fallback
更空），而且完全靜默——沒有錯誤、`validate` 也不會擋。

修法照古蹟三級與主要作物三類的既有做法：宣告
`items.detail: { type: "geo", collection: "koppen-groups" }`，內容放
`src/content/geo/koppen-groups/{a,b,c,d,e}.json`。宣告之後 `validate-content.mjs`
**兩個方向都會擋**（少一份內容檔失敗、多一份對不到 item 的也失敗），這個洞才補得住。

⚠️ **兩張卡回答的是不同的問題，不要把其中一張寫成另一張的摘要**：
大類卡講「這一類的判準是什麼、底下的字母怎麼組、為什麼分布在那條緯度」，
亞型卡講「Cfa 是什麼、代表地點有哪些」。地圖上點色塊仍然開亞型卡（實測臺北一帶
是 Cfa、撒哈拉是 BWh），只有點抽屜裡的名稱才開大類卡。

⚠️ **面積是自己從產物算的**（`geometryAreaKm2`，跟板塊、大洲同一支）：
A 3,343 萬 km²（21%）、B 4,211 萬（27%，最大的一類）、C 2,397 萬（15%）、
D 3,441 萬（22%）、E 2,466 萬（15%）。卡片寫的是「占五類的 X%」而不是「占陸地的
X%」——0.5° 網格把跨海岸線的格子整格算進來，五類總和是 1.586 億 km²，比陸地的
1.49 億多了 6%，講成「占陸地」會是一個對不上的數字。

### 世界主要河流：118 筆裡的 33 筆有說明卡

課本會點名的那些（尼羅河與青／白尼羅河、亞馬遜、長江與金沙江、黃河、湄公河與瀾滄江、
恆河、印度河、布拉馬普特拉與雅魯藏布、黑龍江、鄂畢、勒拿、伏爾加、多瑙、剛果、尼日、
尚比西、密西西比與密蘇里、聖羅倫斯、馬更些、育空、巴拉那、奧里諾科、墨累與達令、
伊洛瓦底、幼發拉底），其餘維持 fallback。

⚠️ **上游是依「河段名稱」收錄的**，所以同一條河在圖上常常是好幾筆：尼羅河另外有
艾伯特段、維多利亞段、傑貝勒段與兩條三角洲分流，長江上游叫金沙江／通天河／沱沱河，
湄公河的中國段叫瀾滄江，黑龍江有 `amur-3` 與 `heilong-jiang-59` 兩筆。**卡片講的是
整條河**，這件事寫在圖層的 `notes` 裡（連同「118 筆裡有 33 筆有卡片」），不要在
三十幾張卡上各寫一次。

⚠️ **「內格羅河」故意沒有卡片**：維基百科那個標題是消歧義頁（多條同名的河），而這一層
的內格羅河有兩筆（亞馬遜支流與巴塔哥尼亞）。**不要為了補齊而把來源連到消歧義頁**
——本站的規則是「每一筆來源都要連得到那份資料本身」。

### 火山帶與最高峰：只補課本會點名的那幾座

`volcanoes`（1,214 座）補了 9 張（喀拉喀托、坦博拉、富士山、聖海倫斯、皮納土玻、
基拉韋亞、埃特納、聖托里尼、波波卡特佩特），`world-mountain-peaks`（39 座）補了 10 張
（聖母峰、K2、阿空加瓜、迪納利、厄爾布魯士、白朗峰、查亞峰、科修斯科、庫克山、
雷尼爾）。其餘維持 fallback——那兩層的 geojson 本來就帶著類型、海拔、最後噴發年代與
「某某山脈主峰」，fallback 讀得出東西，跟河流那種只有名字的情況不同。

⚠️ **維蘇威火山刻意不寫 `volcanoes` 的卡片**：它在「火山與災害」那一層已經有一張以龐貝
事件為主的卡（不同 collection），再寫一張只會變成兩份會漂開的文字。

⚠️ **主峰的內容檔要寫 `schematic: false`**，理由跟 `attach.schematic: false` 相同：
母圖層的中軸線是算出來的示意幾何，主峰是真實座標。內容檔一旦存在，`FeatureCard` 讀的
就是 `feature.schematic`，漏寫會讓聖母峰那張卡冒出一行「這是簡化的教學示意幾何」。

⚠️ **這幾座的維基條目標題不等於卡片上的名字**：迪納利山的條目是「德納利峰」、
聖海倫斯火山是「聖海倫火山」的重新導向（實際標題「聖海倫斯火山」）、聖托里尼**不加
「島」**、K2 要用「喬戈里峰」。全部實測過 HTTP 200，改動時照 `sourceLinks.ts` 那段
註解的規則走。

### 世界人口分布：planned 的兩個圖層之一 2026-08 做出來了

`world-population`（505 個都會區人口 100 萬以上的城市）。原本跟「主要農業帶」一樣掛著
`status: "planned"`，做得出來的關鍵是發現 **Natural Earth 的 `ne_10m_populated_places`
帶著 `NAME_ZHT` 正體中文名**——本站對世界圖層的中文名一向是自己維護對照表（39 條山脈、
1,214 座火山的 40 幾個常見名），505 個城市用對照表是不可能的。

⚠️ **要用完整版那一份，不是 `_simple`**：後者沒有任何多語名稱欄位。取得、join 與筆數
檢查在 `scripts/lib/world-population.mjs`，中文國名從**大洲分區已經在抓的那份國界檔**
join 過來（key 是 `ADM0_A3`，⚠️ 不要用國名字串）。

#### 為什麼是點資料而不是人口密度網格

這一層要教「世界人口分布不均」。真正對應那句話的是人口密度網格（SEDAC GPW、WorldPop），
但那些檔案以 GB 計、而且 **SEDAC 要註冊帳號**——本站的硬性禁令第 1 條就是不得引入需要
金鑰的服務。點資料換來的是「哪裡有大城市」，換不到「鄉村人口有多密」，⚠️ 這件事寫進
`notes` 第一條（恆河平原、爪哇與尼羅河谷的鄉村密度是全球最高的幾處，在這一層上只有幾顆點）。

#### 顏色：紫在這個主題裡是 hard FAIL

語意上該沿用臺灣人口與都市體系的紫（`POPULATION_COLOR` `#87519e`），但世界主題有
**火山帶的洋紅** `#c0259c` 同框，紫對洋紅的 CVD ΔE 只有 **2.8**、一般視覺 **12.0**
（低於 15 的硬下限）。紫色系整族都不行，實測見 thematicColors.ts 的
`WORLD_POPULATION_COLOR`。

固定 `place` 藍與 `volcano` 洋紅之後掃過 OKLCH 色域，**9,967 個候選全數通過**——這一格
不像線／面色票那樣擠，所以判準改成「餘裕最大」，選出橄欖金 `#80610d`：

```bash
node <dataviz-skill>/scripts/validate_palette.js "#2a78d6,#c0259c,#80610d" --pairs all --mode light|dark
# → 兩模式五項全數 PASS、零 WARN；最弱的一對仍然是既有的洋紅↔藍（CVD 11.3／一般 26.0）
```

⚠️ **驗證器測不到的那一關（疊在半透明面染上讀不讀得出來）另外算過**，因為這一層的
教學用法就是跟柯本或生物群系疊著看：把兩層的面染先合成到底圖陸地色上，再跟圓點比——

| 疊在什麼上 | 合成後的顏色 | ΔE（一般） | ΔE（deutan） | 對比 |
|---|---|---|---|---|
| 柯本 A–E（0.25） | `#c1dde0`…`#efd4bd` | 34.2–38.6 | 33.5–37.7 | 3.5–4.1 |
| 生物群系六類（0.18） | `#d1dbcc`…`#eed6ea` | 37.6–40.9 | 37.2–40.1 | 4.0–4.4 |
| 純底圖陸地 | `#f7f2e8` | 46.0 | — | 5.2 |

⚠️ 深綠與青綠在這一關會輸（熱帶雨林 `#25744c` 與溫帶林 `#369db8` 染出來的正好是同一個
亮度帶），所以**要改這個顏色就重算一次這張表**，不要只看驗證器。

#### ⚠️ `POP_MIN` 有一條壞掉的尾巴——這是「看畫面」那一關抓到的

卡片第二行原本無條件印「市轄區人口」（上游的 `POP_MIN`）。**數值檢查全過、
validate 全過，是實際打開巴黎那張卡才發現它寫著「市轄區人口 1.1 萬」。**

逐筆量過：505 筆裡有 **24 筆的 `POP_MIN` 不到 `POP_MAX` 的一成**，而那 24 筆
每一個都是明顯壞掉的值——拉哥斯 0.2 萬／947 萬（0.02%）、巴黎 1.1／990、
聖地亞哥 4.7／572、馬德里 5.0／557、羅馬 3.5／334。一成到兩成半那一段（63 筆）
相反，**全部合理**：東京 834／3,568（23%）、波士頓 59／447、深圳 100／758、
里約 201／1,175。所以門檻設在 **10%**：低於它就不印那一行（那 24 張卡仍然有
名稱、國家與都會區人口），其餘照印，實測 434／505 有那一行。

⚠️ **不要改回無條件印**：在一張教地理的卡片上印「巴黎市轄區人口 1.1 萬」，是把
上游的錯誤當成事實教出去。⚠️ 也不要拿它當通則去清理別的欄位——`POP_MAX` 本身
是好的，壞的只有 `POP_MIN` 這條尾巴。

#### 半徑與「被蓋住的 35 個點」

`sqrt(人口)` 內插到 2.2–12 px。用產物座標算 Web Mercator 投影，主題預設視角（zoom 1.8、
1512 px 寬）下 505 個點有 221 個與鄰點相切或重疊、**35 個被完全蓋住**——⚠️ 那 35 個
**全部是巨型都會旁的衛星市**（新北市／橫濱／吉薩／豪拉…），在這個尺度上它們本來就屬於
同一團；zoom 3 剩 18 個、zoom 4 剩 10 個。

⚠️ **不要為了它們把上限壓到 8 px**：實測只換回 12 個，代價是唯一的編碼（大小＝人口）
變弱——三千萬與一千萬的差別會從 5.5 px 縮到 3 px。

#### ⚠️ 刻意沒有 `browse`，所以它不進搜尋索引

兩個理由（比照火山帶）：505 列會變成全站最長的可點清單，而這一層要讀的是**分布的形狀**；
更重要的是**「世界重要城市」那 31 個才是可檢索的清單**——兩層都進索引的話，搜「東京」
會出現兩筆同名結果，而其中只有一筆開得出寫過的卡片。要改成可搜尋之前，先想清楚那
31 個重複名怎麼辦。

#### 「主要農業帶」為什麼還留在 planned

不是忘了做，是**兩個條件同時不成立**：

1. **沒有免金鑰的現成資料集。** FAO 的 GAEZ 要走它自己的 data viewer 匯出、ESA WorldCover
   是 10 m 的土地覆蓋（不是農業型態）而且以 GB 計。剩下的路是**自己手繪示意面**放
   `geo-manual/`（本站對示意幾何是有先例的），但那等於編者要替全球畫一份「小麥帶／稻作區／
   放牧區」的界線——那是內容判斷，不是資料取得。
2. **面色票沒有名額。** 這一層必然是 `items`（好幾種農業型態各一個顏色），而面色票已經被
   大洲、生物群系六類、柯本五類、板塊佔滿，新的一組要自己掃一份 all-pairs 出來，而且
   `MAX_ACTIVE_BY_KIND.fill` 是 2——它跟柯本／生物群系搶名額，而那兩層正是它最該疊著看的。

⚠️ 要做它之前先把這兩件事解掉，不要直接動註冊表。

### 古文明發源地：「人文專題」底下的第一個 ready 圖層

2026-08 新增 `world-civilizations`（6 個遺址，circle），掛在**人文專題**——那一組原本
只有 `status: "planned"` 的「主要農業帶」，所以抽屜上根本看不到那個標題
（`LayerPanel` 對空的分組是 `return null`，planned 的圖層雖然列得出來，但那一組在這之前
只有它一個）。

⚠️ **它一度是當時還叫「作者精選」的編者選集底下的一個 `category`，同一天就搬出來了。**
搬的理由不是技術問題（同一層混不同 `category` 本來就是那一層的常態，而且同月稍後那些
`category` 全部各自升格成獨立圖層了），而是定位：編者選集收的是**編者挑的地方，彼此
沒有共同主題**；這六筆相反——它們是
**同一個問題的六個答案**，要對著看才有意義。搬家連帶動了四個地方，缺一不可：
幾何從 `world-picks.geojson` 切到 `world-civilizations.geojson`（前者回到 17 筆）、
內容檔從 `src/content/geo/world-picks/` 移到 `src/content/geo/world-civilizations/`
（六份的 `collection` 欄位要跟著改，`validate-content.mjs` 兩個方向都會擋）、
`world-picks` 的 `description`／`notes`／`sources` 要把加進去的那幾條**移除**，
以及註冊表多一個圖層物件。

#### 為什麼是六個，而不是課本的「四大」

「四大文明古國」是梁啟超等人提出、盛行於東亞的說法，**未被學術界或國際社會採納**
（中文維基〈文明的搖籃〉那一條就是這麼寫的）；學界普遍承認的獨立起源地至少有六處。
**那兩個新大陸的例子正是這一層存在的理由**——沒有它們，這一層就只剩下課本已經講過的
四個名字，也講不出下面這件事：

| | 環境（本站圖層實測） | 它推翻了什麼 |
|---|---|---|
| 烏魯克・孟菲斯・摩亨佐-達羅 | BWh 沙漠氣候，靠一條外來河 | —（這三個正是「大河＋灌溉」的原型） |
| 二里頭 | **Cwa 冬乾夏雨** | 四個舊大陸文明裡唯一不是全靠灌溉的 |
| 卡拉爾 | 海岸沙漠（RESOLVE 畫的是沙漠與乾旱地） | 沒有陶器、沒有文字，先有城市才有那些「必要條件」 |
| 聖羅倫索 | Am 熱帶季風氣候 | 問題是水太多不是太少，而且沒有大河 |

⚠️ 那六格的柯本與生物群系**是拿本站自己的圖層資料做點在多邊形內測出來的**，不是抄的；
六張卡的「對照重點」寫的就是這個結果。

#### 可點清單分「舊大陸／新大陸」，那不是經度分組

`browse.groupBy: "category"`，而 `category` 是 **`"舊大陸"` / `"新大陸"`**——這一層的
整個論證就靠那條線。⚠️ `groupBy` **依序切、不排序**，所以 geojson 裡舊大陸那四筆必須
連續排在前面（孟菲斯 → 烏魯克 → 摩亨佐-達羅 → 二里頭），新大陸兩筆接在後面
（聖羅倫索 → 卡拉爾），**各組內才是由西往東**。這跟「海峽」那一層的七條海峽
「整層依經度排」的規則不同，是刻意的。

#### 每一筆的錨點是一個遺址，取景則是逐筆實測的

文明本身是一整條河谷或一整片低地（印度河流域文明已知的聚落就有一千多處），本站沒有
那種面圖層——所以取的是各自**最早的、位置指得出來的核心遺址**，座標逐筆取自維基百科
條目所載的度分秒（聖羅倫索在中文維基**沒有條目**，取英文條目）。

`properties.zoom` 全部是實測 `m.getBounds()` 調出來的（畫布 1920×873），而且每一筆的
取景條件都是**那張卡自己的論證需要**：

| | zoom | 取景條件（實測） |
|---|---|---|
| 孟菲斯 | 7.4 | 上埃及的長河谷與下埃及的三角洲要同時在畫面上（lat 28.26–31.41，尼羅河與羅塞塔、杜姆亞特兩條分流都畫得出來；開羅在「世界重要城市」、吉薩與亞歷山卓在「世界人口分布」） |
| 烏魯克 | 6.6 | 要讓**札格洛斯山前的聚合型邊界**進畫面（`plate-boundaries` 的 `maxzoom` 是 8，實測 6.6 時聚合 3 段、錯動 1 段）——「兩河平原是板塊擠出來的凹槽」那一段的憑據 |
| 摩亨佐-達羅 | 7.2 | 印度河與旁邊的乾燥平原一起入鏡（實測 `world-rivers-label` 標得出「印度河」） |
| 二里頭 | 8.6 | 黃河在藍點北邊 **19.6 公里**（拿 `world-rivers.geojson` 的節點實測，卡片寫「約 20 公里」就是這個數字），洛陽市（20 公里）與鄭州市都在框裡 |
| 聖羅倫索 | 8.0 | 要框得到**聖馬丁火山**（圖斯特拉火山群，18.562/−95.199）——巨石人頭像的玄武岩就是從那一帶來的。⚠️ 8.4 在 1920×873 下勉強框得到（北界 18.62），但 1512×698 的視窗北界只到 18.44 就掉出去，所以退到 8.0 |
| 卡拉爾 | 5.9 | **必須小於 6**：卡片叫讀者勾「洋流」看祕魯寒流，而那一層的 `maxzoom` 是 6（實測 5.9 畫得出「祕魯寒流」與它的標註）。同一個尺度剛好讓海岸、安地斯與南邊的利馬一起進畫面 |

#### ⚠️ `memphis` 這個 id 不能用——CLAUDE.md 那條「撞 id」警告第一次真的發生

`world-population.geojson` 已經有一個 `memphis`（美國田納西州的曼非斯，都會區 108 萬），
而 `highlightIds` 是一份**跨圖層的扁平清單**：撞了之後點埃及孟菲斯會連帶把美國那顆人口
圓點也加粗，而兩層都是圓點、又同屬世界主題（可以同時勾）。所以圖徵 id 是
**`memphis-egypt`**，內容檔檔名跟著它。⚠️ **新增任何一筆之前先跑一次全域 id 掃描**
（`public/data/geo*/**.geojson` 的 `properties.id`），不要只看同一個 collection。

#### ⚠️ `meta` 裡要出現「古文明」三個字，那不是贅字

搜尋的 haystack 是 name＋shortName＋en＋meta＋內容檔別名，**`category` 不在裡面**
（它只餵 `browse.groupBy`）——所以打「古文明」原本只搜得到圖層本身，六個遺址一個都
出不來（實測）。六筆的 `meta` 因此各自用自然的寫法帶進那三個字（「兩河古文明的起點」
「尼羅河古文明的首都」「黃河流域古文明」…）。⚠️ **比對是子字串比對**：孟菲斯初稿寫
「古埃及文明的首都」就搜不到（「古文明」不是它的子字串），這一步一定要實際打字驗一次。

#### 顏色沿用 `place` 藍，代價寫在 `notes` 裡

POINT 色票已經是 all-pairs 全過的飽和狀態，而「藍點＝地圖上一個有詳情卡的地點」是本站
記錄在案的規則（世界重要城市與編者選集那七個點圖層共用同一個 hex 就是這條規則）。⚠️ 代價是**它跟
「世界重要城市」同色，而這兩層很可能一起打開**（孟菲斯與開羅只差 23 公里、二里頭與
洛陽市只差 20 公里，那正是這一層的教學點之一）。要換成專屬色的話，**必須先跑
dataviz skill 的 `validate_palette.js`**，用 `--pairs all` 對同框的三個點色驗明暗兩模式
（地形景點藍 `#2a78d6`、火山洋紅 `#c0259c`、世界人口 `#80610d`）——不要憑感覺挑
（CLAUDE.md 硬性禁止事項第 15 條）。

⚠️ **它跟全球地震帶、火山帶、世界重要城市、世界人口分布一起搶
`MAX_ACTIVE_BY_KIND.circle` 的四個名額**：五個裡勾滿四個之後，第五個的核取方塊會是
`disabled`（實測過）。**預期行為，不要為此把上限調到 5 或 6**（理由同線圖層那一節，
三組色票的分析都建立在各幾何的同框上限上）。⚠️ 世界櫥窗那七個點圖層**不在這個數裡**
——它們宣告了 `exemptFromMaxActive`（判準見那一節），所以九層全開也不會把這裡的名額
吃掉。

#### 六張卡各有一段是「它被收進來的理由」

改稿時不要當成枝節刪掉：烏魯克是「最早的文字是帳本」、孟菲斯是
「Hut-ka-Ptah → Aigyptos → Egypt」與「金字塔是它的墓地」、摩亨佐-達羅是
「那套文字至今讀不出來」、二里頭是「洪水改道之後才有這座城」與「是不是夏還沒有定論」、
聖羅倫索是「沒有輪子與役畜怎麼搬那顆石頭」、卡拉爾是「沒有陶器沒有文字，靠魚與棉花」。
**都是同一種內容**（一個結論是怎麼被判斷出來的），跟理查特結構、百慕達三角、土渕海峽
那幾張是同一條線。

⚠️ **二里頭那張卡刻意不寫成「這裡是夏朝的首都」。** 碳十四把它放在前 1750–前 1520，
位置與年代都對得上文獻裡的夏，但遺址沒有出土能自證朝代與王名的文字（中國最早被確認的
成熟文字是三百多年後殷墟的甲骨文）。卡片把「這是推論不是證據」直接寫出來——那正是這一筆
的教學價值，改成肯定句就等於替一個未定的學術問題下結論。

⚠️ **卡拉爾那張卡承認本站自己的圖層畫錯了尺度**：柯本那一層在這一格畫的是 **BSh**
（半乾燥），但蘇沛河谷本身幾乎不下雨——那是 0.5° 網格把海岸與山區平均掉的結果
（RESOLVE 生態區在同一個點畫的就是「沙漠與乾旱地」）。**不要為了讓兩層一致而去改資料**，
卡片寫清楚解析度限制就是這一筆額外的收穫（比照 ERA5 平滑掉地形雨的既有做法）。

⚠️ **主 chunk 因此從 gzip 188.9 KB 變成 190.9 KB**——多的是**圖層的 `description` 與
`notes`**（那兩個欄位在註冊表裡、會進主 chunk），六張卡本身走
`public/data/geo-content/world-civilizations.json`（19 KB）的延遲載入分片，一個 byte
都不佔。

### ⚠️ 「國界」那個 planned 圖層 2026-08 拿掉了

原本掛在「國界與大洲」分組下，`status: "planned"`。拿掉的理由不是「還沒做」，而是
**兩種建議底圖本身就畫著國界**（OpenFreeMap Liberty 與 Positron 都有 `boundary` 圖層），
再疊一條自己的國界線只是把同一條線畫兩次，而且會多吃一個線圖層名額（見下面「線圖層
變成七個」）。分組因此改名成「**大洲**」。

⚠️ **不要把它加回來。** 「政治疆界 vs 自然地理界線」那個教學點由「大洲分區」的洲界
負責——烏拉山、土耳其海峽、蘇伊士、巴拿馬那幾條**才是底圖畫不出來的線**。


### ⚠️ 「全球地理形貌」在 2026-08 併進「世界地理」

兩者講的是同一張世界地圖的不同層次——「為什麼這一帶是沙漠」與「這一帶有哪些城市」
本來就該疊在一起看。**全球尺度的圖層排在前面**（它們是骨架），世界地理原本的圖層
接在後面；`groups` 的順序同理。檔案上是 `themes/global.ts` 改名成 `themes/world.ts`
（舊的 world.ts 併進去），`THEMES` 從三個變兩個。

⚠️ **舊網址 `/theme/global` 由 `App.tsx` 重導到 `/theme/world`，那條路由不要拿掉。**
它必須排在 `/theme/:themeId` **前面**，否則會被萬用路由吃掉——而 `ThemeMapPage`
對不認得的 themeId 是重導到**臺灣主題**，等於安靜地跑錯地方。

⚠️ **相機沿用原本全球地理形貌的 `[0, 10] / zoom 1.8`，不是舊世界地理的
`[30, 20] / zoom 2`。** 火山帶的半徑與不透明度、全球地震帶的半徑下限、生物群系的
外框、行星風系的標註間隔**全都是對著那個視角實測調出來的**（見下面各節與驗證清單
第 31–35 項）。換掉它等於讓那一整批實測值失效。

#### ⚠️ 合併帶來一組新的同框色，其中一對是真的撞在一起的

合併之前 `ThemeMapPage` 只算繪當前主題，所以「世界地理」的圖層與「全球地理形貌」的
圖層不可能同框。現在可以了，於是多出三組要驗的配對：

| 配對 | 幾何 | 一般視覺 ΔE | 處置 |
|---|---|---|---|
| 世界城市藍 `#2a78d6` ↔ 火山洋紅 `#c0259c` | circle | 26.0（CVD 11.3） | 重驗**兩模式五項全數 PASS**，不必動 |
| 寒流藍 `#133eec` ↔ 水系藍 `#2a78d6` | line | 14.0（CVD 9.6） | 豁免：**洋流全在海上、世界主要河流全在陸上**，一個像素都不會疊 |
| **聚合藍 `#2f74c9` ↔ 水系藍 `#2a78d6`** | line | **2.1** | ⚠️ 同一個顏色，而且真的會疊 → **板塊邊界改畫虛線** |

最後那一對不能靠文件帶過（ΔE 2.1 不是「相近」，是同一個顏色），而喜馬拉雅、安地斯、
阿爾卑斯、札格洛斯這些聚合帶正好是大河的源頭。**換色救不了，這是掃出來的**：水系藍
動不得（它同時是臺灣主題那組已驗證六色的一員，也是點色票的 `place`），而在固定
張裂橘＋錯動綠＋水系藍＋火山洋紅之後掃遍整個 OKLCH 色域，能替代聚合藍的候選不是
離洋流更近（h 277–285 的藍紫，對寒流藍掉到 9.5），就是淡到畫在藍色海面上看不見
（h 200／L 0.82）。所以改用本站既有的第二通道——**線型**（比照交通軸線的軌道虛線／
公路實線）：板塊邊界 `[4, 1.5]` 虛線、河流維持實線。實線段刻意偏長，邊界要讀得出
「一條連續的帶」，不能碎成點。

⚠️ **要把板塊邊界改回實線，就得先解掉那個 ΔE 2.1。**

#### ⚠️ 合併之後線圖層變成七個，而上限仍然是 3

合併前「全球地理形貌」有五個 ready 的線圖層、「世界地理」只有一個；再加上 2026-08
補上線的世界主要山脈，現在**七個**擠在同一個主題裡搶 `MAX_ACTIVE_BY_KIND.line` 的三個
名額（緯度參考線、國際換日線、行星風系、洋流、板塊邊界、世界主要河流、世界主要山脈）。
**這是預期行為，不是 bug**——實測「緯度參考線（`defaultOn`）＋板塊邊界＋世界主要河流」
勾滿之後，洋流的核取方塊就是 `disabled`。

⚠️ **不要為了「東西變多了」就把上限調到 4。** 那個 3 是三組色票策略的執行面，而且
上面整段色彩分析都是在「最多三條線同時出現」的前提下做的——調高等於讓那份分析失效。
要一次看更多層，正確的做法是取消不需要的那一層（緯度參考線是最常被讓出來的一個）。

---

## 已知資料限制（世界）

> 對應 `CLAUDE.md` 的「已知資料限制」。跨主題的幾條（feature 順序即清單順序、
> 相鄰面的次像素縫隙）留在那邊，臺灣的在 `CLAUDE_TW.md`。

- **Natural Earth 的河流沒有中文名欄位**，中文名靠 `build-geodata.mjs` 裡的 `RIVER_NAMES_ZH` 對照表。對不到就沿用原名。注意 NE 把黃河的 name 寫成 `"Huang"`（不是 `"Huang He"`）。

- **國際換日線（`date-line`）不是 180° 經線，不要自己畫一條直線交差。** 它為了不讓同一個
  國家跨在兩個日期上，繞開了楚科奇、阿留申群島、吉里巴斯與薩摩亞——那個折線形狀正是這
  一層唯一要教的東西（吉里巴斯 1995 年把萊恩群島改到線的西側，才有現在往東凸到西經 150°
  的那一大塊）。幾何取自 Natural Earth 的 `ne_10m_geographic_lines`（`featurecla` 就是
  `Date line`），是製圖界的標準畫法，所以這一層**不標 `schematic`**；但「沒有國際條約規定
  它在哪」仍然要講，寫在註冊表的 `notes` 裡。

  ⚠️ **上游把它切成 5 段是必要的，不是資料髒。** 折線橫跨 ±180，接成一段的話某兩個相鄰
  節點會從 179.99 跳到 -179.99，maplibre 會照著畫一條**繞過整個地球**的橫線，而且不會有
  任何錯誤訊息。`build-geodata.mjs` 的 transform 因此逐段檢查經度跨距 < 180°。

  ⚠️ **沿線標註的 `spacing` 不可以照抄緯度參考線的 320。** 那是給九條各自很長的橫線用的；
  換日線被切成 5 段、單段在畫面上經常短於 320px——實測主題預設視角（zoom 1.8）用 320 是
  **0 個標註**、240 是 1 個，200 才穩定拿到 2 個。標註是垂直排列的（maplibre 對 CJK 的
  `line` 放置會自動直排，字是正的不是側躺），跟紙本地圖的習慣一致。

  ⚠️ **搜「國際換日線」只會勾起圖層、相機不會動**，那是 `kind: "layer"` 搜尋結果的**既有
  行為**（`ThemeMapPage` 的 pendingHit effect 在 `!pendingHit.featureId` 就 return，
  fitBounds 那一段永遠走不到），不是這一層特有的——實測搜「全球地震帶」也一樣。要修的話
  是那條守衛，不是這個圖層。

---

## 驗證檢查清單（世界地理主題，第 28–40 項、第 44–47 項與第 52 項）

> 對應 `CLAUDE.md` 的「驗證方式 → 檢查清單」。**編號刻意接續、沒有重排**：
> 第 1–14 項（等高線、比較頁、版面、切底圖、沿線標註、搜尋、選取強調、附屬圖層）
> 與第 43 項（地理要素說明的延遲載入）還在 CLAUDE.md，第 15–27 項與第 41–42 項
> 在 `CLAUDE_TW.md`；本檔案收的是第 28–40 項，以及 2026-08 補內容時加的第 44–47 項與第 52 項
> （第 43 項——地理要素說明的延遲載入——留在 CLAUDE.md，那一項是跨主題的）。
>
> ⚠️ 臺灣那一份也有第 31–33 項（專屬經濟海域、海峽中線、北回歸線），跟這裡的
> 第 31–33 項（火山帶、森林與沙漠帶、柯本氣候分區）**編號撞號**，那是兩份文件
> 各自接續切出來的結果，引用時要連主題一起講。
>
> 開伺服器的規矩、瀏覽器自動化的前景分頁陷阱、地圖除錯掛勾也都在 CLAUDE.md。

28. **世界底圖的地名語系**（`/theme/world`，底圖維持「世界地圖」，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [10, 25], zoom: 2.6 });
    // 每個「用地名當文字」的 symbol 圖層都要被改寫，ref 與門牌不可以被動到
    const sym = m.getStyle().layers.filter(l => l.type === 'symbol');
    sym.filter(l => !(Array.isArray(l.layout?.['text-field']) && l.layout['text-field'][0] === 'let'))
       .map(l => l.id + '=' + JSON.stringify(l.layout?.['text-field']))
    // 期望只剩公路盾牌（["to-string",["get","ref"]]）、單向箭頭與本站的 contour-labels
    ```
    - ⚠️ **這一項只能用眼睛驗，`queryRenderedFeatures` 是看不出來的**——它回的是圖徵的
      原始 properties，不是算繪出來的字串。截圖看歐洲與北非那一帶：德國、法國、義大利、
      西班牙、土耳其、埃及、沙烏地阿拉伯全部要是**完整的**繁體中文
    - ⚠️ **回歸判準是「最後一個字有沒有被吃掉」**（`index-of` 參數順序寫反的症狀，
      見上）。德國變「德」、埃及變「埃」時 console 一個字都不會印
    - **多值名稱要只剩第一段**：阿拉伯聯合大公國不可以畫成「阿拉伯聯合大公國;阿拉伯聯合酋長國」
    - **`ZH_HANT_OVERRIDES` 要生效**：同一個視角要看得到剛果民主共和國、中非共和國、
      迦納、幾內亞、幾內亞灣、瓜地馬拉、吉爾吉斯、奧地利（而不是刚果民主共和国、加纳、
      几内亚湾、危地馬拉……）
    - **備援底圖也要localize**：把 `BASEMAP_STYLES.liberty` 暫時改成一個會 404 的網址、
      重載，`m.getStyle().sources` 應該有 `carto`，而地名一樣是繁體中文。驗完記得改回來
    - **切到 NLSC 再切回世界地圖**，改寫必須還在（`setStyle` 會清光樣式）
    - ⚠️ **一定要在 production build 驗一次**（`npm run build:debug` + `npm run preview`）：
      這一項動到的是**向量底圖**的載入路徑，正好是檢查清單第 7 項那個「worker 檔案沒被
      複製就整片空白、而且零錯誤訊息」的地方

29. **國際換日線**（`/theme/world` → 參考線，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [-175, 15], zoom: 2.6 });
    m.queryRenderedFeatures({ layers: ['date-line-line'] }).length          // 5（一段一筆）
    m.queryRenderedFeatures({ layers: ['date-line-label'] }).length         // 2
    m.getPaintProperty('date-line-line', 'line-dasharray')                  // [3,3]，跟緯度參考線同一組樣式
    ```
    - ⚠️ **形狀是這一層的全部**：那條線必須在吉里巴斯處往東凸到西經 150°、在薩摩亞附近往回折，
      不可以是一條直的 180° 經線。**只數圖徵是驗不到的**，一定要看畫面
    - ⚠️ **不可以出現一條橫貫整個地球的橫線**（某一段跨過 ±180 的症狀，建置期已擋，但改動
      `tolerance` 或上游改版後要再看一次）
    - 標註是**直排的正體字**（maplibre 對 CJK 的 line 放置會自動直排），不是側躺的
    - 圖例要有兩列（緯度參考線、國際換日線），色塊同色是刻意的——`reference` 是非分類的固定角色
    - 切底圖之後重驗存在、顏色、虛線與排序（在 `contour-lines` 之上、`contour-labels` 之下）
    - ⚠️ 搜「國際換日線」會勾起圖層但**相機不動**，那是 `kind: "layer"` 結果的既有行為（見上）

30. **板塊與板塊邊界**（`/theme/world` → 地體構造，兩層一起勾，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [-40, 10], zoom: 2.2 });
    new Set(m.queryRenderedFeatures({layers:['plates-fill']}).map(f=>f.properties.id)).size  // 23
    m.queryRenderedFeatures({ layers: ['plates-label'] }).length                              // 20
    ['divergent','convergent','transform'].map(k =>
      m.getPaintProperty(`plate-boundaries-${k}-line`, 'line-color'))
    // 期望 ["#c95c1c","#2f74c9","#159c6b"]，**不可以隨勾選順序改變**
    ```
    - ⚠️ **這一層的正確性只能用眼睛驗**：大西洋中脊必須是橘（張裂）與綠（錯動）交錯的
      階梯狀、祕魯－智利海溝與日本海溝必須是藍（聚合）。數字全對但顏色配錯是看得出來的
    - **顏色綁在邊界種類上，不是勾選順序**（回歸判準，比照古蹟三級與交通軸線）：
      先勾「錯動型」再勾「張裂型」，張裂仍然必須是橘色；圖例色塊要跟地圖一致
    - **勾母圖層時三種要自動全勾**（`items.defaultAll`）。⚠️ 但**只在還沒選過時才補**
      ——搜「錯動型邊界」只打開那一種之後，再去勾母圖層不會被打回全開，那是刻意的
    - **點板塊要開得了卡**（`fill-opacity` 是 0，但 maplibre 的 hit test 不看不透明度）：
      點菲律賓海板塊 → 標題「菲律賓海板塊／Philippine Sea Plate」、副標「全球唯一
      幾乎整圈都被隱沒帶包住的板塊，也是造出臺灣的那一塊」，底下四格數據與五條
      `facts`。⚠️ **52 塊每一塊都要是內容檔那一版，不能退回 fallback**——退回時標題
      一樣對，但副標會變成 `meta`（「次要板塊・544 萬 km²」）、內文變成整片圖層說明，
      而且 52 張卡逐字相同。抽查幾塊微板塊（馬努斯、復活節島、富圖納）比只看主要
      板塊有用。資料來源那一行**必須有三個連結**（維基百科條目、Bird、Nordpil）。
      **選取時整塊面要浮出 0.38 的暖褐**：
      ```js
      JSON.stringify(m.getPaintProperty('plates-fill','fill-opacity'))
      // ["case",["in",["get","id"],["literal",["plate-ps"]]],0.38,0]
      ```
    - 抽屜清單依分類分三組：`["主要板塊:8","次要板塊:14","微板塊:30"]`，組內依面積由大
      到小（開頭是太平洋板塊 1.05 億 km²）。⚠️ **主要是 8 塊不是 7 塊**，見上
    - **三種邊界各要開得了說明卡，而且兩條路徑開出同一張**（內容檔在
      `src/content/geo/plate-boundaries/`，三個 id 必須一致，見上）：
      ```js
      // ① 點抽屜裡的子項目名稱
      [...document.querySelectorAll('.layer-drawer .layer-row')]
        .find(r => r.textContent.startsWith('板塊邊界'))
        .querySelector('.species-name-btn').click();   // 「張裂型邊界」
      // ② 點地圖上的線（先用 queryRenderedFeatures 找一個只命中該層的像素再派事件）
      document.querySelector('.map-detail-panel').innerText.split('\n')[0]  // 兩者都是該種邊界的名字
      ```
      ⚠️ 卡片開了不算數，要看**內文**：退回 `FeatureCard` fallback 時標題一樣對，
      但內容會變成圖層說明（代表內容檔的 id 對不上）
    - 選取時該種邊界要加粗，而且**清單裡是不帶前綴的 id**：
      ```js
      JSON.stringify(m.getPaintProperty('plate-boundaries-divergent-line','line-width'))
      // ["case",["in",["get","id"],["literal",["divergent"]]],["*",1.6,2.2],1.6]
      ```
    - 搜「太平洋板塊」「Pacific」都要找得到；搜「錯動」要出現「錯動型邊界」子項目
    - **三種邊界都是虛線**，而且**跟「世界主要河流」一起打開時要分得出來**（見上，
      那是這一層畫虛線的唯一理由）：
      ```js
      m.getPaintProperty('plate-boundaries-convergent-line','line-dasharray')  // [4, 1.5]
      ```
      ⚠️ 用眼睛看喜馬拉雅一帶（`jumpTo([85, 28], 4)`）：藍色實線＋沿線河名是恆河與
      布拉馬普特拉河，藍色虛線是聚合型邊界。**兩條都畫成實線就是回歸**
    - 資料來源那一行**必須有三個連結**（Bird、Nordpil、維基百科）——ODC-BY 要求標示出處，
      少一個就違反授權
    - ⚠️ **不可以出現一條橫貫整個地球的橫線**（某一段跨過 ±180 的症狀，建置期已擋）
    - 切底圖之後重驗存在、顏色與排序（面 < 線 < 標註，全部在 `contour-lines` 之上、
      `contour-labels` 之下）

31. **火山帶**（`/theme/world` → 地體構造，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.getPaintProperty('volcanoes-points', 'circle-color')                       // "#c0259c"
    new Set(m.queryRenderedFeatures({layers:['volcanoes-points']}).map(f=>f.properties.id)).size
    // 主題預設視角（center [0,10]、zoom 1.8、1512×772）為 949
    ```
    - ⚠️ **這一層最重要的一項只能用眼睛驗，而且必須在「勾起來的第一眼」看**：不要先
      `jumpTo` 到環太平洋。主題預設視角的正中央是非洲與大西洋，全球地震帶就是在這個
      視角上「數得到 1,829 筆但畫面上什麼都看不到」的。要看得出安地斯、中美洲、印尼、
      日本、地中海與東非大裂谷各條火山帶
    - **跟「板塊邊界」「全球地震帶」疊起來**（`jumpTo([135,20], 3)`）：洋紅的火山點在
      島弧上、灰色震央偏海溝側、藍線沿海溝——三者分得開才算過。⚠️ 洋紅↔橘（張裂型）
      是這一層唯一的色彩風險，冰島與東非大裂谷那一帶要特別看
    - 點任一座火山要開得了卡，而且卡上**沒有圖層說明**（`hideLayerDescription`）：
      ```js
      // 富士山 → 標題「富士山」、副標「Fujisan・層狀火山・海拔 3,776 公尺」、
      //          下一行「最後噴發 1708 年・西北太平洋火山區」
      ```
    - 臺灣的兩座要在（`龜山島`、`大屯火山群`），中文名不可以退回英文
    - **搜「火山」找得到圖層本身，搜「富士山」是空的**——那是刻意的（沒有 `browse`
      就不進索引，見上）。⚠️ 順帶確認聚焦搜尋框**不會**抓 `volcanoes.geojson`
    - 切底圖之後重驗存在、顏色與排序（在 `contour-lines` 之上、`contour-labels` 之下，
      而且在板塊邊界的線之上）

32. **森林與沙漠帶**（`/theme/world` → 氣候與生物群系，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.getStyle().layers.map(l => l.id).filter(i => /^biomes-.*-fill$/.test(i)).length   // 6（defaultAll）
    ['tropical-forest','savanna','desert','temperate-forest','boreal','tundra']
      .map(k => m.getPaintProperty(`biomes-${k}-fill`, 'fill-color'))
    // 期望 ["#25744c","#989401","#b4410d","#369db8","#695ba9","#c754f4"]，**不隨勾選順序改變**
    m.getPaintProperty('biomes-desert-outline', 'line-opacity')                         // 0.15（補縫用，見上）
    ```
    - ⚠️ **這一層的成敗只能用眼睛驗**，而且要在**主題預設視角**（zoom 1.8）看：
      撒哈拉與阿拉伯要連成一條壓在北回歸線上的橘色帶、薩赫爾是橄欖綠、剛果與亞馬遜是
      深綠、歐亞中緯度是藍綠、加拿大與西伯利亞是靛紫、青藏高原與安地斯高地是洋紅
    - ⚠️ **檢查有沒有白縫**：`jumpTo([10,20], 3.6)` 看撒哈拉／薩赫爾交界。露出一條條
      白色細縫代表外框的補縫失效（`outlineOpacity` 或 `outlineWidth` 被改掉了）
    - ⚠️ 反過來也要看：全球視角下**不可以**出現一張細線網（那是外框太實，見上）
    - 點任一塊面要開得了卡（六類各有內容檔）：點撒哈拉 → 標題「沙漠與乾旱地」、
      副標「南北緯 30° 附近那兩條——副熱帶高壓底下不下雨」；**選取時整類一起加深**
      （`fill-opacity` 0.25 → 0.38，全球的沙漠同時變深才是對的）
    - 六類的核取方塊可以各自取消，取消後不會重抓（`resolveLayerData` 有快取）
    - 搜「沙漠」要出現「沙漠與乾旱地」子項目，選它只勾那一類（不是六類全開）
    - `maxzoom: 5`：`jumpTo` 到 zoom 5.5 整層應該消失，這是刻意的（見上）
    - 資料來源那一行**必須有兩個連結**（RESOLVE、Esri Living Atlas）——CC-BY 要求標示出處
    - 切底圖之後重驗存在、顏色、外框不透明度與排序（面在 `contour-lines` 之上、
      `contour-labels` 之下）

33. **柯本氣候分區**（`/theme/world` → 氣候與生物群系，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    ['a','b','c','d','e'].map(k => m.getPaintProperty(`koppen-zones-${k}-fill`, 'fill-color'))
    // 期望 ["#1d9dc8","#d8783d","#147811","#ba229e","#5e42fe"]，**不隨勾選順序改變**
    // 亞型數：A 4／B 4／C 9／D 11／E 2（合計 30，一個都不能少）
    ['a','b','c','d','e'].map(k =>
      new Set(m.queryRenderedFeatures({layers:[`koppen-zones-${k}-fill`]}).map(f=>f.properties.id)).size)
    ```
    - ⚠️ **這一層最快的正確性檢查是拿幾個城市對答案**（`queryRenderedFeatures` 在
      `m.project([lng,lat])` 上查）：臺北 `Cfa`、東京 `Cfa`、倫敦 `Cfb`、開羅 `BWh`、
      西伯利亞（100°E,62°N）`Dfc`。⚠️ **高雄會是 `Am` 而不是本站地點卡的 `Aw`**，
      那是 0.5° 網格的已知落差，不是壞掉（見上）
    - 點任一塊要開出**亞型**的卡：標題「Cfa 溫暖濕潤氣候」、副標「溫帶・全年有雨・
      最暖月 ≥22 °C」、下一行「代表地點：…」。⚠️ 卡上**不該有圖層說明**
      （`hideLayerDescription`）
    - **搜「Cfa」要找得到「C 溫帶氣候」**（靠 `items[].keywords`，這一層沒有開
      `indexFeatures`）。⚠️ 順帶確認聚焦搜尋框**不會**抓 `koppen-zones-*.geojson`
    - ⚠️ **不可以出現白縫**：這一層的相鄰類別邊界是逐位元相同的格線，看得到縫代表
      建置期的 `tolerance` 被改成大於 0 了（見上）
    - 南極要是一塊完整的冰帽（EF 加一圈 ET），**不可以變成橫貫地圖的一條帶**
    - 全球視角下五類的分布要對得上課本的柯本圖：撒哈拉／阿拉伯／澳洲內陸橙、
      亞馬遜／剛果／東南亞青、歐洲與美東綠、俄羅斯與加拿大紫紅、北極與南極藍紫
    - `maxzoom: 5`：超過就整層消失（0.5° 網格再放大只是一格一格的階梯）
    - 切底圖之後重驗存在、顏色與排序（在 `contour-lines` 之上、`contour-labels` 之下）

34. **行星風系**（`/theme/world` → 氣候與生物群系，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    const parts = ['pressure-belts','trades','westerlies','polar-easterlies'];
    parts.map(i => m.getPaintProperty(`wind-belts-${i}-line`, 'line-color'))  // 四個都是 "#5a6f96"
    m.getPaintProperty('wind-belts-pressure-belts-line', 'line-dasharray')    // [1,2.5]（只有氣壓帶是點線）
    // ⚠️ 標註數要在**縮到 zoom 1.2** 之後數，主題預設視角看不到極地東風（見上）
    m.jumpTo({ center: [0, 0], zoom: 1.2 });
    parts.map(i => m.queryRenderedFeatures({layers:[`wind-belts-${i}-label`]}).length)
    // 1512×772 實測 [16, 8, 7, 6]；預設視角（zoom 1.8）是 [6, 4, 4, 0]
    ```
    - ⚠️ **這一層的成敗在箭頭的方向，只能用眼睛驗**：信風指向西南（北半球）與西北
      （南半球）、西風指向東北與東南、極地東風指向西南與西北。**方向畫反是最容易
      發生又最不會報錯的錯**，而它正好是這一層唯一要教的東西
    - ⚠️ 六條帶的箭頭在畫面上要**看起來一樣斜**（斜度依緯度校正過）。極地東風變成
      接近垂直代表 `ARROW_SCREEN_SLOPE_DEG` 那段校正被改掉了
    - 氣壓帶是**間斷的短線段**而不是橫貫全圖的長線（否則會跟緯度參考線疊成一條），
      而且三條各自標著「赤道低壓帶」「副熱帶高壓帶」「副極地低壓帶」
    - 點箭頭或點氣壓帶要開得了卡（六筆圖徵各有內容檔）：點信風 → 標題「信風」、
      副標「從副熱帶高壓吹回赤道的風…」；點 30° 那條 → 「副熱帶高壓帶」
    - 圖例要有四列、色塊同色，而且「氣壓帶」那一列帶著 `（示意）` 標記（schematic）
    - 搜「信風」「馬緯度」「咆哮」都要找得到（分別命中信風、氣壓帶、西風——靠
      `items[].keywords`，這一層沒有可點清單）
    - ⚠️ **不可以出現橫貫整個地球的線**（箭頭跨過 ±180 的症狀）
    - `maxzoom: 4`：再放大一支箭頭會比畫面還寬，整層應該消失
    - 切底圖之後重驗顏色、虛線與排序（在 `contour-lines` 之上、`contour-labels` 之下）

35. **洋流（暖流／寒流）**（`/theme/world` → 海洋，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    [m.getPaintProperty('ocean-currents-warm-line','line-color'),
     m.getPaintProperty('ocean-currents-cold-line','line-color')]   // ["#b00a1d","#133eec"]
    // 主題預設視角（center [0,10]、zoom 1.8）：18 條**全部**畫得出來，而且全部標得出名字
    const u = l => new Set(m.queryRenderedFeatures({layers:[l]}).map(f=>f.properties.id)).size;
    [u('ocean-currents-warm-line'), u('ocean-currents-cold-line')]  // [10, 8]
    m.queryRenderedFeatures({layers:['ocean-currents-warm-label','ocean-currents-cold-label']}).length
    // 1800×953 實測 20（長線會拿到兩個標註）
    ```
    - ⚠️ **這一層的成敗在箭頭的方向，只能用眼睛驗**：北半球的環流順時針（灣流北上、
      加利福尼亞寒流南下）、南半球逆時針（巴西暖流南下、本格拉寒流北上）。
      **方向畫反是最容易發生又最不會報錯的錯**，而它正好是這一層要教的東西
    - ⚠️ **不可以出現橫貫整個地球的橫線**（某一段跨過 ±180 的症狀）。北太平洋暖流、
      兩條赤道暖流與西風漂流一定會跨換日線，要看它們在畫面左右兩側**各接上一半**，
      而且線真的畫到圖幅邊緣、不是提前幾度就停住
    - ⚠️ **經度寫錯方向的回歸判準**：往西流的洋流（兩條赤道暖流）如果被寫成 wrap 過的
      經度，會產出一堆碎線＋一條 44° 的橫線。建置期沒有東西擋，只能看畫面
    - 點任一條要開得了卡（18 條都有內容檔）：點黑潮 → 標題「黑潮 Kuroshio Current」、
      副標「從菲律賓東方北上…」，底下有「這是簡化的教學示意幾何」的警語
    - **搜「黑潮」「祕魯寒流」要直接命中那一條洋流**（不是只命中「暖流」子項目），
      選了會 `fitBounds` 過去並開卡。⚠️ 順帶確認聚焦搜尋框**不會**因此多抓任何檔案
      ——這一層是程式產生的，`performance.getEntriesByType('resource')` 裡不該有
      任何 `ocean-currents` 的項目
    - 顏色**綁在暖／寒上，不隨勾選順序改變**：先勾寒流再勾暖流，暖流仍然必須是紅的
    - ⚠️ **跟「板塊邊界」一起打開時的已知衝突要親眼確認一次**（`jumpTo([-78,-25], 3)`）：
      藍色的祕魯寒流會與藍色的聚合型邊界沿祕魯－智利海溝平行並排。**那是已知且記錄
      在案的**（見上），判讀靠洋流的沿線名稱；發現連名字都讀不出來才是回歸
    - `maxzoom: 6`：再放大整層應該消失
    - 切底圖之後重驗存在、顏色與排序（在 `contour-lines` 之上、`contour-labels` 之下）

36. **大洲分區**（`/theme/world` → 國界與大洲，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [0, 10], zoom: 1.9 });
    new Set(m.queryRenderedFeatures({layers:['world-continents-fill']}).map(f=>f.properties.id)).size  // 7
    m.queryRenderedFeatures({ layers: ['world-continent-labels-label'] }).map(f=>f.properties.name)
    // 1920×873 實測 6 個（南極洲在這個視角的畫面外）；**每一洲最多一個**
    m.getPaintProperty('world-continents-outline', 'line-color')   // "#a05a80"
    ```
    - ⚠️ **最重要的一項是「洲名有沒有重複」**，而且只有數標註或看畫面才抓得到：把
      洲名改回掛在面上的話，實測全球視角會出現六十幾個「亞洲」「北美洲」散在各個島上
      （maplibre 逐塊逐圖磚算錨點，見上）。`queryRenderedFeatures` 數出來每一洲 >1 就是回歸
    - ⚠️ **不可以出現橫貫大陸的直線**（分界線被套用到不該切的國家的症狀，見上）。看
      `jumpTo([40,35], 2.6)`：烏拉山（東經 60°）、烏拉河、土耳其海峽、蘇伊士各有一條
      短短的洲界，**沒有**任何一條線延伸到別的大陸去
    - 點南美洲內陸與點「大洋洲」那四個字，兩者都要開卡，而且開的是**那一洲自己的卡**
      （洲名是附屬圖層，id 與母圖徵相同）
    - 選取時整洲浮出 0.38 的面染：
      ```js
      JSON.stringify(m.getPaintProperty('world-continents-fill','fill-opacity'))
      // ["case",["in",["get","id"],["literal",["africa"]]],0.38,0]
      ```
      ⚠️ 順便用眼睛確認**西奈半島沒有跟著非洲一起變深**——那是蘇伊士那條洲界有沒有
      切對的唯一證據
    - 抽屜清單是 7 列、依面積由大到小（亞、非、北美、南美、南極、歐、大洋），副標是
      面積與占陸地比例
    - 搜「大洋洲」「Asia」「南極」都要找得到；⚠️ 這一層有 `browse`，所以聚焦搜尋框
      **會**多抓 `world-continents.geojson`（326 KB），那是已知成本
    - 南極洲要是一整片完整的陸地（`jumpTo([20,-60], 1.6)`），**不可以有一條橫貫地圖的
      橫線**——它的環沿著南緯 90° 繞回起點，建置期的檢查對那條邊是刻意放行的
    - `maxzoom: 5`（面）與 `maxzoom: 4`（洲名）：zoom 4.2 應該只剩面、zoom 5.2 整層消失
    - 切底圖之後重驗存在、顏色與排序（面 < 線 < 洲名，全部在 `contour-lines` 之上、
      `contour-labels` 之下）

37. **世界主要山脈**（`/theme/world` → 地形水系，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [0, 10], zoom: 1.8 });
    m.getPaintProperty('world-mountains-line', 'line-color')   // "#8e26ff"（**不是** relief 的 #c23f8f）
    new Set(m.queryRenderedFeatures({layers:['world-mountains-line']}).map(f=>f.properties.id)).size
    // 主題預設視角、1920×873 實測 38（橫貫南極山脈在畫面外）
    m.queryRenderedFeatures({ layers: ['world-mountains-label'] }).length          // 12
    m.queryRenderedFeatures({ layers: ['world-mountain-peaks-points'] }).length    // 38
    ```
    - ⚠️ **這一層的成敗只能用眼睛驗**：中軸線要真的落在山脈上。看安地斯（沿南美西緣
      一路到火地島）、洛磯與海岸山脈（平行的兩條）、喜馬拉雅（沿青藏高原南緣的弧）、
      大分水嶺（澳洲東岸）。**一條穿過平原或海面的直線就是回歸**（多半代表
      `polygonAxis()` 的平滑或連通分量過濾被改壞了）
    - ⚠️ **跟「火山帶」一起打開**（`jumpTo([-70,-30], 4)`）：紫色的線是安地斯山脈、
      洋紅的點是火山。**兩者分不出來就是回歸**——那正是這一層不能沿用 `relief` 洋紅的
      唯一理由（見上）。冰島與東非大裂谷那一帶也要看一次
    - **跟「板塊邊界」一起打開**（`jumpTo([84,31], 4)`）：藍色**虛線**是聚合型邊界、
      紫色**實線**是喜馬拉雅山脈，兩條平行貼著
    - 點地圖上的線要開得了卡，而且要是**內容檔**那一版（不是 fallback）：標題下面有
      四格數據（最高峰／主要國家／長度或走向），底下是 `facts`、示意警語與三個來源
      連結。⚠️ 只看標題對不對是分不出來的——退回 fallback 時標題一樣對，但四格數據
      會消失、內文變成 geojson 的 `meta` 與 `detail` 那兩行
    - **點最高峰開的是山峰自己的卡，而且卡上不可以有示意警語**（`attach.schematic: false`，
      見上）：
      ```js
      // 點抽屜裡的「聖母峰」→ 卡片是「聖母峰／Mount Everest／喜馬拉雅山脈主峰・8,848 公尺」
      document.querySelector('.map-detail-panel').innerText.includes('教學示意')   // false
      ```
    - **母子雙向連動強調**，清單一定是兩筆（比照五大山脈 → 主峰）：
      ```js
      JSON.stringify(m.getPaintProperty('world-mountains-line','line-width'))
      // ["case",["in",["get","id"],["literal",["himalaya","himalaya-peak"]]],["*",2.6,2.2],2.6]
      ```
    - ⚠️ **點最高峰之後山脈必須還在畫面上**（`attach.browse.zoom` 是 5.5、母圖層
      `maxzoom` 是 6，見上）。只看卡片有沒有開是抓不到的：
      ```js
      // 點「聖母峰」之後
      m.getZoom()                                                              // 5.5
      m.queryRenderedFeatures({ layers: ['world-mountains-line'] }).length > 0  // true
      ```
    - 抽屜清單是 78 列（39 條山脈各帶一座巢狀的最高峰）、依洲分七組
      （亞、歐、非、北美、南美、大洋、南極），組內依主峰高度由高到低，開頭是喜馬拉雅
    - 搜「安地斯」「Andes」「聖母峰」「庫克山」都要找得到；⚠️ **原名也要**：
      搜「K2」「Denali」「Aoraki」各要命中一座最高峰——那是附屬圖徵的 haystack
      有沒有收 `en` 的唯一證據（見「搜尋索引」那節，這個洞真的漏過）。
      ⚠️ 兩層都有 `browse`，所以聚焦搜尋框**會**多抓 40 KB（30 + 10），那是已知成本
    - `maxzoom: 6`：zoom 6.2 整層應該消失（中軸線再放大就是假精確）
    - 切底圖之後重驗存在、顏色與排序（線 < 最高峰 < 沿線標註，全部在 `contour-lines`
      之上、`contour-labels` 之下）

38. **世界櫥窗：地表之最**（⚠️ **這兩層 2026-08 起已下架，整項暫時跳過**；下面留著是為了重新設計後照著重驗一次，見上）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [0, 10], zoom: 1.8 });
    m.getPaintProperty('world-superlative-peaks-points', 'circle-color')  // "#2a78d6"（place 藍）
    m.getPaintProperty('world-superlative-ranges-line', 'line-color')     // "#8e26ff"（mountain 紫）
    const u = l => new Set(m.queryRenderedFeatures({layers:[l]}).map(f=>f.properties.id)).size;
    [u('world-superlative-peaks-points'), u('world-superlative-ranges-line')]   // [5, 2]
    m.queryRenderedFeatures({ layers: ['world-superlative-ranges-label'] }).length  // > 0
    ```
    - ⚠️ **中洋脊的形狀就是這一層的成敗，只能用眼睛驗**：大西洋正中央要有一條 S 形的
      線從冰島一路彎到南大西洋、東太平洋隆起沿著南美西方外海往北、印度洋是一個
      倒 Y。**一條橫貫整張圖的橫線**代表某一段跨過了 ±180（建置期已擋，改動
      `mergeStepRuns` 之後要再看一次）
    - ⚠️ **東非大裂谷與貝加爾湖那裡不可以有紫線**：那是大陸裂谷（CRB），不屬於中洋脊。
      看得到就代表 `MID_OCEAN_RIDGE_CLASS` 的篩選被改成整個「張裂型」了
    - **跟「板塊邊界」一起打開**（`jumpTo([-30, 0], 3)`）：紫色**實線**是中洋脊、
      橘色**虛線**是張裂型邊界，兩條疊在一起是對的（同一件事的兩種說法），
      分不出線型才是回歸
    - **跟「世界主要山脈」一起打開**（`jumpTo([-70,-30], 4)`）：安地斯那條線必須
      **完全重合**成一條（兩層同色同寬），看得到兩條平行的紫線就代表幾何漂開了：
      ```js
      // 建置期的比對（不需要瀏覽器）：兩份產物的安地斯幾何必須逐位元相同
      // node -e "…world-superlatives-ranges 的 longest-land-range === world-mountains 的 andes"
      ```
    - 七張卡都要開得了，而且是**內容檔**那一版（標題下面有四格數據）：
      點聖母峰 →「海拔最高的山峰，8,848.86 公尺」、點日和山 →「海拔最低的山，3 公尺」、
      點普哈胡努 →「體積最大的火山，約 150,000 立方公里」、
      點奧霍斯－德爾薩拉多山 →「海拔最高的火山，6,893 公尺」、
      點尤耶亞科山 →「海拔最高、有噴發紀錄的活火山，6,739 公尺」、
      點安地斯 →「最長的陸上山脈，約 7,000 公里」、點中洋脊 →「最長的海底山脈，約 65,000 公里」
    - 抽屜清單是 5 列、依 `category` 分「山」（2）與「火山」（3）兩組
    - ⚠️ **跟「火山帶」一起打開**（`jumpTo([-68.5,-26], 7)`）：奧霍斯－德爾薩拉多山
      與尤耶亞科山的藍點必須**正好蓋在洋紅點上**（同一份 GVP 座標）。差開幾公里就
      代表座標被改成別的來源了。點下去開的要是**世界之最**那張卡，不是火山帶的
    - ⚠️ 「火山帶」那一層的 `Llullaillaco` 現在應該顯示中文名**尤耶亞科山**
      （`lib/volcanoes.mjs` 的 `NAMES_ZH` 補了 355110）
    - ⚠️ **示意警語只能出現在安地斯那一張**（`schematic` 掛在內容檔上，見上）：
      ```js
      document.querySelector('.map-detail-panel').innerText.includes('教學示意')
      // 安地斯 true；中洋脊、聖母峰、日和山都是 false
      ```
    - ⚠️ **抽屜那一列不會有「（教學示意圖，非精確界線）」**，那是刻意的（圖層層級沒標）
    - 逐筆取景寫在 `properties.zoom` 上、不是 `browse.zoom`：日和山 14、聖母峰 9、
      普哈胡努 7、兩座安地斯火山 9。日和山那一格要看得到仙台港南邊的海岸線、
      普哈胡努那一格要看得到夏威夷島鏈的走向，不是一片空白的海
    - ⚠️ **聖母峰會有兩顆藍點**（本層 + 世界主要山脈的最高峰，相距約 4 公里），
      放大到 zoom 10 以上看得出來。那是已知且記錄在案的，不是重複的資料
    - 搜「聖母峰」會同時命中本層與「世界主要山脈」的最高峰；搜「中洋脊」「日和山」
      「普哈胡努」「尤耶亞科」「Everest」「Hiyoriyama」「Llullaillaco」都要找得到。
      ⚠️ 兩層都有 `browse`，所以聚焦搜尋框**會**多抓 33 KB（31 + 2），那是已知成本
    - `maxzoom: 6`（山脈那一層）：zoom 6.2 只剩兩顆山峰、線不見了。**山峰那一層沒有
      maxzoom**（一個座標點沒有「再放大就是假精確」的問題）
    - 切底圖之後重驗存在、顏色與排序（線 < 點 < 沿線標註，全部在 `contour-lines`
      之上、`contour-labels` 之下）

39. **世界櫥窗：編者選集那九層**（`/theme/world` → 世界櫥窗，見上）。
    ⚠️ **先驗「拆成九層」本身，再逐筆驗內容**——下面每一小段的圖層 id 都換過了
    （`world-picks-points` 那個 id 已經不存在）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    const g = [...document.querySelectorAll('.layer-group')]
      .find(s => s.querySelector('.layer-group-title')?.textContent.includes('世界櫥窗'));
    const rows = [...g.querySelectorAll('.layer-row')];
    rows.length                                                                 // 9
    rows.map(r => r.querySelector('label').textContent.trim())
    // 火山與災害／地形與地質／海洋／海峽／運河／海角／湖泊／板塊與地形／傳說中的地方

    // 九個要能同時勾起來（exemptFromMaxActive），而且勾完沒有一個是 disabled
    rows.forEach(r => { const i = r.querySelector('input'); if (!i.checked) i.click(); });
    rows.every(r => r.querySelector('input').checked && !r.querySelector('input').disabled)  // true

    // 九層共用兩份幾何：全開之後 geojson 請求**只有兩次**
    performance.getEntriesByType('resource')
      .filter(r => /world-picks.*geojson/.test(r.name)).length                  // 2
    m.getPaintProperty('world-picks-volcanoes-points', 'circle-color')          // "#2a78d6"
    ```
    ⚠️ 那個「2」只有 **production build（`npm run preview`）**量得準：dev 模式會載入
    幾百個 ESM 模組，`performance` 的資源緩衝（250 筆）早就滿了，geojson 一筆都查不到
    ——症狀是「明明抓了卻查不到」，那是既有的坑，不是這一項壞了。

    - **上限仍然對其他圓點圖層生效**（豁免不是失效）：九層全開的同時，勾
      世界重要城市／世界人口分布／古文明發源地／火山帶四層之後，**全球地震帶那一個
      核取方塊必須是 `disabled`**，而且九層一個都不會被取消勾選。
    - **切分正確**（每一層只拿到自己那幾筆）：
      ```js
      const q = id => [...new Set(m.queryRenderedFeatures({ layers: [id] }).map(f => f.properties.id))];
      m.jumpTo({ center: [30, 20], zoom: 1.6 });
      q('world-picks-straits-points').length   // 7，而且不含 dead-sea
      q('world-picks-lakes-points').length     // 4（含 lake-superior）
      q('world-picks-canals-points').length    // 2
      ```
      ⚠️ **切底圖之後要再數一次**（`reapply` 會重切一份）：實測 liberty ↔ nlsc-emap
      來回四次之後仍然是 7 筆，`m._listeners.click.length` 前後相同。
    - **搜尋不會出現重複**（`searchIndex.ts` 那道 `featureIds` 過濾的回歸判準）：
      搜「貝加爾湖」只有**一筆**圖徵結果、副標是「湖泊」；搜「海峽」出得來七條海峽
      各一次外加「海峽」這個圖層本身，**沒有**同一個地方出現七次。
      ⚠️ 少了那道過濾時，錯的那幾筆點下去會勾錯圖層、飛不到、聚焦之後畫面空白，
      **而且 console 一個字都沒有**。
    - **聚焦（只顯示這一筆）的計數是「同層」的**：在「海峽」層上選一條海峽，chip 要
      寫「已隱藏同層 **6** 筆」（不是拆層前的 16）。
    ```js
    m.jumpTo({ center: [14.426, 40.821], zoom: 9 });
    m.queryRenderedFeatures({ layers: ['world-picks-volcanoes-points'] }).length          // 1
    ```
    - ⚠️ **維蘇威那張卡的內文一定要提到龐貝城**——那是這一筆被收進來的理由：
      ```js
      const t = document.querySelector('.map-detail-panel').innerText;
      [t.includes('龐貝'), t.includes('普林尼'), t.includes('教學示意')]   // [true, true, false]
      ```
      標題是「維蘇威火山」、副標是「西元 79 年埋掉龐貝城的那座火山」
    - **跟「火山帶」一起打開**：藍點必須**正好蓋在洋紅點上**（同一份 GVP 座標），
      點下去開的要是**「火山與災害」**那張卡
    - ⚠️ **它不可以出現在「世界之最・高山與火山」的清單裡**（那一層是 5 列、
      沒有維蘇威）。混回去就是回歸——兩層的判準不同，見上
    - 搜「維蘇威」「Vesuvius」要找得到，而且會**同時命中**本層與「火山帶」那一座
    - **理查特結構（撒哈拉之眼）**：
      ```js
      m.jumpTo({ center: [-11.4036, 21.1249], zoom: 9.5 });
      m.queryRenderedFeatures({ layers: ['world-picks-landforms-points'] })
        .map(f => f.properties.id)                                              // ["richat-structure"]
      ```
      - ⚠️ **這一筆的成敗只能用眼睛驗，而且要看那圈同心圓有沒有畫出來**：等高線與
        地形陰影（兩個都是預設開著的）從 DEM 把環狀山脊算出來，畫面正中央要是一個
        清楚的靶心。**空白的沙漠底圖就是回歸**（多半是 `properties.zoom` 被改掉、
        或疊圖被關掉了）
      - ⚠️ **卡片上「兩個都關掉才會消失」那句話是實測過的**：到左下「圖層」磚
        只關地形陰影，等高線畫的環還在（所以不能寫成「關掉地形陰影就消失」）；
        兩個都關掉才真的一乾二淨——向量底圖在那裡什麼都沒畫
      - 卡片的內文要留著「怎麼判斷它不是隕石坑」那一段（同上）：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('撞擊'), t.includes('IUGS'), t.includes('教學示意')]   // [true, true, false]
        ```
      - 搜「理查特」「撒哈拉之眼」「Richat」都要找得到。⚠️ **「撒哈拉之眼」只寫在
        geojson 的 `meta` 上**，所以它同時是「搜尋 haystack 有沒有收 `meta`」的回歸判準
      - ⚠️ 柯本氣候分區疊上去要落在 **BWh**（已用點在多邊形內實測），卡片上那句
        「B 乾燥氣候的 BWh」不是憑印象寫的
    - **尼莫點**：
      ```js
      m.jumpTo({ center: [-123.3933, -48.8767], zoom: 2.6 });
      m.queryRenderedFeatures({ layers: ['world-picks-ocean-points'] })
        .map(f => f.properties.id)                                              // ["point-nemo"]
      ```
      - ⚠️ **取景要看得到「離什麼都很遠」**：那個視角上紐西蘭在左、智利與阿根廷在右、
        南極圈在下緣，中間一大片空的南太平洋只有那顆藍點。**畫面上一塊陸地都沒有
        就是回歸**（`properties.zoom` 被改大了，實測 3.5 就已經全是海）
      - 卡片要留著 Bloop 那一段（同上）：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('Bloop'), t.includes('冰震'), t.includes('2,688')]     // [true, true, true]
        ```
      - **跟「洋流」＋「柯本氣候分區」一起打開**（那正是卡片的對照重點，已實測）：
        西風漂流在藍點南邊、祕魯寒流沿智利、東澳暖流在紐西蘭那側；柯本那五層
        **在整片海上一個圖徵都查不到**（`queryRenderedFeatures` 打在尼莫點上是 0，
        整個畫面 39 個全部在陸地上）
      - 搜「尼莫」「Point Nemo」要找得到
    - **馬里亞納海溝**（同一層的第二筆，2026-08 新增）：
      ```js
      m.jumpTo({ center: [142.2, 11.35], zoom: 4.6 });
      m.queryRenderedFeatures({ layers: ['world-picks-ocean-points'] })
        .map(f => f.properties.id)                    // ["point-nemo" 不在畫面上] → ["mariana-trench"]
      ```
      - ⚠️ **藍點必須落在水上**（`queryRenderedFeatures` 打 `water` 圖層是 1），而且要
        落在溝裡：打開「3D 地形」後 `queryTerrainElevation([142.2,11.35]) / m.getTerrain().exaggeration`
        約 **−10,500** 公尺（⚠️ 不除 `exaggeration`（1.4）會讀成 −14,687，那不是壞掉）
      - ⚠️ **打開「地形陰影」要看得到那條弧形的溝與西邊的島弧**——這是全站少數幾個
        「底圖畫得出海底地形」的地方（terrarium DEM 含水深），比照理查特結構那一筆
      - **取景要讓整條島弧進畫面**：實測 zoom 4.6（1920×803）視野 lat −1.23 至 23.40，
        25 座島弧火山全部在內；**5.5 只剩 13 座**（北界 17.91），弧的上半截被切掉
      - **跟「板塊邊界」＋「火山帶」一起打開**（那正是卡片的對照重點，已實測）：
        聚合型邊界那條虛線沿著海溝畫過去，島弧火山在它西邊約 200 公里處平行排開
        （25 座到邊界的最近距離中位數 214 公里，本站兩層量的）
      - 卡片要留著那串深度數字與距地心那一段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('11,034'), t.includes('10,928'), t.includes('6,352.8'), t.includes('塑膠袋')]
        // [true, true, true, true]
        ```
      - 搜「馬里亞納」「瑪麗亞娜」「海溝」「Mariana」「挑戰者深淵」五種都要找得到
    - **荷姆茲海峽**：
      ```js
      m.jumpTo({ center: [56.44, 26.685], zoom: 7.5 });
      m.queryRenderedFeatures({ layers: ['world-picks-straits-points'] })
        .map(f => f.properties.id)                                              // ["strait-of-hormuz"]
      ```
      - ⚠️ **藍點必須落在水上**（主航道中線），而且畫面上要同時看得到阿巴斯港、
        格什姆島、穆桑代姆半島與東邊的阿曼灣。點壓在陸地上就是座標被改壞了
      - 卡片要留著臺灣那一段與有時效的那一段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('61.7%'), t.includes('2,100 萬桶'), t.includes('2026 年 8 月')]  // [true, true, true]
        ```
      - ⚠️ **那個「2026 年 8 月」是刻意寫死的日期**，不是忘了更新的痕跡；要更新的話
        連同 `sources` 裡 Lloyd's List Intelligence 那一條一起換（見上）
      - 資料來源那一行要有**四個**連結（維基百科／EIA／能源署／Lloyd's List）
      - 搜「荷姆茲」「Hormuz」要找得到
    - **直布羅陀海峽**：
      ```js
      m.jumpTo({ center: [-5.50, 35.95], zoom: 9 });
      m.queryRenderedFeatures({ layers: ['world-picks-straits-points'] })
        .map(f => f.properties.id)                                              // ["strait-of-gibraltar"]
      const p = m.project([-5.50, 35.95]);
      m.queryRenderedFeatures([p.x, p.y], { layers: ['water'] }).length          // 1
      ```
      - ⚠️ 取景要同時框得住塔里法、直布羅陀、丹吉爾與休達（實測 1920×929 的畫布上
        lat 35.43–36.47、124 m/px，14.2 公里的最窄處有 115 px）
      - 卡片要留著地中海換水與墨西拿鹽度危機那兩段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('蒸發'), t.includes('墨西拿'), t.includes('海格力斯')]   // [true, true, true]
        ```
      - 搜「直布羅陀」「Gibraltar」要找得到
    - **博斯普魯斯海峽**：
      ```js
      m.jumpTo({ center: [29.061, 41.083], zoom: 10.5 });
      m.getBounds().getNorth()                                                   // ≈ 41.25（北口要在框內）
      ```
      - ⚠️ **zoom 11 會把北口切掉**（實測北界只到 41.203，海峽北端在 41.24）。
        整條 31 公里的海峽必須都在畫面上，這是這一筆的取景條件
      - ⚠️ 700 公尺的水道在 zoom 10.5 橫向約 **20 個像素**：
        ```js
        const p = m.project([29.061, 41.083]);
        let n = 0; for (let d = -60; d <= 60; d++)
          if (m.queryRenderedFeatures([p.x + d, p.y], { layers: ['water'] }).length) n++;
        n   // ≈ 20（0 就是回歸；先確認 document.visibilityState 是 visible）
        ```
      - ⚠️ **卡片不可以叫讀者打開「大洲分區」**：那一層 `maxzoom` 是 5，在這個視角
        一個圖徵都不畫。驗法同麻六甲那條（勾開它 `getStyle().layers` 有東西，
        `queryRenderedFeatures` 是 0）
      - 卡片要留著蒙特勒公約與洲界那兩段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('700 公尺'), t.includes('蒙特勒'), t.includes('zoom 5')]   // [true, true, true]
        ```
      - 搜「博斯普魯斯」「Bosphorus」「伊斯坦堡海峽」都要找得到——⚠️ 第三個是
        `meta` 有沒有進 haystack 的回歸判準
    - **曼德海峽**：
      ```js
      m.jumpTo({ center: [43.333, 12.583], zoom: 7.2 });   // ⚠️ 必須 < 8，見下
      const ids = m.getStyle().layers.map(l => l.id);
      // 先勾「板塊邊界」與「板塊與地形」（東非大裂谷），兩層都要畫得出來
      [...new Set(m.queryRenderedFeatures({ layers: ids.filter(i => /^plate-boundaries-/.test(i)) })
        .map(f => f.properties.name))]        // ["張裂型邊界", "錯動型邊界"]
      [...new Set(m.queryRenderedFeatures({ layers: ids.filter(i => /^world-picks-rift-/.test(i)) })
        .map(f => f.properties.name))]        // ["東非大裂谷"]
      ```
      - ⚠️ **這是這一筆取景的硬條件**：卡片的「對照重點」叫讀者打開那兩層，而它們的
        `maxzoom` 都是 **8**。zoom 7.5 也畫得出來，但實測裂谷北端（14.2°N）離上緣
        只剩 0.14°，換一個矮一點的視窗就會掉出去，所以定 7.2（實測 lat 10.41–14.74）
      - 卡片要留著 EIA 與 PortWatch 那兩段，⚠️ **PortWatch 的三個數字要一起在**
        （只留曼德那一個，「船沒有消失、只是改走遠路」就沒有證據了）：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('620 萬桶'), t.includes('74.6'), t.includes('89.9'), t.includes('2026 年 8 月 23 日')]
        // [true, true, true, true]
        ```
      - 搜「曼德」「Mandeb」要找得到
    - **麻六甲海峽**：
      ```js
      m.jumpTo({ center: [103.455, 1.157], zoom: 5.9 });
      m.queryRenderedFeatures({ layers: ['world-picks-straits-points'] })
        .map(f => f.properties.id)                                              // ["strait-of-malacca"]
      const p = m.project([103.455, 1.157]);
      m.queryRenderedFeatures([p.x, p.y], { layers: ['water'] }).length          // 1（藍點必須落在水上）
      ```
      - ⚠️ **取景要讀得出「西北寬、東南收窄」的漏斗**：1920×873 的畫布上北界應是
        **6.28°N**（`m.getBounds()`），蘇門答臘北端與檳城一帶都要在畫面上，
        吉隆坡、馬六甲市、新加坡沿著海峽排開。只看得到新加坡一帶就是
        `properties.zoom` 被調大了（實測 6.5 只框得到東南三分之一）
      - ⚠️ **藍點在東南端、不是菲利普水道**：卡片講的 2.8 公里瓶頸在更東南的新加坡
        海峽裡，這一層刻意不標到那裡（見上）
      - 卡片要留著「界線畫到哪裡」與 EIA 的年份（同上）：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('2.8 公里'), t.includes('38 公里'), t.includes('2016 年'), t.includes('61.7%')]
        // [true, true, true, true]
        ```
      - ⚠️ **「對照重點」指的是「赤道」參考線，不是「行星風系」**：`wind-belts` 的
        `maxzoom` 是 4，在 zoom 5.9 一個圖徵都不畫。驗法是勾開它之後
        `m.getStyle().layers.filter(l => /wind-belts/.test(l.id))` 有東西，但
        `queryRenderedFeatures` 是 **0**；而 `latitude-lines-label` 查得到「赤道」
      - 搜「麻六甲」「馬六甲海峽」「Malacca」都要找得到——⚠️ 第二個是 `meta` 有沒有
        進 haystack 的回歸判準（別譯只寫在 `meta` 上）
    - **臺灣海峽**：
      ```js
      m.jumpTo({ center: [119.92833, 24.81111], zoom: 6.3 });
      m.getBounds().getSouth()                                                   // ≈ 21.23（鵝鑾鼻 21.90 必須在框內）
      m.queryRenderedFeatures({ layers: ['world-picks-straits-points'] })
        .map(f => f.properties.id)                                              // ["taiwan-strait"]
      ```
      - ⚠️ **取景條件是「整個臺灣島都在畫面上」**，不是看海峽多窄。實測 1920×929：
        zoom 6.9 的南界只到 22.457°N，**鵝鑾鼻會被切掉**；6.3 是 21.227–28.295，
        臺灣、福建、呂宋海峽北段都在框裡
      - 卡片要留著 PortWatch 的排名、澎湖 1 號與板塊那三段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('238.8'), t.includes('澎湖 1 號'), t.includes('揚子板塊'), t.includes('麓山帶')]
        // [true, true, true, true]
        ```
        ⚠️ **不可以退回成「整條海峽都在歐亞板塊上」**——實測是揚子板塊，見上
      - 勾「板塊」與「板塊邊界」（`maxzoom` 都是 8 > 6.3）之後兩層都要畫得出來：
        ```js
        const ids = m.getStyle().layers.map(l => l.id);
        [...new Set(m.queryRenderedFeatures({ layers: ids.filter(i => /^plates-/.test(i)) })
          .map(f => f.properties.name))]   // 含「揚子板塊」「菲律賓海板塊」「沖繩板塊」
        ```
      - 搜「臺灣海峽」「**台灣海峽**」「黑水溝」「福爾摩沙海峽」「Taiwan Strait」
        五種都要找得到，而且前兩種的結果要**逐筆相同**——⚠️「黑水溝」與
        「福爾摩沙海峽」寫在 `meta` 上，而「台灣海峽」靠的是 `searchIndex.ts` 的
        `normalizeForMatch()`（台→臺），那是那支正規化的回歸判準
    - **土渕海峽**：
      ```js
      m.jumpTo({ center: [134.1861833, 34.4864722], zoom: 16 });   // 16 = MapView 的 maxZoom
      m.queryRenderedFeatures({ layers: ['world-picks-straits-points'] })
        .map(f => f.properties.id)                                              // ["dobuchi-strait"]
      ```
      - ⚠️ **那條 9.93 公尺寬的水道真的畫得出來，一定要量過**（肉眼會以為它斷掉——
        永代橋與這一層自己的藍點壓在正上方）。逐像素打 `water` 掃出那條帶——它是
        **西北－東南斜的**，所以要一列一列掃，不能只沿著單一軸打：
        ```js
        const p = m.project([134.1861833, 34.4864722]);
        const hit = (dx, dy) => m.queryRenderedFeatures([p.x + dx, p.y + dy], { layers: ['water'] }).length;
        const row = dy => dy + ' ' + Array.from({ length: 29 }, (_, i) => hit(i - 14, dy) ? '#' : '.').join('');
        [-10, -4, 0, 4, 10].map(row).join('\n');
        // 實測（1920×873 畫布）每一列都有連續 5 個 #，中心每往下一列往右移約 1.1 px：
        //  -10 ..#####......................
        //   -4 .........#####...............
        //    0 .............#####...........
        //    4 ..................#####......
        //   10 ........................#####
        ```
        **整片全是 `.` 就是回歸**（多半是 `properties.zoom` 被調小了）。
        ⚠️ 但全 0 也可能只是**分頁在背景**：量之前先確認 `document.visibilityState`
        是 `visible`。這一項踩過——螢幕休眠時 `queryRenderedFeatures` 一律回 0，
        看起來跟圖層壞掉一模一樣
      - 卡片要留著退件與登載地形圖那一段（同上）：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('1990'), t.includes('國土地理院'), t.includes('9.93')]   // [true, true, true]
        ```
      - 資料來源那一行要有**四個**連結（土庄町／日文維基／地理院地圖／中文維基），
        **不可以有沒有連結的純文字**
      - 搜「土淵海峽」「土渕海峽」「Dofuchi」「世界最窄」都要找得到——
        ⚠️ 前者是 `meta` 有沒有進 haystack 的回歸判準（異體字只寫在 `meta` 上）
    - **巴拿馬運河**：
      ```js
      m.jumpTo({ center: [-79.75, 9.12], zoom: 9.3 });
      m.queryRenderedFeatures({ layers: ['world-picks-canals-points'] })
        .map(f => f.properties.id)                                              // ["panama-canal"]
      ```
      - ⚠️ **取景條件是「科隆與巴拿馬城要同時在畫面上」**——卡片講的反直覺點
        （大西洋端在西、太平洋端在東）只有這樣才看得到。實測 1920×873：
        123 m/px、lat 8.639–9.600，科隆在左上、巴拿馬城在右下，加通湖在中間
      - 卡片要留著船閘、耗水與乾旱那三段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('26 公尺'), t.includes('2 億公升'), t.includes('20.7'), t.includes('麥哲倫')]
        // [true, true, true, true]
        ```
        ⚠️ **麥哲倫那一段不可以拿掉**：「巴拿馬塞住時船並沒有繞南美洲」是靠它跟
        蘇伊士／好望角的對比才成立的
      - 搜「巴拿馬運河」「Panama」「運河」都要找得到（⚠️「Panama」會同時命中
        「巴拿馬板塊」，那是預期的）
    - **蘇伊士運河**：
      ```js
      m.jumpTo({ center: [32.34417, 30.705], zoom: 8 });
      m.getBounds()   // lat 應涵蓋塞德港 31.26 與蘇伊士 29.97（實測 29.669–31.730）
      ```
      - ⚠️ 整條 193 公里的運河要都在畫面上，尼羅河三角洲與西奈半島也要進得來
        （「亞非洲界走這條地峽」那一段靠它）
      - 卡片要留著拿破崙、黃色艦隊、長賜輪與雷賽布遷徙：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('拿破崙'), t.includes('黃色艦隊'), t.includes('長賜輪'),
         t.includes('73.6'), t.includes('雷賽布遷徙'), t.includes('君士坦丁堡公約')]
        // 六個都要是 true
        ```
        資料來源那一行要有**五個**連結
      - ⚠️ **卡片不可以叫讀者打開「大洲分區」**（`maxzoom` 5，在 zoom 8 不畫），
        處理方式跟博斯普魯斯那張卡一致
      - 搜「蘇伊士運河」「Suez」「運河」都要找得到
    - **兩個海角（合恩角／好望角）**：
      ```js
      // 逐筆取景寫在 properties.zoom 上，實測值（畫布 1920×772）：5.6 / 7.8
      m.jumpTo({ center: [-67.289167, -55.98], zoom: 5.6 });
      m.queryRenderedFeatures({ layers: ['world-picks-capes-points'] })
        .map(f => f.properties.id)                                              // ["cape-horn"]
      m.jumpTo({ center: [18.475556, -34.358056], zoom: 7.8 });
      m.getBounds().getEast()      // ≈ 21.50（厄加勒斯角 20.01 必須在框內）
      ```
      - ⚠️ **兩個藍點都要落在陸地上**（岬頭本身，不是外海）：
        ```js
        const on = (lng, lat) => { const p = m.project([lng, lat]);
          return m.queryRenderedFeatures([p.x, p.y], { layers: ['water'] }).length === 0; };
        // zoom 13 各驗一次，兩個都要是 true
        ```
        ⚠️ 好望角那一筆要停在**西南側**那個岬頭上；東邊 1.2 公里那塊更高的懸崖是
        **開普角**，中文維基所載的座標就落在那一側（見上，那是刻意不用它的理由）
      - ⚠️ **合恩角的 `zoom` 必須小於 6**：卡片叫讀者勾「洋流」看西風漂流，而那一層
        `maxzoom` 是 6。實測 5.6：
        ```js
        m.queryRenderedFeatures({ layers: ['ocean-currents-cold-line'] })
          .map(f => f.properties.id)     // 都是 "west-wind-drift"（實測 6 段、標註 3 個）
        ```
        ⚠️ 好望角相反（7.8 > 6），它的卡片**不可以**叫讀者直接勾洋流，處理方式跟
        博斯普魯斯／蘇伊士對「大洲分區」一致
      - ⚠️ **好望角的取景條件是「厄加勒斯角要在同一個畫面上」**——「它不是非洲最南端」
        那一段的憑據就是這件事。實測 zoom 8.5 東界只到 20.34，窄一點的視窗就會把它
        擠掉；7.8 的東界 21.50，畫面右下角看得到斯特雷斯拜、阿古拉斯那一段海岸
      - 卡片要留著這幾段：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        // 合恩角
        [t.includes('弗羅厄德角'), t.includes('67°16′'), t.includes('麥哲倫海峽'),
         t.includes('4.2')]                                    // 四個都要是 true
        // 好望角
        [t.includes('厄加勒斯角'), t.includes('東經 20 度'), t.includes('開普角'),
         t.includes('91.4'), t.includes('20.2')]               // 五個都要是 true
        ```
        ⚠️ **「大洋分界是一條經線」那兩段不可以拿掉**，那是兩張卡共同的重點；
        ⚠️ **合恩角的麥哲倫海峽數列與好望角的 48.9 → 91.4 也不可以只改一邊**，
        它們跟巴拿馬、蘇伊士那兩張卡互相引用（見上，四張要一起維護）
      - 資料來源那一行：合恩角**五個**連結、好望角**六個**，
        **不可以有沒有連結的純文字**
      - 搜「合恩角」「Cape Horn」「好望角」「Cape of Good Hope」都要找得到；
        抽屜的可點清單裡「海角」這一組要排在「運河」與「湖泊」之間，組內
        合恩角在前
    - **四個湖（蘇必略湖／死海／裏海／貝加爾湖）**：
      ```js
      // 逐筆取景寫在 properties.zoom 上，實測值：5.6 / 8.4 / 4.8 / 5.8
      [['lake-superior',[-87.08333,47.11667],5.6], ['dead-sea',[35.47,31.5],8.4],
       ['caspian-sea',[50.7,41.8],4.8], ['lake-baikal',[108.2,53.5],5.8]]
      // 可點清單的順序（由西往東）：蘇必略湖／死海／裏海／貝加爾湖
      ```
      - ⚠️ **四個藍點都要落在水上**（打向量底圖的 `water` 圖層要命中），而且取景要
        框得住整個湖：蘇必略湖見下、死海要看得到南端的蒸發池（卡片有講）、裏海要
        五個沿岸國同時在畫面上、貝加爾湖要整條新月形都在
      - ⚠️ **蘇必略湖的取景還要同時容得下麥基諾水道與休倫湖**（1920×803 實測 zoom 5.6
        時，湖身 535 px、西端德盧斯在 x=614 清得過詳情面板，蘇聖瑪麗、密西根湖與
        休倫湖的名字都在畫面上）——**那不是美觀問題**，卡片第三段講的正是「密西根湖
        與休倫湖水文上是同一個湖」，看不到那兩個湖等於那一段沒有依據。5.9 就會掉出去
      - ⚠️ **蘇必略湖的面積是 82,100 平方公里（美國環保署），刻意跟中文維基的 82,414
        不同**（同一份也是蓄水量、最大深度與 191 年滯留時間的出處），比照貝加爾湖
      - 卡片的互相指涉要留著（改一張就看另外三張）：
        ```js
        // 死海 → 提到貝加爾湖的 −1,186 公尺；貝加爾湖 → 提到死海與東非大裂谷
        // 蘇必略湖 → 提到裏海（鹹水）、貝加爾湖（水量與裂谷成因）與密西根－休倫湖
        const t = document.querySelector('.map-detail-panel').innerText;
        ```
      - ⚠️ 貝加爾湖的最深值是 **1,642 公尺**，**刻意跟中文維基的 1,742 不同**（見上）
      - 搜「裏海」「裡海」「霍爾木茲」「蘇必略湖」「蘇必利爾湖」「Lake Superior」
        都要找得到——那是 `meta` 有沒有進 haystack 的回歸判準（別譯只寫在 `meta` 上）
    - ⚠️ **這九層不佔圓點名額**（`exemptFromMaxActive`），驗法見本項開頭那一段。
      本檔舊版寫的「這一層讓圓點圖層變成五個、勾滿會 disabled」已經不適用於它們，
      但**對其餘五個圓點圖層仍然有效**。

40. **世界櫥窗：有範圍的那兩層**（`/theme/world` → 世界櫥窗 → 「板塊與地形」與
    「傳說中的地方」，見上。⚠️ 2026-08 之前是合成一層的「作者精選・範圍」，
    `world-picks-areas-*` 那組 id 已經不存在）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.jumpTo({ center: [-72, 25], zoom: 4.2 });
    m.queryRenderedFeatures({ layers: ['world-picks-legend-line'] }).length     // > 0
    m.queryRenderedFeatures({ layers: ['world-picks-legend-label'] }).length    // > 0（沿線寫著「百慕達三角」）
    m.getPaintProperty('world-picks-legend-line', 'line-dasharray')             // [3, 3]
    m.getPaintProperty('world-picks-rift-line', 'line-dasharray')               // [3, 3]（兩層都是）
    ```
    - ⚠️ **一定要是虛線**：實線會讓一條根本不存在的界線看起來像真的，那正好是這張卡
      要拆穿的東西。這是回歸判準，不是樣式偏好
    - 形狀要是一個**閉合的三角形**（邁阿密－百慕達－聖胡安），不是兩段折線
    - 點三角形的邊或點標註都要開得了卡：標題「百慕達三角」、副標「一個沒有官方界線
      的「地方」——傳說怎麼被統計拆穿」，而且卡上要有**示意警語**（`schematic` 在
      內容檔上）：
      ```js
      const t = document.querySelector('.map-detail-panel').innerText;
      [t.includes('沒有證據'), t.includes('教學示意')]        // [true, true]
      ```
    - ⚠️ 它跟「緯度參考線」「國際換日線」**長得一樣**（同樣的灰虛線），那是刻意的
      ——三層一起打開時要靠沿線標註分辨。標註數是 0 就等於這一層沒有身分
    - **東非大裂谷**（同一層的第二筆）：
      ```js
      m.jumpTo({ center: [34.5, 0], zoom: 4.1 });
      // 兩支各是一段，line 只有一筆圖徵；1568×774 的截圖尺度下標註 ≥ 2
      new Set(m.queryRenderedFeatures({layers:['world-picks-rift-line']}).map(f=>f.properties.id))
      // → {"east-african-rift"}
      ```
      - ⚠️ **形狀是這一筆的全部，只能用眼睛驗**：東支要從阿法爾（達納基爾）沿衣索比亞
        裂谷南下、穿過圖爾卡納湖與奈瓦沙湖，在坦尚尼亞中部消散；西支要沿艾伯特湖、
        愛德華湖、基伏湖、坦干伊喀湖、馬拉威湖一路到莫三比克。⚠️ **兩支必須是分開的
        兩段**（維多利亞湖那一塊不在裂谷裡），接成一條就是回歸
      - ⚠️ **跟「板塊邊界」＋「火山帶」一起打開**（`jumpTo([33.5,-4], 4.3)`）：橘色
        虛線（張裂型）只壓在**西支**上、東支一條橘線都沒有，而洋紅色的火山點沿著
        **東支**排成一串。實測 Bird (2003) 在 34–38.5°E／−6–4.5°N 三種邊界各 0 個點
        ——**那是已知且寫進 `notes` 與卡片的，不是漏畫**
      - 點線或點標註要開得了卡：標題「東非大裂谷」、副標「一塊大陸正在裂開，而且看得到
        過程」，卡上要有**示意警語**與五個來源連結：
        ```js
        const t = document.querySelector('.map-detail-panel').innerText;
        [t.includes('維多利亞湖並不在裂谷裡'), t.includes('教學示意')]   // [true, true]
        ```
      - 搜「東非大裂谷」「裂谷」「Rift」「East African」都要找得到
    - ⚠️ **兩層各自一個核取方塊、各自一份清單**（拆層前是同一層底下 `groupBy` 出來的
      兩個組名）：「板塊與地形」列東非大裂谷、「傳說中的地方」列百慕達三角，各 1 列。
    - 兩層的 `maxzoom` 都是 8：zoom 8.2 兩層都應該消失
    - ⚠️ 兩層**都不佔線圖層名額**（`exemptFromMaxActive`）：跟洋流、板塊邊界、
      緯度參考線、國際換日線一起打開時，沒有一個核取方塊會因為它們而變 `disabled`
    - 切底圖之後重驗存在、虛線與排序

44. **世界重要城市（31 個）與新的 `/compare` 配對**（見上面「世界重要城市」）：
    ```js
    // 主題預設視角（zoom 1.8），世界主題進站就有（defaultOn）
    const m = window.__gaiaMaps.at(-1);
    m.queryRenderedFeatures({layers:['world-places-points']}).length   // 1512 寬的畫布實測 31
    ```
    - 點任一顆藍點要開 `PlaceCard`：面板標題列（`.map-detail-head-title`）＝城市名，
      卡片有 4–5 條 `facts`，最後一條是「對照重點」，來源是「維基百科 <條目>、
      Open-Meteo ERA5 再分析資料」兩條**都可點**
    - ⚠️ **`PlaceCard` 沒有氣候圖表**（既有規則），所以卡片內文不可以寫「下面那張圖」
    - `/compare` 的下拉選單要有兩個 `<optgroup>`：
      ```js
      [...document.querySelectorAll('.map-panel-head select')[0].querySelectorAll('optgroup')]
        .map(g => [g.label, g.children.length])        // [["臺灣",25],["世界",31]]
      ```
    - 五組新配對各點一次：網址要出現 `a=`／`b=`、兩張地圖飛到同一個 `lat`、
      **四張圖表都在**且左右 Y 軸範圍相同。雷克雅維克 ↔ 努克那一組是最好認的一組
      （一月 −0.9 ↔ −14.4），氣溫線的落差一眼可見
    - ⚠️ **新增地點之後要逐筆核對卡片上的數字與 ERA5**（利馬／雪梨／努克那三筆的
      處理方式見上面那張表）：
      ```bash
      node -e "const j=require('./public/data/climate/lima.json');
        console.log(j.temperature_c[0], j.temperature_c[6],
          Math.round(j.precipitation_mm.reduce((a,b)=>a+b)))"
      ```

45. **柯本 30 張卡、世界主要河流 33 張卡、火山 9 張、主峰 10 張**（見上面各節）：
    ```js
    // 勾起圖層 → 分片必須是 200，而且只抓勾起來的那幾份
    performance.getEntriesByType('resource').filter(r => /geo-content/.test(r.name))
      .map(r => [r.name.split('/').pop(), r.responseStatus, Math.round(r.transferSize/1024)+'KB'])
    // 實測 preview（4173）勾三層後：koppen-zones 14KB／world-rivers 15KB／volcanoes 5KB，全 200
    ```
    - **有卡片的圖徵**：`.detail-facts` 有內容、`.feature-fallback` 不存在，來源換成
      內容檔自己的那一組（例如黃河是「維基百科 黃河、Natural Earth 1:50m 河流與湖泊中心線」）
    - **沒有卡片的圖徵**：走 fallback，而且**面板標題列不可以是空的**——那是
      `useDetailTitle` 的 `geo` fallback（見 `CLAUDE.md`）。兩種各驗一次最省事：
      ```js
      // 抽屜清單點「黃河」→ 標題「黃河」、facts 4 條
      // 再點「尼羅河（艾伯特段）」→ 標題「尼羅河（艾伯特段）」、fallback 那段圖層說明
      ```
    - 柯本那 30 張的 `stats` 第四格必須是「代表地點」（有內容檔之後 geojson 的
      `meta`／`detail` 就不畫了，見上）
    - ⚠️ **柯本的五個大類要另外點一次**（`items.detail`，見上）：勾起圖層之後點
      抽屜裡「A 熱帶氣候」…「E 極地氣候」那五個**名稱**，每一張都要有標題、副標、
      四格 `stats` 與四到五條 `facts`——只有圖層名稱加一行資料來源就是回歸。
      點完再回地圖上點一塊色塊，開的必須仍然是**亞型**卡（臺北一帶 Cfa、撒哈拉 BWh）
    - 搜尋：「熱帶雨林氣候」「地中海型氣候」「針葉林氣候」「苔原氣候」「沙漠化」
      都要搜得到對應的**大類子項目**（keywords 的補強，見上）；「尼羅河」要列出
      主流與五個河段；「聖母峰」「富士山」要開得了卡
    - ⚠️ 主峰卡片**不可以**出現示意警語（`.feature-schematic`），內容檔的
      `schematic: false` 就是為此

46. **世界人口分布**（`/theme/world` → 城市，見上面「世界人口分布」）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    m.queryRenderedFeatures({ layers: ['world-population-points'] }).length   // 主題預設視角 505 上下
    m.getPaintProperty('world-population-points', 'circle-color')             // "#80610d"
    ```
    - **這一層的重點是空白處**：撒哈拉、阿拉伯半島、西伯利亞、澳洲內陸與亞馬遜要看得出
      幾乎沒有點，而東亞、南亞、歐洲、北美東北部連成一片。⚠️ 這件事
      **`queryRenderedFeatures` 驗不到，只能看畫面**（比照全球地震帶那次「勾了圖層
      但看不見」的教訓）
    - **疊在氣候帶上要讀得出來**：把「柯本氣候分區」（五類全開）或「森林與沙漠帶」
      一起打開，跳到東亞與南亞看圓點有沒有被面染吃掉。數字上的餘裕見上面那張表
      （ΔE ≥ 34、對比 ≥ 3.5），但**這一關要用眼睛確認**
    - 點任一顆要開卡：標題是中文名＋原名，`meta` 是「首都・國家・都會區人口」，
      `detail` 是「市轄區人口…」，而且**沒有** `.detail-facts`（這一層沒有內容檔）
    - ⚠️ **`POP_MIN` 那一行要抽兩個相反的例子看**（那條 10% 門檻的回歸判準，見上）：
      東京要有「市轄區人口 834 萬」（跟都會區 3,568 萬的差距正是這一行要教的），
      **巴黎不可以出現那一行**（上游給的是 1.1 萬）。實測 434／505 有那一行
    - **不進搜尋索引**：聚焦搜尋框之後 `performance.getEntriesByType('resource')`
      裡**不可以**出現 `world-population.geojson`；搜「世界人口分布」只會搜到**圖層**
      那一筆（`kind: "layer"`），搜「東京」只會出現「世界重要城市」那一筆
    - 切底圖之後圓點與大小要還在（`reapply` 的既有路徑）

47. **古文明發源地**（`/theme/world` → 人文專題，見上面「古文明發源地」）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    // 逐筆取景寫在 properties.zoom 上，實測值（畫布 1920×873）：
    // 孟菲斯 7.4／烏魯克 6.6／摩亨佐-達羅 7.2／二里頭 8.6／聖羅倫索 8.0／卡拉爾 5.9
    m.queryRenderedFeatures({ layers: ['world-civilizations-points'] }).length
    ```
    - 六筆都要能從抽屜的可點清單點開，而且都不是 `FeatureCard` 的 fallback：
      ```js
      const card = () => ({
        head: document.querySelector('.map-detail-head-title')?.textContent,
        facts: document.querySelectorAll('.map-detail-panel .fact-label').length,        // 5
        //  ⚠️ 卡片的 facts 是 <ul class="detail-facts"> + .fact-label／.fact-value，
        //     **不是 dl/dt**——用 `.detail-facts dt` 數會得到 0 而誤判成卡片壞了（踩過）
        fallback: !!document.querySelector('.map-detail-panel .feature-fallback'),      // false
      });
      ```
    - ⚠️ **可點清單要分成「舊大陸」（四筆）與「新大陸」（兩筆）兩組**，順序是
      孟菲斯、烏魯克、摩亨佐-達羅、二里頭／聖羅倫索、卡拉爾——**不是整層依經度排**
      （`groupBy` 依序切、不排序，所以 geojson 裡舊大陸那四筆必須連續）
    - ⚠️ **每一筆的取景條件都要重驗，那不是隨手填的 zoom**（見上面那張表）：
      ```js
      m.jumpTo({ center: [-94.76, 17.75361111], zoom: 8.0 });          // 勾「火山帶」
      m.queryRenderedFeatures({ layers: ['volcanoes-points'] })
        .map(f => f.properties.name)          // 要有 "San Martin"（石材的產地）
      m.jumpTo({ center: [-77.52027778, -10.89361111], zoom: 5.9 });   // 勾「洋流」
      m.queryRenderedFeatures({ layers: ['ocean-currents-cold-line'] })
        .map(f => f.properties.name)          // 要有「祕魯寒流」（zoom 必須 < 6）
      m.jumpTo({ center: [45.63722222, 31.32416667], zoom: 6.6 });     // 勾「板塊邊界」
      m.queryRenderedFeatures({ layers: ['plate-boundaries-convergent-line'] }).length  // > 0
      m.jumpTo({ center: [31.25083333, 29.84472222], zoom: 7.4 });     // 勾「世界主要河流」
      m.queryRenderedFeatures({ layers: ['world-rivers-label'] })
        .map(f => f.properties.name)          // 尼羅河 + 羅塞塔分流 + 杜姆亞特分流
      ```
    - ⚠️ **搜「古文明」要出得來六筆**（`category` 不在 haystack 裡，靠的是六筆各自的
      `meta`；而且是**子字串**比對——「古埃及文明」不含「古文明」，孟菲斯踩過）。
      另外「烏魯克」「Uruk」「摩亨約達羅」「歐美克」「美索不達米亞」也都要找得到
    - ⚠️ **孟菲斯的圖徵 id 必須是 `memphis-egypt`**：`world-population` 那一層已經有一個
      `memphis`（美國曼非斯），撞了會互相連動強調（見上）。兩層一起勾、點埃及孟菲斯之後：
      ```js
      m.getPaintProperty('world-civilizations-points', 'circle-radius')
      // ["case",["in",["get","id"],["literal",["memphis-egypt"]]],["*",6,2],6]
      m.getPaintProperty('world-population-points', 'circle-radius')
      // 同一份清單，但沒有任何人口圓點的 id 是 memphis-egypt → 一顆都不會被放大
      ```
    - 六張卡各自的重點要留著（見上，改稿時最容易被當枝節刪掉）：
      ```js
      const t = document.querySelector('.map-detail-panel').innerText;
      // 烏魯克：'帳本'、孟菲斯：'Hut-ka-Ptah'、摩亨佐-達羅：'讀不出來'
      // 二里頭：'洪水' 與 '推論'、聖羅倫索：'輪車'、卡拉爾：'陶器'
      ```
      ⚠️ **二里頭那張卡不可以寫成「這裡是夏朝的首都」**（沒有自證性的文字，見上）；
      ⚠️ **卡拉爾那張卡的「柯本畫成 BSh 是網格解析度的結果」也不可以刪掉去遷就圖層**
    - 資料來源那一行**不可以有沒有連結的純文字**（六張卡合計十五個新來源，
      四筆 UNESCO 的名錄編號是從 Wikidata 查的：86／138／1114／1269）
    - ⚠️ **`world-picks` 要退回 17 筆**（搬家之後那一層不可以還留著這六筆）：
      ```js
      await fetch('/data/geo-manual/world-picks.geojson').then(r => r.json())
        .then(d => d.features.length)                                   // 17（⚠️ 那是搬家當下的數字；
        // 同月加了馬里亞納海溝之後是 18，要驗的是「這六筆古文明不在裡面」而不是那個總數）
      ```
      而且編者選集那九層的圖層說明、`notes` 與 `sources` 裡**不可以再出現古文明的字句**

52. **世界紀錄熱帶氣旋路徑**（`/theme/world`，「海洋」組，見上）。⚠️ 這一項全部要在
    **前景分頁**做，而且**別在小視窗量標註**——下面的數字實測於 1920×859 的 CSS 視窗。
    ```js
    const m = window.__gaiaMaps.at(-1);
    const uniq = l => new Set(m.queryRenderedFeatures({ layers: [l] }).map(f => f.properties.id)).size;
    // 主題預設視角（center [0,10]、zoom 1.8）
    uniq('world-cyclones-line')                                             // 33
    m.queryRenderedFeatures({ layers: ['world-cyclones-label'] }).length    // 25（24 條不重複）
    m.queryRenderedFeatures({ layers: ['world-cyclone-centers-points'] }).length  // 1729
    ```
    - ⚠️ **沒有任何一條線的經度跨距 ≥ 180**（八條跨換日線的切段是否成功，見上）：
      ```js
      const span = {};
      for (const f of m.queryRenderedFeatures({ layers: ['world-cyclones-line'] })) {
        const gs = f.geometry.type === 'LineString' ? [f.geometry.coordinates] : f.geometry.coordinates;
        for (const g of gs) { const xs = g.map(p => p[0]);
          span[f.properties.id] = Math.max(span[f.properties.id] || 0, Math.max(...xs) - Math.min(...xs)); }
      }
      Object.entries(span).filter(([, v]) => v >= 180)                      // []（最大是弗雷迪 87.6°）
      ```
    - **色階與 `nodata`**：`circle-color` 必須是
      `["case",["has","wind"],["step",["get","wind"],…34,…64,…113,…],"#7d7c76"]`，
      而且**波拉與馬希納整條路徑一筆 `wind` 都沒有**（跳到孟加拉灣 zoom 5 查
      `world-cyclone-centers-points`，`bhola-1970` 的每一筆 `properties.wind` 都是
      undefined）。圖例要有五列，最後一列是「無風速紀錄」
    - **`exemptFromMaxActive` 真的生效**（見上）：先勾滿三個吃色票的線圖層
      （緯度參考線是 `defaultOn`，再勾國際換日線與世界主要河流），此時
      「世界主要山脈」與「洋流」的核取方塊是 `disabled`，**「世界紀錄熱帶氣旋路徑」
      不是**，而且勾下去 33 條全部畫得出來
    - **附屬定位點與母圖層共用 id**：點任何一顆圓點要開出**那個氣旋**的卡片
      （用 `m.project()` 算螢幕座標再對 canvas 派 `mousedown/mouseup/click`），
      面板標題列與卡片標題都要是那個名字；`world-cyclone-centers-label` 只在該氣旋
      被選取時才有東西（zoom < 4 只標 UTC 00:00 的日標，≥ 4 每筆都標日期＋時刻）
    - **抽屜清單 33 列、依洋盆分組**，順序是西北太平洋 →…→ 南大西洋，組內依年份；
      第一列是「卡門　1960 年・風眼最大（直徑約 370 公里）」，最後一列是卡塔琳娜
    - **搜尋**：「狄普」「HAIYAN」「卡崔娜」都要出得來（⚠️ 搜「卡崔娜」會**同時**
      出現哈維，那是對的——哈維的 `meta` 寫著「與卡崔娜並列損失最大」）。
      選下去要只飛一次、勾上圖層、開卡片。⚠️ 聚焦搜尋框之後
      **只能多抓 `world-cyclones.geojson`（33 KB）**，
      `world-cyclone-centers.geojson`（268 KB）必須是 **0 次**
    - **切底圖之後全部重驗一次**（`.map-tile` → `.basemap-select select`）：三個圖層都還在、
      排序仍然是 `contour-lines < line < points < label`、選取強調的表達式還帶著那個 id
    - 33 張卡各自的重點要留著（改稿時最容易被當枝節刪掉）：
      ```js
      const t = document.querySelector('.map-detail-panel').innerText;
      // 狄普：'870'、帕翠莎：'24 小時'、波拉：'孟加拉'、馬希納：'不是儀器實測'
      // 奧里薩 ↔ 法尼：兩張卡都要提到對方（同一地點、差一百倍的死亡人數）
      // 卡門 ↔ 葳瑪：兩張卡都要互相提到（風眼最大 vs 最小）
      // 狄普 ↔ 馬可：兩張卡都要互相提到（範圍最大 vs 最小）
      ```
      ⚠️ **資料來源那一行不可以有沒有連結的純文字**：33 張卡合計 35 個來源標籤
      （〈熱帶氣旋〉、IBTrACS，加上 33 個各自的條目，其中四筆是英文維基）
48. **規模最強地震**（`/theme/world` → 地體構造，見上面「規模最強地震」）：
    ```js
    const m = window.__gaiaMaps.at(-1);
    // 主題預設視角（zoom 1.8）就要看得到全部 18 筆——這一層的重點是「最強的幾次
    // 全部沿著隱沒帶排列」，看不到就等於這一層沒有存在的理由
    [...new Set(m.queryRenderedFeatures({ layers: ['quakes-strongest-points'] })
      .map(f => f.properties.id))].length                                  // 18
    ```
    - ⚠️ **這一項一定要看畫面，不能只數圖徵**（比照全球地震帶那次「勾了圖層但什麼都
      沒有」）：18 顆深灰白框點在 zoom 1.8 要一眼認得出智利、阿拉斯加、堪察加、日本、
      蘇門答臘那幾串，而且**跟「全球地震帶」一起打開時要浮在那團灰上面**——白框就是
      為此而畫的（母圖層刻意不畫，兩層判準相反）
    - **可點清單＝規模由大到小**，前四列是 1960 智利 9.5／1964 阿拉斯加 9.2／
      1833 蘇門答臘 9.2／2011 東北 9.1；⚠️ **2007 蘇門答臘（8.4）排最後**，那跟維基
      那張表的第 9 名不一樣，是採用 USGS 規模的必然結果，不是排序壞了
    - **四筆規模跟維基那張表對不上，`magWiki` 必須在**（建置時也會印四行警告）：
      ```js
      m.queryRenderedFeatures({ layers: ['quakes-strongest-points'] })
        .filter(f => f.properties.magWiki)
        .map(f => `${f.properties.id} ${f.properties.mag}/${f.properties.magWiki}`)
      // sumatra-2004 9.1/9.3、cascadia-1700 9/9.2、sumatra-2005 8.6/8.7、sumatra-2007 8.4/8.5
      ```
    - ⚠️ **1755 里斯本的點一定要落在大西洋上**（經度 −11，不是 +11，見上）：
      ```js
      m.jumpTo({ center: [-11, 36], zoom: 5 });
      m.queryRenderedFeatures(m.project([-11, 36]), { layers: ['water'] }).length > 0   // true
      ```
      而且那個取景要同時看得到里斯本與直布羅陀（卡片講的是「震央不在城下，在西南方
      約 200 公里的海底」）
    - **18 筆都要有內容檔，不可以是 `FeatureCard` 的 fallback**（卡片的 facts 是
      `.detail-facts .fact-label`，不是 `dt`，見第 47 項）：
      ```js
      !!document.querySelector('.map-detail-panel .feature-fallback')   // 每一筆都要是 false
      ```
    - ⚠️ **`exemptFromMaxActive` 是豁免、不是上限失效**（實測）：把四個吃色票的圓點
      圖層勾滿（世界重要城市、火山帶、世界人口分布、古文明發源地）之後，
      **「全球地震帶」是 `disabled`、「規模最強地震」仍然勾得下去**，而且五層同時算繪
    - **搜尋要吃得到別稱**（那是 `meta` 有沒有進 haystack 的回歸判準）：
      「311」「東日本大震災」「Tohoku」都要命中 2011 那一筆、「耶穌受難日」命中 1964、
      「明打威」命中 1833、「郯城」「里斯本」各命中一筆
    - **切底圖之後要再驗一次**（`reapply` 會重建圖層）：liberty ↔ nlsc-emap 來回之後
      圖層還在、堆疊順序仍然是 `quakes-points` 在它下面、`contour-labels` 在它上面，
      而且 `m._listeners.click.length` 前後相同
