# `npm run build:debug`（vite build --mode debug）會讀這個檔案。
#
# 為什麼需要它：地圖圖層的驗證流程（見 CLAUDE.md「驗證方式」）必須在 production
# build 下做——maplibre worker 檔案沒被複製、向量底圖畫不出來這類問題，`npm run dev`
# 用的是原始 ESM，根本踩不到。但 `import.meta.env.DEV` 在 production build 會被靜態
# 替換成 false，除錯掛勾就跟著消失，驗證指令會拿到 undefined 而看不出原因。
#
# 所以驗證用的建置走 `npm run build:debug && npm run preview`。
# 正式的 `npm run build`（部署用）不會讀到這個檔案，掛勾不會進到線上。
VITE_DEBUG_MAPS=1
