/**
 * Explore 頁三種主題圖層的顏色。
 *
 * 地形景點／原住民族分佈是固定單一色（清單靠點擊瀏覽，不靠顏色分類比較），
 * 特有種是唯一需要「依類別上色以便一眼比較」的圖層——多個物種疊圖時，
 * 顏色是分辨「這是哪個物種」的主要方式。
 *
 * 這裡的顏色組合已用 dataviz skill 的 scripts/validate_palette.js 驗證過
 * 明暗兩模式的色盲區辨度（--pairs all，因為核取方塊可以任意複選組合，
 * 不能只驗證清單裡「相鄰」的顏色）：
 *   - 地形景點藍 + 原住民族紅：兩色 all-pairs 全數 PASS
 *   - 物種三色（青／黃／紫）：三色 all-pairs 全數 PASS
 * 混色測試（例如把橘色也加進物種色票、或把物種色跟藍/紅混在一起做 5–6 色
 * all-pairs）都會失敗（橘配黃、青配洋紅在其中一種色盲模式下 ΔE 會掉到
 * 2–7，遠低於安全門檻），所以刻意只留這 5 個顏色、且物種色刻意跟兩個固定色
 * 錯開，不是隨手選的。**要改動或新增顏色，必須重新用 validate_palette.js
 * 驗證，不要憑感覺挑色。**
 *
 * 地圖圖層是 WebGL 畫布，深色模式不會跟著切換色票（比照 contour/hillshade
 * 既有做法固定寫死），所以地圖上只有一組顏色，不分 light/dark。
 * **但驗證時仍然要驗明暗兩模式**——理由不是地圖，是圖例與圖層抽屜的色塊：
 * 那些色塊用的是同樣的 hex，卻畫在會跟著主題切換的面板裡。
 * （不要把這一句「最佳化」掉，它解釋了為什麼規則 #10 要求驗兩個模式。）
 *
 * ## 圖層變多之後的策略：三組獨立色票
 *
 * 現有 5 色是當成**一組**做 all-pairs 驗證的，而註冊表會長到 ~20 個圖層，
 * 把同一個約束擴張上去不可能成立。出路是**形狀本身就在區辨**：18% 透明度的
 * 面染跟 6px 圓點是不同的視覺通道，沒有人會混淆。所以維持
 * `POINT_PALETTE` / `LINE_PALETTE` / `FILL_PALETTE` 三組，各自**組內**
 * all-pairs 驗證即可，跨幾何的配對不驗證也不需要驗證。
 * 每組再用 `MAX_ACTIVE_BY_KIND`（registry/types.ts）封頂，需求才維持在可解範圍。
 *
 * ⚠️ 這個模組刻意保持**零 import**：它被 Node 可讀的註冊表檔案 value-import，
 * 多一個 import 就可能讓 validate-content.mjs 的載入鏈斷掉。
 */
export const PLACES_COLOR = "#2a78d6";
export const INDIGENOUS_COLOR = "#e34948";

/** 物種比較色，依序指派。同時勾選超過這個數量時，UI 要限制不能再選。 */
export const SPECIES_COLORS = ["#1baf7a", "#eda100", "#4a3aa7"];

export const MAX_SIMULTANEOUS_SPECIES = SPECIES_COLORS.length;

/**
 * 線／面圖層的分類色。
 *
 * 已用 validate_palette.js 以 `--pairs all` 驗證過**明暗兩模式**：
 *   node scripts/validate_palette.js "#2a78d6,#d95926" --pairs all --mode light|dark
 *   → 六項檢查兩模式全數 PASS（CVD 最差 ΔE 25.4、一般視覺 ΔE 32.3，遠高於門檻）
 *
 * 行政區橘刻意用 `#d95926`（色票的 dark step）而不是 light step `#eb6834`：
 * 後者在 **dark 模式的亮度帶檢查會 FAIL**（L 0.671 超出 0.48–0.67）。地圖是
 * WebGL 畫布、只能有一組固定色，所以必須挑一個「兩個模式都過」的值，
 * 而不是各模式一個。這也是為什麼抽屜裡的色塊在深色主題下不會過亮。
 */
export const BOUNDARY_COLOR = "#d95926";
export const HYDROLOGY_COLOR = "#2a78d6";

/**
 * 山脈稜線（五大山脈、世界主要山脈）。洋紅。
 *
 * 三色一起以 `--pairs all` 驗證過**明暗兩模式**：
 *   node scripts/validate_palette.js "#2a78d6,#d95926,#c23f8f" --pairs all --mode light|dark
 *   → 兩模式各五項全數 PASS（CVD 最差 ΔE 12.3、一般視覺最差 ΔE 16.7）
 *
 * 為什麼**不能**用直覺上最像山的棕色：等高線是 `rgba(120,78,42,.55)`、地形陰影是
 * `#5a4632`，兩者都是棕色而且正好是使用者會同時打開的圖層——山脈線畫成棕色就等於
 * 畫在它自己要說明的那片地形上看不見。綠色同樣不行：NLSC 通用電子地圖的山區底色
 * 就是綠的。
 *
 * 紫色 `#7a3fa6` 其實比較接近製圖上「構造分區」的慣例，但它在 **dark 模式的對比
 * 檢查只有 2.56:1（WARN）**；比照 boundary 橘的既有決定——地圖是 WebGL 畫布只能有
 * 一組固定色，所以挑「兩個模式都乾淨通過」的值。`#6d3f9e` 與物種紫 `#4a3aa7` 更是
 * 直接在 dark 模式的亮度帶 FAIL。
 */
export const RELIEF_COLOR = "#c23f8f";

/**
 * 國家公園與各類保護區（面）。紫。
 *
 * 四色一起以 `--pairs all` 驗證過**明暗兩模式**：
 *   node scripts/validate_palette.js "#2a78d6,#d95926,#c23f8f,#7538ae" --pairs all --mode light|dark
 *   → CVD 最差 ΔE 9.2、一般視覺最差 ΔE 16.6（對洋紅），亮度帶與彩度下限兩模式皆過
 *
 * ⚠️ 上面那次是**加入時**的四色驗證。`TRANSPORT_COLOR` 後來在另一條分支上加入，
 * 現在線／面色票實際上是**五色**，合併時已重新一起驗過（見該常數的說明與
 * CLAUDE.md「顏色」一節）。要再加色請驗完整的五色清單。
 *
 * ## ⚠️ 這一個角色有一項 WARN，是知道之後接受的
 *
 * `#7538ae` 在**深色模式**對面板底色（`--surface: #1a1a19`）的對比是 **2.43:1**，
 * 低於 3:1（light 模式 5.2:1 沒問題）。這正是當初否掉山脈紫 `#7a3fa6`（2.56:1）的
 * 那一項，所以要說清楚為什麼這次的判斷相反：
 *
 * 1. **山脈那次有一個乾淨的替代品**（洋紅），這次沒有。掃過整個 OKLCH 色域（L
 *    0.48–0.68 × C 0.08–0.24 × 全色相），在四色 all-pairs 下**六項全 PASS** 的
 *    只剩兩族：h 152–160 的綠（`#5aa173` 一帶）與 h 308 的淡紫 `#a684c5`。
 * 2. **那兩個在地圖上都不能用，是實測不是推測。** 本主題的建議底圖是 NLSC 通用
 *    電子地圖，山區底色就是綠的，而保護區有一半以上落在山區——`#5aa173` 疊上去
 *    之後玉山、太魯閣、大武山的界線在畫面上幾乎描不出來（這與 `RELIEF_COLOR`
 *    當初排除綠與棕是同一個理由，只是那時排除的是線）。`#a684c5` 彩度只有 0.10，
 *    疊在花花綠綠的地形上同樣糊掉。`#7538ae` 則清楚可讀。
 * 3. **WARN 要求的「relief」在這個 UI 裡本來就成立。** 驗證器說對比 WARN 不可
 *    逕行忽略，必須有可見標籤或表格佐證——而色塊只出現在圖層抽屜與圖例，兩處
 *    **一律緊接著圖層名稱的文字**，色塊從來不是唯一的辨識線索。地圖畫布本身不隨
 *    主題變色，那 2.43:1 影響的只有深色面板上那一小塊色票。
 *
 * 換句話說：可讀性的取捨是「地圖上看不看得見」對「深色面板上的色票夠不夠亮」，
 * 前者是這個圖層存在的理由。**要改這個顏色，請先把上面兩族候選在 NLSC 底圖的
 * 山區實際疊一次再說。**
 *
 * 順帶一提，CVD 分離度最好的其實是棕／鏽色帶（h 24–72，最高 ΔE 12.3），但等高線
 * `rgba(120,78,42,.55)` 與地形陰影 `#5a4632` 都是棕的，理由同 `RELIEF_COLOR`。
 * 飽和的森林綠 `#1f7a4d` 對行政區橘的 CVD 只有 **4.4（hard FAIL）**，也不是選項。
 */
export const CONSERVATION_COLOR = "#7538ae";

/**
 * 交通軸線（高鐵、國道、臺鐵幹線）。翠綠。
 *
 * ⚠️ **這個顏色已經不再被算繪使用**：`tw-transport` 在 2026-08 改成七條軸線各自
 * 一色（見底下的 `TRANSPORT_COLORS`），母圖層因此沒有 `colorRole`。常數與
 * `LAYER_COLORS.transport` 保留下來，是因為底下這一整段記錄了「第四個線／面色
 * 是怎麼掃出來的」以及一整串被排除的候選——那段散文對下一個要加線／面色的人
 * 仍然有效，刪掉等於把結論丟了。要重新啟用它時直接把 `colorRole` 加回去即可。
 *
 * 四色一起以 `--pairs all` 驗證過**明暗兩模式**：
 *   node scripts/validate_palette.js "#2a78d6,#d95926,#c23f8f,#2da26d" --pairs all --mode light|dark
 *   → 兩模式各五項全數 PASS（CVD 最差 ΔE 8.3、一般視覺最差 ΔE 16.7）
 *
 * ⚠️ 上面那次是**加入時**的四色驗證，當時 `CONSERVATION_COLOR` 還在另一條分支上。
 * 合併後線／面色票是**五色**，已重新一起驗過：
 *   node scripts/validate_palette.js "#2a78d6,#d95926,#c23f8f,#7538ae,#2da26d" --pairs all --mode light|dark
 *   → 兩模式五項全數 PASS；最差的 CVD 對仍然是翠綠↔洋紅 ΔE 8.3（沒有因為多一色
 *     而變差），紫↔綠這一對比它寬鬆。唯一的 WARN 是深色模式下保護區紫的 2.43:1，
 *     那是加入 CONSERVATION_COLOR 時就知道並接受的（見上）。
 *
 * ## 這個顏色是算出來的，不是挑出來的
 *
 * 前三色（藍／橘／洋紅）已經把色相空間佔掉大半，第四色的可行區間非常窄。用
 * OKLCH 掃過整個色域（色相每 2.5°、L 0.44–0.78、C 0.10–0.30，逐點丟進
 * validate_palette.js 跑明暗兩模式的 all-pairs），**零 WARN 的候選只剩 21 個**，
 * 集中在兩處：色相 150–162° 的一段綠，以及兩個彩度極低、當地圖線太虛的淡紫。
 * 直覺上會先想到的選擇全都不合格：
 *
 *   - 紫 `#7a3fa6`／`#8335c3`／`#7b43b9` —— dark 模式對比只有 2.56–2.77:1（WARN），
 *     跟當初 relief 拒絕紫色是**同一個**原因，不是新問題。
 *   - 青綠 `#00857a`、`#007a8a` —— 直接 FAIL。
 *   - 綠 `#009e73` —— 對洋紅的 deutan ΔE 只有 6.2（WARN）。
 *
 * `#2da26d` 是那段綠裡兩個 binding 條件（對洋紅的 CVD 8.3 > 8.0、淺色模式對比
 * 3.15 > 3.0）餘裕最平衡的一點。**要換色請重跑掃描，不要憑感覺往旁邊挪**——
 * 這一格四周就是 WARN。
 *
 * ## 為什麼綠色在這裡可以，在 relief 卻不行
 *
 * 山脈被禁用綠色的理由是「NLSC 通用電子地圖的山區底色就是綠的，山脈線畫成綠色
 * 等於畫在它自己要說明的那片地形上看不見」。交通軸線的處境相反：它們絕大部分
 * 路段走在西部平原與海岸走廊，NLSC 在那裡是白／灰底；而且這一層的教學重點正是
 * 「路線**繞開**山地」。少數穿山路段（國道五號雪山隧道、北迴線、南迴線）是飽和
 * 翠綠對上 NLSC 的淡黃綠山區暈渲，色相與彩度都拉得開。
 */
export const TRANSPORT_COLOR = "#2da26d";

/**
 * 十條交通軸線的顏色，分成**四族**。
 * **全站第一個不參與線／面色票 all-pairs 的分類圖層。**
 *
 * （這一整段記錄的是當初七條分三族時的推導。第四族「橫貫公路」為什麼只有一個
 * 顏色、而不是照樣三階，寫在下面 `CROSS_ISLAND_CYAN` 的說明裡——那次掃描是在
 * **十個**同框色的限制下重跑的，結論不同。）
 *
 * ## 為什麼要放寬那條規則
 *
 * 七條軸線共用一個翠綠時，圖上分不出高鐵、國道與臺鐵——而「西部走廊五條路線幾乎
 * 重疊、東部只有一條鐵路」正是這一層要教的東西。但七個彼此分得開的顏色在現行規則
 * 下**不存在**，這是量出來的，不是試不出來：
 *
 * 用 validate_palette.js 同一組判準（OKLab ΔE、Machado CVD 矩陣、明暗兩模式）掃過
 * 整個 OKLCH 色域，在**保留現有五個線／面色**（藍／橘／洋紅／紫／磚紅）的前提下，
 * 全色域只剩三座色相孤島——146–209°、264–275°、304–322°——而能再塞進去的顏色數是：
 *
 *   - 允許 WARN 上線（＝本站現行實務，保護區紫就是這樣）：**4 色**
 *   - 對比兩模式都嚴格 ≥3:1：**3 色**
 *   - 再要求彩度 ≥0.13（地圖上 2.2px 細線畫得出來不糊）：**2 色**
 *
 * 上限的來源是四個約束疊加，其中**雙模式是最大的單一項**：淺色模式允許
 * L∈[0.43,0.77]、深色只允許 [0.48,0.67]，交集只剩 0.19，砍掉 44% 的亮度範圍；
 * 再疊上 CVD ΔE≥8、all-pairs ΔE≥15（難度隨 N² 成長）與彩度下限，可行區就這麼大。
 *
 * 所以這一層改用**三族色相 × 族內三階**，並宣告它不參與線／面色票的 all-pairs。
 * 代價逐條列在下面，圖層的 `notes` 也必須講出來。
 *
 * ## 三族錨在官方識別色的色相上（語意優先）
 *
 *   高鐵 朱紅 h30 ／ 國道 綠 h154（高速公路指示牌綠）／ 臺鐵 靛 h278
 *
 * **跨族 all-pairs：一般視覺最差 ΔE 29.1**（遠高於 15 的硬下限）。
 * **族內 `--ordinal`**（比照水庫、古蹟、人口色階的既有判例）：
 *   node <dataviz-skill>/scripts/validate_palette.js "#026838,#028d4d,#01b464" --ordinal --mode light|dark
 *   node <dataviz-skill>/scripts/validate_palette.js "#4803ea,#5f54fe,#8088fe" --ordinal --mode light|dark
 *   → 亮度單調 PASS、相鄰 ΔL 0.111（綠）／0.109（靛）皆 ≥0.06、色相跨度 0.2°／0.1°
 *     皆 ≤40°、最淺階對比 2.65:1／2.98:1 皆 ≥2.0
 *
 * ## 兩個色相刻意偏離官方色，那是實測逼出來的
 *
 *   - **臺鐵從官方 h254 移到 h278。** h254 時東部幹線對水系藍 `#2a78d6` 的
 *     ΔE 只有 **1.8**——那不是「撞色」，是同一個顏色，而鐵路與河川在花東縱谷
 *     與西部平原大量並行交錯。移到 h278 之後是 ΔE 11.2 / CVD 11.8。
 *   - **高鐵用朱紅 h30 而不是企業識別橘 h43。** h43 對行政區橘 `#d95926` 的
 *     ΔE 只有 **2.5**。h30 之後是 ΔE 10.3 / CVD 10.0。THSR 標誌本來就偏橘紅，
 *     語意損失很小。
 *
 * ⚠️ **要換色的話先重跑這兩個距離**，那是這組色票唯一真正緊的兩個約束。
 *
 * ## ⚠️ 已知且接受的衝突
 *
 *   - **高鐵朱紅 ↔ 國道綠：CVD ΔE 4.0。** 橘（紅）與綠正好橫跨紅綠軸，
 *     **任何「高鐵橘 ＋ 國道綠」的組合在色盲下都是 ΔE 2.8–6.5，永遠低於 8**
 *     ——這是數學事實，不是搜尋不夠努力。掃過全色域，把高鐵推亮橘反而更糟（2.8）。
 *     **補償是線型**：軌道虛線、公路實線（見 `TRANSPORT_DASH`），比照斷層用線寬
 *     分第一／二類的既有判例。**那不是裝飾，是無障礙的必要條件，不要為了視覺
 *     乾淨拿掉。**
 *   - **國道深綠 `#026838` ↔ 斷層磚紅 `#8f463f`：CVD ΔE 3.9。** 同樣是紅綠軸。
 *     兩層同框時靠線型（斷層是實線但沒有標註、國道有 shortName 沿線標註）與
 *     地理分佈（斷層沿麓山帶與縱谷、國道一號走平原）區分。
 *   - **國道深綠 ↔ 等高線棕 `rgba(120,78,42,.55)`：ΔE 13.3。** 緩解是國道一號
 *     幾乎全程走西部平原，而等高線在 zoom 9 以下不畫、平原上也本來就稀疏。
 *
 * ## ⚠️ 帶 WARN 上線的對比（比照保護區紫 2.43:1 的既有判例）
 *
 *   `#ff7360` 淺色 2.60:1、`#01b464` 淺色 2.65:1、`#4803ea` 深色 2.05:1。
 *   接受的理由同保護區紫：色塊只出現在圖例與圖層抽屜，而且一律緊接著軸線名稱
 *   的文字，從來不是唯一的辨識線索。
 */
/**
 * 三條橫貫公路（臺7／臺8／臺20 線）共用的青。**這一族刻意只有一個顏色。**
 *
 * ## 為什麼不是「第四族色相 × 族內三階」
 *
 * 那是先試過而且**量出來做不到**的。用上面同一組判準（OKLab ΔE、Machado CVD、
 * 明暗兩模式）掃過整個 OKLCH 色域，找一組 L 0.455／0.565／0.675 的三階（＝綠族
 * 與靛族的亮度剖面），要求對現有七條軸線與所有會同框的線／面色（山脈洋紅、
 * 保護區紫、水系藍、等高線棕、斷層磚紅、行政區橘）的一般視覺 ΔE 都 ≥15：
 *
 *   - **合格的候選是 0 個。**
 *   - 放寬成「只要跨族 ΔE ≥15」之後活下來的 36 組**全部落在 h 325–347 的洋紅帶**，
 *     而那一帶對山脈洋紅 `#c23f8f` 只有 ΔE 2–7——山脈是 `defaultOn`，而橫貫公路
 *     的整個中段就是騎在稜線上，這是全站最不能撞的一對。
 *   - 把目標改成「最大化對所有同框色的最小 ΔE」，全色域的最佳值是 **9.4**，
 *     離 15 的硬下限還差得遠。
 *
 * 三階做不到的原因跟七條那次一樣，只是更緊：現在要避開的是**十個**顏色。
 *
 * ## 一族一色在這裡是對的，不是退而求其次
 *
 * 西部走廊那五條需要各自一色，是因為它們**在同一條走廊上幾乎重疊**，位置分不出
 * 誰是誰。橫貫公路相反：北橫在北緯 24.7、中橫 24.2、南橫 23.1，**三條彼此相距
 * 上百公里、完全不重疊**，而且各自有沿線標註（「北橫」「中橫」「南橫」）與自己的
 * 核取方塊。色相在這裡本來就不是區辨通道，位置才是。
 *
 * ⚠️ 代價要知道：圖例會出現三列同色的色塊。那讀起來是「這三條是同一族」——那是
 * 事實，跟綠族三階讀起來都是「國道」是同一件事。
 *
 * ## `#04b4d2` 是掃出來的
 *
 * 限制在「單一顏色」之後可行區就打開了，落在 h 213–220、L≈0.70、C≈0.12 的青。
 * 對**所有**十三個同框色（七條軸線＋六個線／面色）：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#01b464,#04b4d2" --pairs all --mode light
 *   node <dataviz-skill>/scripts/validate_palette.js "#2a78d6,#04b4d2" --pairs all --mode light
 *   → 最緊的兩對（國道五號、水系藍）在淺色模式四項 PASS：
 *     國道五號 ΔE 15.9 / CVD 14.7、水系藍 ΔE 17.0 / CVD 17.0
 *
 * ⚠️ **加入這一色之後，跨族 all-pairs 的最差值從 ΔE 29.1 掉到 15.9**（對國道五號），
 * 也就是餘裕只剩 0.9。**要換色請重跑整個掃描**，不要憑感覺往旁邊挪——這一格四周
 * 就是 15 以下。CVD 最差值沒有變（仍是既有的高鐵↔國道三號 4.0）。
 *
 * ⚠️ **有一個顏色離它更近，但那是另一個主題的**：板塊邊界的錯動綠 `#159c6b`
 * （`PLATE_BOUNDARY_COLORS[2]`）對這個青只有 **ΔE 15.0**，剛好卡在硬下限上。
 * 今天仍然不成問題，因為 `plate-boundaries` 在「世界地理」主題、橫貫公路只在
 * 「臺灣地理」主題，而 `ThemeMapPage` 一次只算繪當前主題的圖層——兩者不可能同框。
 * （2026-08 把「全球地理形貌」併進「世界地理」時重新確認過這一點：板塊那一層換了
 * 主題，但仍然不在臺灣主題裡。）**⚠️ 要是哪天把板塊那一層搬進臺灣主題，這一對
 * 就會變成真的衝突**，屆時要重跑掃描，不是把 15.0 當成「反正過了」。
 *
 * ⚠️ **水系藍是這個顏色最需要盯著的一對**（ΔE 17.0）：三條橫貫公路整段都沿著河谷
 * 走（北橫沿大漢溪、中橫沿大甲溪與立霧溪、南橫沿荖濃溪與新武呂溪），實際上會跟
 * 河川線長距離並行。17.0 過得了下限，但比 `#0fadc9` 那個候選的 15.5 多出來的餘裕
 * 正是選它而不選更暗一階的理由。
 *
 * ## ⚠️ 兩項已知且接受的 WARN
 *
 *   - **淺色模式對比 2.42:1**（低於 3:1）。同保護區紫 2.43、高鐵朱紅 2.60、
 *     國道五號 2.65 的既有判例：色塊只出現在圖例與圖層抽屜，一律緊接著軸線名稱
 *     的文字，從來不是唯一的辨識線索。深色模式是 7.02:1。
 *   - **深色模式亮度帶 FAIL（L 0.709 > 0.67）**。國道五號 0.676 與南迴線 0.675
 *     也在帶外，那本來就是三階族最亮階的必然結果（所以族內驗的是 `--ordinal`
 *     而不是分類判準）。這一色再亮一點是「要跟水系藍拉開就得比它亮」換來的：
 *     掃描顯示把 L 壓回 0.67 以內時，唯一還能過 ΔE 15 的是對臺鐵靛 CVD ≈ 0 的
 *     電光洋紅，完全不能用。實際後果是深色面板上的色票更亮，對比 7.02:1，
 *     那一項本來就是在防過暗。
 *
 * ## 為什麼綠色不行（跟國道相反）
 *
 * 國道能用綠，理由寫在 `TRANSPORT_COLOR`：它們幾乎全程走西部平原，NLSC 在那裡是
 * 白／灰底。橫貫公路的處境正好相反——它們**整段中段都在 NLSC 的綠色山區底色上**，
 * 那正是當初禁止山脈線用綠的同一條理由。青在那片黃綠暈渲上色相拉得開，而且
 * L 0.71 比山區底色亮得多。
 */
const CROSS_ISLAND_CYAN = "#04b4d2";

export const TRANSPORT_COLORS = {
  /** 高鐵。朱紅——THSR 標誌本來就偏橘紅，且避開行政區橘。 */
  thsr: "#ff7360",
  /** 國道一號。綠族最深階。 */
  "freeway-1": "#026838",
  /** 國道三號。綠族中階。 */
  "freeway-3": "#028d4d",
  /** 國道五號。綠族最亮階。 */
  "freeway-5": "#01b464",
  /** 臺鐵西部幹線。靛族最深階。 */
  "tra-west": "#4803ea",
  /** 臺鐵東部幹線。靛族中階。 */
  "tra-east": "#5f54fe",
  /** 臺鐵南迴線。靛族最亮階。 */
  "tra-south-link": "#8088fe",
  /** 北部橫貫公路（臺7線）。橫貫公路族——三條共用同一個青，理由見下。 */
  "provincial-7": CROSS_ISLAND_CYAN,
  /** 中部橫貫公路（臺8線）。 */
  "provincial-8": CROSS_ISLAND_CYAN,
  /** 南部橫貫公路（臺20線）。 */
  "provincial-20": CROSS_ISLAND_CYAN,
} as const;

/**
 * 軌道類軸線的線型。**這是無障礙的第二通道，不是裝飾**——見上面「已知且接受的
 * 衝突」：橘綠在色盲下必然不可分，公路實線／軌道虛線是唯一補償。
 *
 * 值是 maplibre 的 `line-dasharray`，單位是**線寬的倍數**（不是像素），所以
 * 選取強調把線寬 ×2.2 之後虛線比例不變。
 */
export const TRANSPORT_DASH: [number, number] = [2.4, 1.6];

/**
 * 活動斷層（線）。磚紅。
 *
 * ## 這是線／面色票的**第六色**，而且它真的過了
 *
 * CLAUDE.md 一度把線／面五色記錄為「已飽和」——那個結論來自一次**格點太粗的掃描**
 * （L 每 0.06、C 從 0.10 起跳），只掃出一個跟保護區紫撞在一起的淡紫。把 L 的步進
 * 降到 0.005 之後才發現 L≈0.485 這一條窄縫是通的：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#2a78d6,#d95926,#c23f8f,#7538ae,#2da26d,#8f463f" --pairs all --mode light|dark
 *   → **六色 all-pairs 兩模式全數 PASS**。一般視覺最差是 `#8f463f↔#c23f8f` ΔE 15.8
 *     （剛好過 15 的硬下限）、CVD 最差仍是既有的翠綠↔洋紅 8.3。
 *     唯一的 WARN 是深色模式對比 2.58，跟保護區紫的 2.43 同一類、同一個既有判例。
 *
 * ⚠️ **餘裕非常小（15.8 對下限 15），往任何方向挪都可能掉出去。** 要改色請重跑
 * 上面那條完整六色指令，而且 L 的掃描步進不要大於 0.005——粗掃會讓你以為無解。
 *
 * ## 為什麼是紅色
 *
 * 官方「臺灣活動斷層分布圖」與課本畫斷層就是紅的，語意最強。⚠️ 它跟古蹟赭紅
 * `#934c3a` 很近，但古蹟是**圓點**、斷層是**線**，跨幾何本站不驗（既有規則），
 * 而且兩者地理分佈相反——古蹟在市區與平原，斷層沿山前與縱谷。
 *
 * 第一類／第二類**不另外給顏色**：那是序位，用**線寬**當第二通道（第一類 2.4px、
 * 第二類 1.2px），可點清單也依類別分組。實測沒有任何一組「深紅／淺紅」兩階能讓
 * 兩階都通過——淺的那階對行政區橘 `#d95926` 的一般視覺 ΔE 一定掉到 15 以下。
 */
export const FAULT_COLOR = "#8f463f";

/**
 * 非分類的固定色角色——比照 hillshade 的棕色與等高線的棕色，
 * **刻意排除在分類色票之外**，也不參與 all-pairs 驗證。
 *
 * `reference`（緯度參考線）是地圖的參考家具，不是一個要跟圖例比對色相的類別，
 * 所以用中性灰＋虛線；虛線本身就是製圖上「這是參考線不是實體」的慣例。
 *
 * `hazard`（地震帶）是**密度場**而不是分類圖層：教學內容是「地震帶沿板塊邊緣
 * 浮現」，不是「這個色相代表地震」。給它一個分類色相不但會擠爆色票驗證，
 * 2800 個不透明白框圓點在教室投影機上也只會是一坨看不懂的東西。
 * 所以用半透明中性色 + 依震級驅動半徑 + 不畫外框。
 */
export const REFERENCE_COLOR = "#5a5852";
export const HAZARD_COLOR = "#3a3a3a";

/**
 * 行星風系（`wind-belts`）的板岩藍。同樣是**非分類的固定角色**，不參與色票驗證。
 *
 * ## 為什麼整層只有一個顏色
 *
 * 這一層有四個核取方塊（氣壓帶、信風、西風、極地東風），但它們是**同一個系統的
 * 四個部位**，不是四個要互相比較的類別——三胞環流是一件事。區辨靠的是形狀與位置：
 * 氣壓帶是點線的短線段、風帶是箭頭，而三個風帶各自待在自己的緯度帶裡，每一個都
 * 帶著沿線標註寫出自己的名字（比照三條橫貫公路共用同一個青的既有判例）。
 *
 * ## 為什麼是低彩度的板岩藍，而不是課本的紅（高壓）／藍（低壓）
 *
 * 因為色相空間在這個主題裡已經滿了。`MAX_ACTIVE_BY_KIND.line` 是 3，所以這一層
 * 可以跟緯度參考線＋板塊邊界（橘／藍／綠）同時打開；掃過整個 OKLCH 色域，能跟
 * 那三個邊界色一起通過 all-pairs 又零 WARN 的**只剩色相 290–360° 的紫／洋紅**
 * ——而那一族已經被火山帶的洋紅 `#c0259c` 佔走了。
 *
 * 所以這一層改走「非分類角色」那條路（比照板塊的暖褐與地震帶的中性灰）：
 * 彩度只有 0.066，**低於分類色的下限 0.10**，本來就不是拿來跟人比色相的顏色。
 * 選 `#5a6f96` 而不是更深的藍：實測明暗兩模式對面板底色分別是 4.93:1 與 3.44:1
 * （兩邊都過 3:1，不必像保護區紫那樣欠一筆 relief），而且它離參考線灰
 * `#5a5852` 有 ΔE 11.1——**這一項是必要的**，兩層的線在 0°／±30°／±60° 完全重疊，
 * 顏色分不開就會看成同一條線。
 */
export const WIND_COLOR = "#5a6f96";

/**
 * 板塊面（`plates`）的顏色。同樣是**非分類的固定角色**，不參與色票驗證。
 *
 * 理由跟 reference／hazard 一樣是「它不是一個要跟別人比色相的類別」：52 塊板塊
 * **鋪滿整個地球**，每一塊都同色——這一層要教的是界線的位置與各塊的名字，不是
 * 「哪個顏色代表板塊」。
 *
 * ⚠️ **因此 `fillOpacity` 是 0，那不是忘了設。** 面既然覆蓋每一個像素，任何均勻的
 * 底色都只是把整張地圖壓暗一階，資訊量是零。這個顏色實際畫出來的是**外框**（板塊
 * 界線）與**圖例色塊**，以及選取某一塊時才浮現的 0.38 面染——那個「點一下看這塊
 * 板塊有多大」才是這一層真正的互動。
 */
export const PLATE_COLOR = "#7d6b4f";

/**
 * 大洲分區（`world-continents`）的梅紫。**非分類的固定角色**，比照板塊的暖褐。
 *
 * ## 為什麼七大洲不是七個顏色
 *
 * 想都不用想：本站掃過整個色域，分類色的上限是**六色**（生物群系那一組），七個
 * 分類色不存在可解的組合。而且七大洲**本來就不需要靠顏色分辨**——它們彼此不相鄰
 * （除了歐亞與亞非那兩條界線），每一塊都在自己的位置上、還帶著自己的名字標註。
 * 所以這一層跟板塊一樣：同一個顏色，畫出來的是**外框與名字**，
 * 「這一洲有多大」交給選取時那層 0.38 的面染。
 *
 * ## 掃描找出可行的色相，實測決定明暗
 *
 * 條件是「跟世界主題會同框的每一個顏色都分得開」，而那個名單很長：非分類角色
 * （板塊暖褐、參考線灰、風系板岩藍、地震帶灰）＋ 面色票 11 色（生物群系六、
 * 柯本五）＋ 線點色（水系藍、三種板塊邊界、暖寒流、火山洋紅）。掃遍 OKLCH
 * （色相每 2.5°、L 0.44–0.70、彩度 0.02–0.22）之後，能同時離這些顏色都遠的
 * **只剩色相 320–350° 的紫紅／玫瑰那一族**，其餘色相全部撞在某一個既有顏色上。
 *
 * ⚠️ **亮度是實測決定的，不是取掃描的最佳值。** 一般視覺最差值的最佳解在
 * L≈0.67（`#b581b0`，最差 15.3、對板塊暖褐 17.5），但那個亮度**在地圖上讀不出來**
 * ——實際疊在 OpenFreeMap Liberty 上（淺米色的陸地、藍色的海）看過三種亮度：
 * 世界視角下 `#b581b0` 的外框像是「海岸線被染了一點粉紅」，烏拉山、蘇伊士這幾條
 * 洲界幾乎看不見，正是全球地震帶那次「勾了圖層卻看不到東西」的同一種毛病。
 * 所以取 L≈0.56 這一格：
 *
 *   #a05a80 對……板塊暖褐 ΔE 11.7、參考線灰 14.6、風系板岩藍 11.9、火山洋紅 11.8、
 *           針葉林 11.5（最差）、柯本 D 12.6；對 Liberty 的陸地底色 ΔE 38
 *   contrast：淺色模式 4.79:1、深色模式 3.54:1（**兩邊都過 3:1，不必欠 relief**）
 *
 * ⚠️ 最差值 11.5 **低於分類色票的 15**，那是刻意的取捨，判例是 `WIND_COLOR`
 * （對參考線灰 11.1）與保護區紫（帶著對比 WARN 上線，因為乾淨的替代色在
 * NLSC 山區底圖上看不見）：**驗證器量不到「疊在底圖上讀不讀得出來」**，而那一項
 * 在這一層是存亡問題。補償有三層：這一層的 `fillOpacity` 是 0（畫出來的是
 * 1.8px 的線，不是一片色塊）、每一洲都有自己的名字標註、圖例與抽屜的色塊一律
 * 緊接著圖層名稱的文字。
 *
 * ⚠️ 要改這個顏色，兩件事都要做：把上面那份同框名單整份重掃，**而且**在
 * Liberty 底圖的世界視角實際疊一次（比照保護區紫要在 NLSC 山區疊一次）。
 */
export const CONTINENT_COLOR = "#a05a80";

/**
 * 三種板塊邊界的分類色（`plate-boundaries` 的 `items.palette`）。
 *
 * ⚠️ **這一組不參與線／面色票的 all-pairs**，比照交通軸線的既有判例。
 *
 * ## ⚠️ 2026-08 合併主題之後，這個豁免的理由已經改了一半
 *
 * 原本的理由是「同框對象一個彩色分類色都沒有」：全球地理形貌主題裡其餘的線圖層
 * 只有緯度參考線與國際換日線（`reference` 中性灰）、板塊面的暖褐外框與地震帶的
 * 中性灰圓點。**那句話現在不成立了**——全球地理形貌併進世界地理之後，這一層第一次
 * 跟**世界主要河流**（水系藍 `#2a78d6`，`hydrology`）同框，而且：
 *
 *   聚合藍 `#2f74c9` ↔ 水系藍 `#2a78d6`：一般視覺 ΔE **2.1**、CVD 2.1
 *
 * 那不是「相近」，是同一個顏色。而且它們真的會疊在一起——喜馬拉雅、安地斯、
 * 阿爾卑斯、札格洛斯這些聚合帶正好是大河的源頭。
 *
 * ### 換色救不了，這是掃出來的
 *
 * 水系藍動不得（它同時是臺灣主題那組已驗證六色線／面色票的一員，也是點色票的
 * `place`）。所以掃的是「換掉聚合藍」：固定張裂橘＋錯動綠＋水系藍＋火山洋紅
 * （火山必須一起驗，理由見 `VOLCANO_COLOR`），掃遍整個 OKLCH 色域（色相每 2.5°、
 * L 0.34–0.82、彩度 0.09–0.30）——
 *
 *   - 硬性全過（含亮度帶與對比）的候選**只剩色相 0–15 的緋紅、85 的橄欖，
 *     與 265–357 的紫／洋紅**，一個藍都沒有。那是必然的：任何藍都會撞水系藍。
 *   - 放寬到「只保住一般視覺 15 的硬下限」之後，藍色端活下來兩族，都不能用：
 *     **h 277–285 的藍紫**（`#5e01f0` 一帶）對寒流藍 `#133eec` 掉到 **9.5**，
 *     等於把衝突從河流搬到洋流；**h 200／L 0.82 的淡青**（`#34dde5` 一帶）
 *     畫在底圖的藍色海面上根本看不見。
 *
 * ### 所以解法是**線型**，不是顏色
 *
 * 板塊邊界改畫虛線 `[4, 1.5]`（見 themes/world.ts 的 `PLATE_BOUNDARY_DASH`），
 * 河流維持實線。這是本站既有的第二通道判例——交通軸線的軌道虛線／公路實線也是
 * 「色相在色盲下必然不可分，只能靠線型補償」。⚠️ **要改回實線，就得先解掉上面
 * 那個 ΔE 2.1。**
 *
 * 臺灣主題的那六個線／面色仍然**不可能**與這一組同時出現（`ThemeMapPage` 只算繪
 * 當前主題），那一半的理由沒有變。
 *
 * 組內 all-pairs 則是實際驗過的，明暗兩模式**五項全數 PASS、零 WARN**：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#c95c1c,#2f74c9,#159c6b" --pairs all --mode light|dark
 *   → CVD 最差 8.8（綠↔橘，deutan）、一般視覺最差 21.2（綠↔藍）
 *
 * ⚠️ 橘↔綠的 CVD 8.8 落在 6–8 以上但不算寬裕，**兩者靠得住的第二通道是圖例與
 * 子項目名稱**（三個核取方塊各自帶著「張裂型／聚合型／錯動型」的文字）。
 * 換色前先重跑上面那條指令。
 *
 * ⚠️ 綠色在臺灣主題是禁忌（NLSC 底圖的山區底色就是綠的），這裡可以，理由與交通
 * 軸線相同——**地理分佈相反**：板塊邊界絕大多數在洋底（中洋脊、海溝、轉形斷層），
 * 而世界底圖在海上是藍的。改色前先在 zoom 2 的太平洋實際看一次。
 */
export const PLATE_BOUNDARY_COLORS = ["#c95c1c", "#2f74c9", "#159c6b"] as const;

/**
 * 全球活火山（`volcanoes`）的圓點色。
 *
 * ## ⚠️ 這一個顏色**必須跟板塊邊界的三個線色一起驗**，跨幾何豁免在這裡不成立
 *
 * 本站的既有規則是「三組獨立色票、跨幾何的配對不驗證」（形狀就在區辨），而且
 * 古蹟赭紅（圓點）與活動斷層磚紅（線）就是靠這條豁免共存的——但那兩層的理由是
 * **地理分佈相反**（古蹟在市區、斷層沿山前）。火山正好相反：**它們幾乎全部長在
 * 板塊邊界線上**，那是這一層要教的第一件事。所以圓點與線在畫面上是真的重疊的，
 * 豁免不能用，要跟三種邊界一起跑 all-pairs：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js \
 *     "#c95c1c,#2f74c9,#159c6b,#c0259c" --pairs all --mode light|dark
 *   → 兩模式五項全數 PASS、零 WARN（CVD 最差仍是既有的綠↔橘 8.8，
 *     一般視覺最差 21.2，都沒有因為多一色而變差）
 *
 * ## 為什麼不是紅色（製圖慣例）
 *
 * 因為**張裂型邊界已經佔走了整個暖色端**。實測紅色 `#d1352b` 對橘 `#c95c1c`
 * 的一般視覺 ΔE 只有 **7.3（hard FAIL，下限 15）**、deutan 3.2——冰島與東非
 * 大裂谷那一帶的紅點會直接融進橘線裡。掃過整個 OKLCH 色域（色相每 2.5°、
 * L 0.48–0.67＝明暗兩模式亮度帶的交集、彩度 0.10–0.26）後，**零 WARN 的候選
 * 只剩色相 285–355° 的紫／洋紅那一族**；深紅 `#ba054a` 雖然過得了 all-pairs，
 * 但深色模式對比只有 2.66 要多欠一筆 relief，而這裡有乾淨的替代品可選
 * （比照保護區紫那次的判準：有乾淨替代品就不要帶 WARN 上線）。
 *
 * 顏色與「火山」的對應靠圖例與圖層名稱的文字，不是靠「像不像」——比照茶葉配藍
 * 的既有判例（`CROP_COLORS`）。
 *
 * ⚠️ 它跟山脈洋紅 `#c23f8f`（relief）同一族，這是**刻意可以**的：兩者不同幾何、
 * 不同主題，`ThemeMapPage` 只算繪當前主題（比照地形景點藍與水系藍共用同一個
 * hex 的既有做法）。順帶一提 `#c23f8f` 本身在這裡是**不夠好**的：它對錯動型
 * 邊界的綠只有 deutan 6.7，落在「只有搭配次要編碼才合法」的 6–8 band。
 */
export const VOLCANO_COLOR = "#c0259c";

/**
 * 洋流的兩個分類色（`ocean-currents` 的 `items`）：暖流紅、寒流藍。
 *
 * ## 組內驗證：兩模式五項全數 PASS、零 WARN
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#b00a1d,#133eec" --pairs all --mode light|dark
 *   → 一般視覺 ΔE 39.1、CVD 最差 32.9（兩項都是全站最寬鬆的一組）
 *
 * 對「這一層真的會一起讀」的兩個中性線色也量過（兩者都是非分類的固定角色，
 * 本來就不參與色票驗證，但緯度參考線與行星風系跟洋流是同一組教學搭配，
 * 分不開就沒有意義，比照 `WIND_COLOR` 當初對參考線灰量 ΔE 11.1 的既有做法）：
 *
 *   暖流／寒流 ↔ 參考線灰 `#5a5852`、風系板岩藍 `#5a6f96`：最小 ΔE **18.8**
 *
 * ## ⚠️ 這一組**不參與板塊邊界那三個線色的 all-pairs**，而且那是量出來的結論
 *
 * `MAX_ACTIVE_BY_KIND.line` 是 3，所以洋流**可以**跟「板塊邊界」同時打開。
 * 照理說該把五個線色一起驗——問題是**那樣就畫不出暖流／寒流**了。掃過整個
 * OKLCH 色域（色相每 2.5°、L 0.48–0.67＝明暗兩模式亮度帶的交集、彩度 0.10–0.32，
 * 共 10,046 × 7,691 組），在固定 `#c95c1c,#2f74c9,#159c6b` 的前提下能讓
 * **五色 all-pairs 全過**的候選只有一族：
 *
 *   暖 = 洋紅／緋紅（h 0–10，例如 `#ab0d5b`）、寒 = 藍紫（h 275–280，例如 `#5301f9`）
 *   ——而且**每一組都還帶著對比 WARN**（最好的一般視覺 ΔE 只有 18.2）。
 *
 * 也就是說：暖流不能是紅的、寒流不能是藍的。**紅＝暖、藍＝寒是這一層唯一要教
 * 的事**（圖層名字就叫「洋流（暖流／寒流）」），拿它去換一組互相分不開的紫，
 * 這一層就不值得做了。把限制放寬到「只保住一般視覺 15 的硬下限」也沒有用：
 * 真正的紅／藍組合要到暖 L 0.40（暗紅棕）配寒 L 0.78（極淡天藍）才過，
 * 而那個淡藍畫在底圖的藍色海面上等於沒畫。
 *
 * 所以走**柯本氣候分區 ↔ 森林與沙漠帶**那條既有判例：兩層可以同時打開，但兩組
 * 色票不可能一起驗，於是把事實寫進 `notes` 請使用者一次看一層，而不是為了讓它們
 * 「能一起看」去改壞其中一組。
 *
 * ⚠️ **已知且接受的衝突（換色時要重新量的就是這兩個數字）**：
 * 在能維持紅／藍的所有組合裡，`#b00a1d` + `#133eec` 是離板塊邊界**最遠**的一組，
 * 但仍然有兩對低於 15 的硬下限——
 *
 *   寒流藍 ↔ 聚合型邊界藍　ΔE **14.0**（CVD 10.0）
 *   暖流紅 ↔ 張裂型邊界橘　ΔE **14.1**（CVD 12.1）
 *
 * 兩對在地理上都是真的會碰到的：祕魯寒流貼著祕魯－智利海溝、親潮貼著千島海溝，
 * 兩段都是「藍線平行壓在藍線上」；紅↔橘則在中洋脊沿線。⚠️ 但**色盲下反而不是
 * 問題**（CVD 10.0 與 12.1 都遠高於 8），短的是一般色覺那一項。
 *
 * ⚠️ 2026-08 合併主題之後這一對**變好讀了，但不是因為顏色**：板塊邊界為了跟世界
 * 主要河流分開而改畫虛線（見 `PLATE_BOUNDARY_COLORS`），於是洋流的實線與它天生就
 * 有了線型差異。加上洋流有沿線標註寫出自己的名字（黑潮、祕魯寒流…）、板塊邊界
 * 沒有，兩個次要編碼疊起來已經夠用。**但 ΔE 沒有變，換色時仍然要看上面那兩個數字。**
 *
 * ⚠️ **不要把寒流改成更「正統」的藍**（h 245–255 那一族）：那正是聚合型邊界的
 * 色相，ΔE 會從 14.0 掉到 7.7（`#2358cf`）。`#133eec` 的 h 265 已經是「還讀得出
 * 是藍色、又離得最遠」的位置。
 *
 * ⚠️ 火山洋紅 `#c0259c` 與這兩色的最小 ΔE 是 17.9，過得了；圓點與線本站本來就
 * 不跨幾何驗（既有規則），這裡只是順手確認過。
 *
 * ## ⚠️ 2026-08 合併主題之後多出來的一對
 *
 * 「全球地理形貌」併進「世界地理」之後，這一層跟**世界主要河流**（水系藍
 * `#2a78d6`）同框：**寒流藍 ↔ 水系藍 ΔE 14.0（CVD 9.6）**，同樣低於 15。
 *
 * 這一對比板塊邊界那兩對**寬鬆得多**，理由是本站既有的「地理分佈相反」判準在這裡
 * 是乾淨成立的：**洋流全部在海上、世界主要河流全部在陸上，兩者一個像素都不會重疊**
 * （比照交通翠綠與保護區紫、古蹟赭紅與斷層磚紅）。而且兩層都有沿線標註寫出自己的
 * 名字。板塊邊界的情形相反——聚合帶就在大河的源頭上，所以那邊得動線型。
 */
export const OCEAN_CURRENT_COLORS = {
  /** 暖流。紅——製圖與課本的通用語彙，這一層的教學內容就是紅暖藍寒。 */
  warm: "#b00a1d",
  /** 寒流。藍——色相刻意推到 h 265，離聚合型邊界的藍最遠，見上。 */
  cold: "#133eec",
} as const;

/**
 * 六個生物群系大類的分類色（`biomes` 的 `items`，由赤道往極區排）。
 *
 * ## ⚠️ 六色是掃出來的上限，而且是本站唯一一組六色分類色票
 *
 * 上游 RESOLVE 有 **14 個生物群系**，但十四個分類色不可能通過驗證（dataviz 的規則
 * 是 8 個就要開始折疊，而本站實測四色就已經很緊）。所以在建置期併成六類
 * （併法與理由見 scripts/lib/biomes.mjs 的 `BIOME_CLASSES`），六色再一起驗：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js \
 *     "#25744c,#989401,#b4410d,#369db8,#695ba9,#c754f4" --pairs all --mode light|dark
 *   → 兩模式五項全數 PASS、零 WARN
 *   → CVD 最差 8.6（沙漠↔熱帶林，protan）、一般視覺最差 18.1（溫帶林↔熱帶林）
 *
 * 選色是**在語意色相窗內做的最佳化**，不是挑的：每一類先框一段色相（熱帶林
 * 135–175°、莽原 88–132°、沙漠 40–86°、溫帶林 176–220°、針葉林 235–290°、
 * 苔原 295–350°），亮度限制在明暗兩模式亮度帶的交集 L 0.48–0.67，再以
 * 「CVD ≥ 8.5 的前提下最大化一般視覺最差值」跑爬山法。⚠️ **一般視覺 15 是硬下限**
 * （低於它連正常色覺都分不出來，而且不能用次要編碼豁免），所以目標函數把餘裕留在
 * 那一項而不是 CVD 上——實測另一組解是 CVD 10.0／一般視覺 15.4，卡在硬下限旁邊。
 *
 * ⚠️ **溫帶林是藍綠、針葉林是靛紫，那不是配錯**：兩者的慣例色都是綠，但綠已經給了
 * 熱帶林，而三個綠彼此的 CVD 分離度不可能同時達標。比照茶葉配藍的既有判例
 * （`CROP_COLORS`）——顏色與類別的對應靠圖例與核取方塊上的文字，不是靠「像不像」。
 *
 * ⚠️ 沙漠的紅褐 `#b4410d` 跟張裂型邊界的橘 `#c95c1c` 是近鄰，但一個是 0.25 不透明度
 * 的面、一個是 1.6px 的實線（跨幾何不驗，見上）。**唯一真的會疊在一起的地方是東非
 * 大裂谷**（橘線壓在沙漠與莽原的面上），實測那一段仍然分得出來，改色前先在那裡看一次。
 *
 * ⚠️ 面的不透明度上限是 0.25（見 registry/types.ts：面疊在底圖地名之上），所以畫面上
 * 看到的是這六色**被底圖沖淡後**的樣子。色票驗證驗的是圖例與抽屜裡的色塊，那才是
 * 使用者用來對照的東西——這一點跟其他面圖層一致，不是這一層的例外。
 */
/**
 * 柯本氣候分區五大類的分類色（`koppen-zones` 的 `items`，依 A→E 排）。
 *
 *   node <dataviz-skill>/scripts/validate_palette.js \
 *     "#1d9dc8,#d8783d,#147811,#ba229e,#5e42fe" --pairs all --mode light|dark
 *   → 兩模式五項全數 PASS、零 WARN
 *   → CVD 最差 8.4（C 綠↔B 橙，protan）、一般視覺最差 23.3（E↔D）
 *
 * 選法跟生物群系那六色一樣：先框語意色相窗（A 藍 185–235°、B 橙 45–90°、
 * C 綠 120–170°、D 紫紅 290–340°、E 藍紫 240–280°），亮度限制在明暗兩模式亮度帶
 * 的交集 L 0.48–0.67，再以「CVD ≥ 8 的前提下最大化一般視覺最差值」爬山。
 * 色相窗刻意貼近 Kottek 原圖的慣例（A 藍、B 橙黃、C 綠、D 紫、E 冷色）。
 *
 * ⚠️ **30 個亞型不會各有一個顏色**：地圖畫的是五大類，亞型只在點下去的卡片上出現。
 * 三十個分類色沒有人分得出來，而本站掃出來的上限是六色（見 `BIOME_COLORS`）。
 *
 * ⚠️ **這一層跟「森林與沙漠帶」蓋的是同一片陸地。** `MAX_ACTIVE_BY_KIND.fill` 是 2，
 * 所以兩層**可以**同時打開——但兩片 0.25 的半透明面疊起來就是一團糊，兩組色票也
 * 不可能一起驗（11 個分類色）。這件事寫在圖層的 notes 裡提醒使用者一次看一層；
 * **不要為了讓它們「能一起看」而去調亮度或不透明度**，那只會讓兩層都變難讀。
 */
export const KOPPEN_COLORS = {
  a: "#1d9dc8",
  b: "#d8783d",
  c: "#147811",
  d: "#ba229e",
  e: "#5e42fe",
} as const;

export const BIOME_COLORS = {
  "tropical-forest": "#25744c",
  savanna: "#989401",
  desert: "#b4410d",
  "temperate-forest": "#369db8",
  boreal: "#695ba9",
  tundra: "#c754f4",
} as const;

/**
 * 四片專屬經濟海域的分類色（`tw-eez` 的 `items.palette`）。
 *
 * ## ⚠️ 線／面色票**沒有**第 7 色可用，這是掃出來的，不是懶得找
 *
 * 用 dataviz 的 `validate_palette.js` 掃過整個 OKLCH 色域（L 0.48–0.67 步進 0.005、
 * C 0.10–0.26 步進 0.01、色相步進 2°），在保留既有六色
 * （`#2a78d6,#d95926,#c23f8f,#7538ae,#2da26d,#8f463f`）的前提下：
 *
 * - **五項全數 PASS 的候選是 0 個。**
 * - 放寬到允許 WARN 只剩 **109 個**，而且**全部**擠在色相 290–312 的電光紫，
 *   也就是保護區紫 `#7538ae` 旁邊——那不是一個能拿來當第七個分類色的位置。
 * - 直覺會先想到的海洋青直接 **FAIL**：`#0d7d8f` 彩度只有 0.092（下限 0.10）、
 *   對水系藍一般視覺 ΔE 11.8（下限 15）、對山脈洋紅 CVD ΔE 5.6（下限 8）。
 *
 * ## 所以這一組宣告**不參與**線／面色票的 all-pairs
 *
 * 比照 `PLATE_BOUNDARY_COLORS` 與 `TRANSPORT_COLORS` 的既有判例，而理由是本站用過
 * 兩次的那個「**地理分佈相反**」的最強形式：**這四片一個像素都不在陸地上**，而臺灣
 * 主題其餘每一個線／面分類色全部在陸上（縣市界、鄉鎮界、流域分區、保護區、河川、
 * 交通軸線、活動斷層）。圖例與圖層抽屜的色塊也一律緊接著名稱文字。
 *
 * ### ⚠️ 這個豁免的代價是量出來的，不要只讀上面那句定性的話
 *
 * 比照 `PLATE_BOUNDARY_COLORS` 在 2026-08 合併主題之後被迫重新推導的教訓（見上面那段：
 * 「同框對象沒有彩色分類色」那句話會隨著主題改組而失效），這裡把**實際距離**記下來。
 * 對臺灣主題其餘線／面色的一般視覺 OKLab ΔE（×100）：
 *
 *   EEZ 綠   `#33a73a` ↔ 交通翠綠 `#2da26d`   **6.4**
 *   EEZ 海藍 `#0c78a4` ↔ 水系藍   `#2a78d6`   **8.1**  ← 跟板塊邊界那次同一個坑
 *   EEZ 赭褐 `#a7560e` ↔ 斷層磚紅 `#8f463f`   **8.3**
 *   EEZ 赭褐 `#a7560e` ↔ 行政區橘 `#d95926`   **9.9**
 *   EEZ 淡紫 `#b066da` ↔ 山脈洋紅 `#c23f8f`  **12.6**（山脈是 `defaultOn`）
 *   EEZ 淡紫 `#b066da` ↔ 保護區紫 `#7538ae`  **16.2**（唯一過 15 的一對）
 *
 * **除了最後一對，每一對都低於 15 的硬下限**，所以這個豁免不是「反正也差不多」——
 * 它是真的把區辨完全押在地理分隔上：那四片全在海上，上面每一個對手全在陸上，兩者
 * 從不重疊，最接近的一組是日本那片的西界與臺灣東岸的河口，中間還隔著臺灣自己那片赭褐。
 *
 * ⚠️ **所以有一件事會讓這個豁免當場失效：在臺灣主題新增任何畫在海上的線／面圖層**
 * （海底電纜、航道、漁區、海域的保護區都算）。真要加的話，這四色必須跟它一起重驗，
 * 不能沿用這一段。
 *
 * ⚠️ 上面那組數字是直接算 OKLab ΔE 得到的（**沒有跑 CVD 模擬**，當時手邊沒有驗證器）。
 * 既然多數已經低於一般視覺的下限，CVD 只會更低、不會翻案；但哪天要把其中一色搬進
 * 正式色票，就得跑完整的 `validate_palette.js`。
 *
 * 組內 all-pairs 則是實際驗過的，明暗兩模式**五項全數 PASS、零 WARN**：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#a7560e,#0c78a4,#33a73a,#b066da" --pairs all --mode light|dark
 *   → CVD 最差 8.7（淡紫↔海藍，protan）、一般視覺最差 21.5（同一對）
 *
 * ⚠️ 綠色在臺灣主題原本是禁忌（NLSC 底圖的山區底色就是綠的），這裡可以，理由與
 * 交通軸線用綠、古蹟用赭紅完全相同——**這一層一個像素都不在陸地上**。
 *
 * ⚠️ **但 NLSC 的海不是淡藍白底，是一整片飽和的水深分層藍**（實測開放海域約
 * rgb(56,115,204)），所以這一組的**藍**（日本）疊上去必然是四色裡最弱的一個。
 * 那是量過之後接受的取捨，換色救不了——完整的實測數字與替代方案見 taiwan.ts
 * `tw-eez` 的 `render` 註解。改色之後要重新在 NLSC 底圖 zoom 6 量一次。
 *
 * ⚠️ 這一組是**掃出來的最佳解**，不是挑的：在「L 0.50–0.64、C 0.11–0.18」的
 * 溫和區間裡，固定「暖色／海藍／綠／洋紅紫」四個色相族去最大化最差配對，
 * 全域最佳就是這一組。**要換色請重跑上面那條完整四色指令**，不要憑感覺往旁邊挪。
 *
 * ⚠️ 顏色**固定綁在海域上**（`LayerItem.color`），不是依勾選順序從 palette 指派
 * ——比照古蹟三級與交通軸線。先勾菲律賓再勾臺灣時，臺灣仍然必須是赭褐。
 */
export const EEZ_COLORS = {
  taiwan: "#a7560e",
  japan: "#0c78a4",
  philippines: "#33a73a",
  senkaku: "#b066da",
} as const;

/**
 * 颱風強度的級距色（ordinal ramp），用在颱風路徑上每 6 小時一筆的中心定位點。
 *
 * ## 級距是官方的，不是自己切的
 *
 * 四段就是**中央氣象署的颱風強度分級**（近中心最大風速，10 分鐘平均）：
 * 熱帶性低氣壓 < 17.2、輕度 17.2–32.6、中度 32.7–50.9、強烈 ≥ 51.0 公尺/秒。
 * 學生課本上背的就是這四級，所以圖例逐字用官方名稱。
 *
 * ## ⚠️ 為什麼只驗 `--ordinal`，不驗分類點色票的 all-pairs
 *
 * 因為**分類點色票已經真的飽和了，這次是量出來的**：拿人口那次的驗證組合
 * （`#2a78d6,#e34948,#934c3a,#87519e`）再加任何第五色跑 `--pairs all`，掃過
 * 整個 OKLCH 色域（色相每 2.5°、五種彩度、兩種亮度剖面，共 712 個先通過
 * `--ordinal` 的候選）**沒有任何一組不 FAIL**。第五個分類點色相是不存在的。
 *
 * 所以走**水庫蓄水率那條既有判例**：circle 的序位色階只驗 `--ordinal`，
 * 分類分離度改由「這一層實際會跟誰同框」來判斷（水庫那次也沒有跟點色票 all-pairs）。
 * 這一層的群組同伴是「臺灣地震」與「重大地震」——兩個都是 `hazard` 中性色，
 * 本來就不參與色票驗證——以及「活動斷層」（線，跨幾何本站不驗）。
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#e79e80,#d35f2a,#af4611,#873203" --ordinal --mode light|dark
 *   → 兩個模式四項全數 PASS（單一色相、亮度單調、相鄰 ΔL ≥ 0.06、淺端對底色 2:1 以上）
 *
 * ## 為什麼是橙紅，不是綠、洋紅或琥珀
 *
 * 掃描結果裡分離度更高的其實是**綠**（h 128–158），但綠在這個站上有前科：NLSC 通用
 * 電子地圖的山區底色就是綠的（山脈線與保護區都因此不能用綠），而颱風路徑最關鍵的
 * 那一段正好在山上。**洋紅**（h 340）對兩種棕色底最遠，但它會跟五大山脈的稜線色
 * `#c23f8f` 撞——山脈是 `defaultOn`，而颱風就是從稜線上橫越過去的。**琥珀**（h 68）
 * 對等高線棕 `rgba(120,78,42,.55)` 的 ΔE 只有 3.7，是所有候選裡最糟的。
 *
 * 橙紅在這三件事上都站得住：對等高線棕 6.6、對地形陰影 9.3、對山脈洋紅夠遠，
 * 而且「暖色 = 強度」是氣象圖的通用語彙。最接近的兩個是古蹟中階 `#934c3a`（6.0）
 * 與原住民族紅 `#e34948`（6.2）——古蹟的 `minzoom` 是 9，在看颱風路徑的
 * zoom 4–8 根本不算繪；原住民族是內陸的 16 個點，跟海上的路徑點幾乎不會擠在一起。
 * ⚠️ **要換色請重跑上面那條指令，並且先確認新色相不會撞到山脈洋紅與 NLSC 的綠。**
 *
 * ⚠️ **刻意不宣告 `nodata`**：實測 757 個定位點**每一筆都有風速**（缺值的是氣壓，
 * 340 筆）。宣告了只會在圖例上多一列永遠不會出現的類別（比照人口密度那條 ramp）。
 */
export const TYPHOON_INTENSITY_RAMP = {
  property: "wind",
  steps: [
    { below: 17.2, color: "#e79e80", label: "熱帶性低氣壓" },
    { below: 32.7, color: "#d35f2a", label: "輕度颱風" },
    { below: 51.0, color: "#af4611", label: "中度颱風" },
    { below: null, color: "#873203", label: "強烈颱風" },
  ],
} as const;

/**
 * 水庫蓄水率的級距色（ordinal ramp）。
 *
 * ## 為什麼這一層可以用「顏色跟著數值走」
 *
 * 別的圖層顏色代表**圖層身分**，選取狀態只准動尺寸與外框（見 layers/geo.ts）。
 * 水庫是這條規則的一個明確例外，理由跟地震帶用震級驅動半徑是同一個：`percent`
 * 是這個圖徵**自己的資料屬性**，不是 UI 狀態。而且這一層存在的理由就是即時水情
 * ——全部畫成同一顆藍點，「哪幾座水庫快見底了」就得一顆一顆點開才看得到，
 * 那正是這個圖層要回答的問題。
 *
 * ## 為什麼是單一色相由淺到深，不是紅→綠
 *
 * dataviz 的規則：sequential／ordinal ＝**一個色相**，由淺到深；紅綠交通號誌那種
 * 配色是 status 用的保留色，而且對紅綠色盲最不友善。單一藍色相在語意上也對：
 * 深藍＝水多，本來就是製圖上水深的畫法。它同時讓水庫留在「藍色＝水系」的家族裡，
 * 跟橘色的行政區、洋紅的山脈不會混淆。
 *
 * ## 驗證
 *
 * 級距是離散的圓點（不是連續 heatmap），所以用驗證器的 **`--ordinal`** 模式，
 * 不是分類色的 `--pairs all`——後者會把一條正確的 ramp 判 FAIL（它本來就橫跨
 * 整個亮度帶、相鄰步階刻意相近），那是設計如此，不是真的失敗。
 *
 *   node scripts/validate_palette.js "#86b6ef,#3987e5,#256abf,#184f95" --ordinal --mode light|dark
 *   → 兩模式各四項全數 PASS（單一色相、亮度單調、步階間距 ≥ 0.06、淺端 2.06:1）
 *
 * 色階直接取自 dataviz 參考色票的藍 ramp step 250／400／500／600。**不要往兩端
 * 再延伸**：step 200 `#9ec5f4` 在淺色模式只有 1.74:1、step 700 `#0d366b` 在深色
 * 模式只有 1.46:1，都低於 2:1 的下限（圖層抽屜與圖例的色塊是畫在會跟著主題
 * 變色的面板上，所以兩個模式都要過）。
 *
 * 「暫無資料」的灰刻意**不在 ramp 裡**：它是資料缺漏，不是「蓄水率很低」，
 * 混進色階等於謊報。`#7d7c76` 在明暗兩個面板上分別是 4.08:1 與 4.16:1。
 */
export const RESERVOIR_FILL_RAMP = {
  property: "percent",
  /** 由低到高。`below: null` 是最後一段（開放上界——滿庫溢流時會超過 100%）。 */
  steps: [
    { below: 20, color: "#86b6ef", label: "未滿 20%" },
    { below: 50, color: "#3987e5", label: "20–50%" },
    { below: 80, color: "#256abf", label: "50–80%" },
    { below: null, color: "#184f95", label: "80% 以上" },
  ],
  nodata: { color: "#7d7c76", label: "暫無資料" },
} as const;

/**
 * 古蹟三級的赭紅色階（國定／直轄市定／縣(市)定）。**由淺到深＝級別由低到高。**
 *
 * ## 為什麼是色階而不是三個分類色
 *
 * 指定級別是**序位**（國定 > 直轄市定 > 縣市定），不是三個平等的類別。序位就該用
 * 單一色相由淺到深，這跟水庫蓄水率是同一條規則（見上），也因此**不佔用分類點色票**
 * ——那份色票（地形景點藍、原住民族紅、物種青／黃／紫）CLAUDE.md 已經記錄為飽和。
 *
 * 因為三級是三個各自可勾選的子圖層，色階是用 `LayerItem.color` 固定綁在各級上，
 * 不是依勾選順序指派（見 registry/resolve.ts 的 `itemColorOf`）。先勾哪一級都不會
 * 換色，否則「越深＝級別越高」這個圖例當場失效。
 *
 * ## 驗證
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#aa604e,#934c3a,#7d3827" --ordinal --mode light|dark
 *   → 兩模式各四項全數 PASS（亮度單調、步階間距 ≥ 0.06、單一色相、兩端對比 ≥ 2:1）
 *
 * ⚠️ 用分類色的 `--pairs all` 去驗它**會 FAIL，那是設計如此**（跟水庫藍 ramp 一樣，
 * 色階本來就刻意讓相鄰步階相近）。不要為了讓它過而去改壞色階。
 *
 * ## 為什麼是赭紅／磚色
 *
 * 語意上就是老磚牆與屋瓦的顏色。但真正決定它的是一項**額外**的檢查：古蹟與原住民族
 * 同屬「人文」群組，是實際上很可能同時勾選的兩個點圖層，所以色階的中間色必須跟
 * 原住民族紅 `#e34948`（以及地形景點藍）分得開。
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#2a78d6,#e34948,#934c3a" --pairs all --mode light|dark
 *
 * 這一關把直覺上最像「古蹟」的**飽和棕**整族刷掉了：`#b5793c` 對原住民族紅的
 * **deutan ΔE 只有 0.6**（紅綠色盲完全分不出來）、一般視覺也只有 12.7（低於 15）。
 * 掃過整個 OKLCH 色域後，同時通過 `--ordinal` 與這一關的只剩三族：低彩度的赭紅／
 * 棕（h 25–50）、黃綠（h 115–155，撞特有種綠與交通翠綠）、洋紅／紫（h 310–345，
 * 撞山脈洋紅與物種紫）。赭紅是唯一語意也對的那一族。
 *
 * ⚠️ **棕色系在這個站上有前科**：等高線是 `rgba(120,78,42,.55)`、地形陰影是
 * `#5a4632`，山脈線就是因此不能用棕色。古蹟能用的理由跟交通軸線能用綠色一樣，是
 * **地理分佈相反**——古蹟絕大多數在市區與平原（臺北 209、臺南 142、金門 96），
 * NLSC 通用電子地圖在那裡是白／灰底，而且圓點有白色外框。要改這個顏色，或懷疑
 * 山區的古蹟看不清楚時，請先在 NLSC 底圖的山區實際疊一次再說。
 */
export const MONUMENT_LEVEL_COLORS = {
  "縣(市)定古蹟": "#aa604e",
  直轄市定古蹟: "#934c3a",
  國定古蹟: "#7d3827",
} as const;

/**
 * 主要作物分布的三色（果樹／蔬菜／茶）。
 *
 * 這是**分類色**（三種作物之間沒有順序），所以用 `--pairs all` 驗證，不是 ramp：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#d36085,#7d9913,#0994de" --pairs all --mode light|dark
 *   → CVD 最差 ΔE 10.4、一般視覺最差 ΔE 25.9、彩度下限與亮度帶**兩模式皆過**
 *
 * ## 為什麼只有三種作物
 *
 * 不是挑剩的，是**掃出來的上限**。用 OKLCH 掃過色相／亮度／彩度找四色分類組合，
 * 能達到現行物種三色那個分離度（CVD ≥ 9.1、一般視覺 ≥ 22.9）的**一組都沒有**——
 * 四個分類色本質上就是沒辦法跟三個一樣好分。所以這一層維持三個子圖層，正好對上
 * 圖層說明原本就點名的「茶、水果」加上面積最大的蔬菜。
 *
 * ## ⚠️ 這組色比現行的物種三色更嚴格，不要拿物種那組當標準
 *
 * 實測**現行的物種三色在深色模式的亮度帶檢查是 FAIL**（`#eda100` L=0.764、
 * `#4a3aa7` L=0.433 都在帶外），CLAUDE.md 舊版寫的「三色 all-pairs 全數 PASS」
 * 用現在的驗證器重跑並不成立。這組作物色刻意選了**兩個模式亮度帶都過**的組合，
 * 分離度也更高（CVD 10.4 對 9.1）。要改色請至少維持這個水準。
 *
 * 顏色與作物的對應是靠圖例文字，不是靠「像不像」——茶配藍不是寫錯，那是掃描後
 * 唯一可行的第三個色相（比照交通軸線的翠綠是算出來的，不是挑出來的）。
 */
export const CROP_COLORS = {
  fruit: "#d36085",
  vegetable: "#7d9913",
  tea: "#0994de",
} as const;

/**
 * 人口密度的級距色（ordinal ramp）＋這一層的身分色。
 *
 * ## 為什麼又是一個「顏色跟著數值走」的圖層
 *
 * 跟水庫、古蹟同一條規則：`density` 是圖徵**自己的資料屬性**，不是 UI 狀態，
 * 而且這一層的兩個量刻意各佔一個視覺通道——**半徑＝年底人口數**（這裡住了幾個人）、
 * **顏色＝人口密度**（擠得多擠）。實測這兩件事真的會分開：人口最多的是板橋區
 * 55.0 萬，密度最高的卻是永和區 37,022 人/km²（人口只有 21.2 萬）；新店區有
 * 30.7 萬人但密度只有 2,553（轄區大半是山）。全部畫成同一顆點就看不出這件事。
 *
 * ## 為什麼是紫，而且為什麼中間色是 `#87519e`
 *
 * **掃出來的，不是挑的**（比照交通翠綠）。條件有兩層：色階本身要過
 * `--ordinal`，而**中間色**還要跟實際上很可能一起勾選的點圖層過 `--pairs all`
 * ——人口與原住民族、古蹟同屬「人文」群組，加上多半開著的地形景點藍。
 * 掃過整個 OKLCH 色域（色相每 2.5°、四種彩度，亮度與彩度剖面照抄水庫那條已驗證
 * 的 ramp）之後，**兩關全過的只剩色相 310–332° 的紫／洋紅那一族**，18 組候選。
 * 這跟古蹟當初掃出來的三族結論一致（那次是赭紅、黃綠、洋紅／紫，選了語意也對的
 * 赭紅），只是這次紅棕那一族已經被古蹟佔走了。
 *
 * 在那 18 組裡選 h=315／彩度 0.14，理由是它是**唯一一組深色模式對比 3.07:1、
 * 真的清得過 3:1** 的：其餘每一組的中間色都落在 2.96–3.02，會多欠一筆 relief。
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#c7a2d7,#a66ac0,#87519e,#673b7a" --ordinal --mode light|dark
 *   → 兩模式各四項全數 PASS（亮度單調、步階 ≥0.06、單一色相、兩端對比 2.14／2.06:1）
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#2a78d6,#e34948,#934c3a,#87519e" --pairs all --mode light|dark
 *   → 五項全數 PASS。⚠️ **輸出裡的兩個 WARN 都不是這個新色造成的**：
 *     CVD 最差的 `#934c3a↔#e34948`（ΔE 7.2 protan）與深色對比 `#934c3a` 2.77:1
 *     都是古蹟↔原住民族那一組**既有**的，拿掉 `#87519e` 重跑一模一樣。
 *     新色自己最差的一般視覺配對是 `#87519e↔#934c3a` ΔE 15.2（過 15 的下限）。
 *
 * ⚠️ **不要改成色階裡別的那幾階當身分色**：實測用第 2 階 `#a66ac0` 會直接
 * **FAIL**——它對地形景點藍 `#2a78d6` 的 protan ΔE 只有 3.6。
 *
 * ⚠️ 保護區紫 `#7538ae`（h 304）只差 11°，但那是**面**、這是**圓點**，跨幾何的
 * 配對本站不驗（18% 透明度的面染跟 6px 白框圓點是不同的視覺通道），而且兩層的
 * 地理分佈相反——保護區在山上，人口集中在西部平原。
 *
 * 這個組合**已經實測過**（NLSC 通用電子地圖、zoom 10）：全臺形心真的落在保護區面
 * 上的人口點只有兩個——花蓮縣卓溪鄉（玉山國家公園）與秀林鄉（太魯閣國家公園），
 * 兩個都是最淺那一階（密度 6 與 10）。放大看，白色外框把圓點跟底下的紫色面切得
 * 乾乾淨淨，讀得出來，跟古蹟那次靠白框的結論一致。**要改這個顏色的話再疊一次。**
 *
 * ## 級距
 *
 * 500 / 2,000 / 10,000 人/km²，實測分佈 168 / 109 / 62 / 29，四級都有份量。
 * 都是圓整好記的數字，而且各級的代表鄉鎮讀起來就是課本的分類：埔里鎮（466）、
 * 安南區（1,908）、中壢區（5,740）、板橋區（23,761）。
 *
 * 這一層**沒有 nodata**：368 個鄉鎮每一個都有人口統計。東沙／南沙那兩列人口是
 * 「…」，但它們在取得層就被濾掉了（見 lib/population.mjs），不會走到這裡。
 */
export const POPULATION_DENSITY_RAMP = {
  property: "density",
  steps: [
    { below: 500, color: "#c7a2d7", label: "未滿 500" },
    { below: 2000, color: "#a66ac0", label: "500–2,000" },
    { below: 10000, color: "#87519e", label: "2,000–10,000" },
    { below: null, color: "#673b7a", label: "10,000 以上" },
  ],
  // ⚠️ 刻意**不宣告 nodata**：368 個鄉鎮每一個都有人口統計（東沙／南沙那兩列的
  // 人口是「…」，但它們在取得層就被濾掉、不會變成 feature）。宣告了只會在圖例上
  // 多一個永遠不會出現的「暫無資料」類別。缺值時退回圖層身分色，見 layers/geo.ts。
} as const;

/** 人口與都市體系（點）。紫——色階的第 3 階，也是上面唯一驗證過的那一個。 */
export const POPULATION_COLOR = "#87519e";

/**
 * 垂直植被帶的海拔分帶與設色。
 *
 * ## 分帶是官方的，不是自己切的
 *
 * 六帶與界線逐字取自**農業部「農業兒童網」〈山地植群帶分布〉**那一頁
 * （`kids.moa.gov.tw/view.php?…&category=B16&id=27`，網址記在 `sourceLinks.ts`）：
 * 榕楠 500 以下、楠櫧 500–1,500、櫟 1,500–2,500、鐵杉雲杉 2,500–3,100、
 * 冷杉 3,100–3,600、第六帶 3,600 以上。該頁自己標的出處是「台灣的自然資源與生態
 * 資料庫－農林漁牧」（**林務局**發行、**臺大生物多樣性研究中心**製作），所以圖層
 * 的 `sources` 三個都要列：那一頁、林業及自然保育署（林務局的繼受單位）、臺大。
 *
 * ⚠️ **三件事是我們自己加工的，不要當成官方原文：**
 *
 * 1. **第六帶的官方名稱是「高山植群帶」**，站上顯示「高山寒原」——那是課綱與課本
 *    的用語（內容檔 `alpine.json` 有一則 fact 把兩個名字都寫出來）。
 * 2. **六帶是那一頁的版本，蘇鴻傑的原始分類是 8 帶**（Z1–Z8，楠櫧與櫟各再分上下
 *    部）。所以圖層說明不再寫「蘇鴻傑分類」四個字了事，改由 `notes` 交代這層簡化。
 * 3. **「以中部地區為準」是從單一段落推廣的**。該頁只在榕楠林帶那一段舉例：「以
 *    中部為例，主要分布於海拔 500 公尺以下的地區，南部地區受北回歸線影響，海拔可達
 *    700 公尺一帶」，並沒有替整組界線宣告基準。`notes` 的措辭要跟著這個事實走，
 *    不要寫得像官方原話。
 *
 * 帶界本來就是依**氣溫與林相轉變**切的，海拔只是代用值——這正是圖層標
 * `schematic: true` 的理由，UI 會顯示警語。要真正的植群圖得用林業保育署的植群調查
 * 多邊形，那是另一件事。
 *
 * ## 為什麼這一組不跑分類色票驗證
 *
 * 這是**暈渲設色**（hypsometric tint）：蓋滿全島、半透明、由 DEM 的高程直接驅動的
 * 連續場，跟 hillshade 的棕色與等高線的棕色是同一類「非分類的固定角色」，
 * 不參與 POINT／LINE／FILL 那三組 all-pairs 驗證（既有規則，見上面 REFERENCE_COLOR
 * 與 HAZARD_COLOR 的說明）。
 *
 * 但**亮度單調與相鄰階的間距仍然要驗**，那是「六帶讀得出順序」的前提：
 *
 *   node <dataviz-skill>/scripts/validate_palette.js "#f0e6da,#e7c89f,#cead59,#949938,#53803c,#246135" --ordinal --mode light|dark
 *   → 亮度單調 PASS、相鄰 ΔL 全部 ≥0.06 PASS（兩個模式都是）
 *
 * ⚠️ 另外兩項是**預期會 FAIL 的，不要為了讓它們過而改壞色階**：
 *
 * - **Single hue（色相跨度 77°）**：暈渲設色本來就是多色相，這正是它跟序位色階
 *   （水庫藍、古蹟赭紅、人口紫）的差別。綠→黃→白對應「闊葉林 → 針葉林 → 裸岩與雪」，
 *   是這一層要教的東西；改成單一色相就講不出來了。
 * - **Light-end contrast（`#f0e6da` 在淺色模式只有 1.20:1）**：那一項量的是「色塊 vs
 *   圖表底色」，而這條 ramp **從來不畫在面板上**，只有圖例的小色塊會碰到面板。
 *   高山寒原就是要接近裸岩與雪的顏色，壓深它等於謊報。比照保護區紫帶著 WARN 上線的
 *   既有判例：色塊一律緊接著帶名文字（「高山寒原　3,600 公尺以上」），從來不是唯一的
 *   辨識線索；另外圖例的色塊用 `.layer-swatch-band` 加了一圈實心邊框補償。
 *
 * ⚠️ **真正該驗的是它疊在 NLSC 通用電子地圖上讀不讀得出來**，那是驗證器測不到的：
 * NLSC 的山區底色本來就是綠的，這也是當初山脈線與保護區都不能用綠的原因。
 * 改色前先在玉山一帶打開 3D 地形實際疊一次。
 */
export const VEGETATION_BELTS = [
  { id: "rongnan", below: 500, color: "#246135", label: "榕楠林帶", note: "500 m 以下" },
  { id: "nanchu", below: 1500, color: "#53803c", label: "楠櫧林帶", note: "500–1,500 m" },
  { id: "quercus", below: 2500, color: "#949938", label: "櫟林帶", note: "1,500–2,500 m" },
  { id: "tsuga", below: 3100, color: "#cead59", label: "鐵杉雲杉林帶", note: "2,500–3,100 m" },
  { id: "abies", below: 3600, color: "#e7c89f", label: "冷杉林帶", note: "3,100–3,600 m" },
  { id: "alpine", below: null, color: "#f0e6da", label: "高山寒原", note: "3,600 m 以上" },
] as const;

/**
 * 帶與帶之間的界線色。
 *
 * ⚠️ **這條線是必要的，不是裝飾。** 最初的版本讓相鄰兩帶平滑漸變（`interpolate`
 * 直接內插兩個代表色），結果是一片糊掉的暖色調——「分帶」這件事在畫面上根本看不
 * 出來，而那正是整層唯一要教的東西。現在每一帶是**平的**，界線另外畫一條線。
 *
 * 線是用同一個 `color-relief` 表達式畫的：在界線高程前後各留 `BELT_EDGE_M` 公尺，
 * 中間插進這個深色。所以它**天生就是一條等高線**——沿著那個高程繞著山走，陡坡上
 * 細、緩坡上寬，跟真的等高線行為一模一樣。這也是為什麼不必（也沒辦法）另外開一個
 * maplibre-contour 來源：它的 `contourProtocolUrl` 只吃「每隔幾公尺」的間距，
 * 而植被帶的界線是 500/1,500/2,500/3,100/3,600 這種不等距的高程。
 *
 * 顏色刻意跟等高線同一個棕色家族（`rgba(120,78,42,.55)`）但更深——圖層本身還有
 * 45% 的不透明度，太淺會整條消失。
 */
export const BELT_EDGE_COLOR = "#3d3021";
/** 界線的高程半寬（公尺）。太小會因為 DEM 取樣而斷斷續續，太大就變成一條粗腰帶。 */
export const BELT_EDGE_M = 10;

/**
 * 語意色角色 → 色碼。元件一律透過角色取色，不直接寫 hex。
 *
 * 只列**已驗證**的角色。要新增角色（boundary／hydrology／reference…）
 * 必須先跑 dataviz skill 的 validate_palette.js，並把通過與失敗的組合
 * 像上面那樣記錄下來——那段散文是至今沒有人弄壞色票的原因。
 */
export const LAYER_COLORS = {
  place: PLACES_COLOR,
  indigenous: INDIGENOUS_COLOR,
  /** 行政區（面＋外框）。橘。 */
  boundary: BOUNDARY_COLOR,
  /** 水系（線）。藍——河川用藍是製圖慣例。 */
  hydrology: HYDROLOGY_COLOR,
  /** 山脈稜線（線）。洋紅——棕與綠都被地形本身佔走了，見上。 */
  relief: RELIEF_COLOR,
  /** 國家公園與保護區（面）。紫——綠會被 NLSC 底圖的山區底色吃掉，見上。 */
  conservation: CONSERVATION_COLOR,
  /** 交通軸線（線）。翠綠——是掃過整個色域後僅存的可行區間，見上。 */
  transport: TRANSPORT_COLOR,
  /** 人口與都市體系（點）。紫——同樣是掃出來的，理由見上。 */
  population: POPULATION_COLOR,
  /** 緯度參考線等參考幾何。中性色，非分類。 */
  reference: REFERENCE_COLOR,
  /** 地震帶等密度場。中性色，非分類。 */
  hazard: HAZARD_COLOR,
  /** 活動斷層（線）。磚紅——線／面色票的第六色，見上。 */
  fault: FAULT_COLOR,
  /**
   * 垂直植被帶。這一層真正的顏色是 `VEGETATION_BELTS` 那六帶，這裡只是圖層抽屜
   * 核取方塊旁邊那個小色塊需要**一個**代表色，取色階中段的櫟林帶。
   * 跟 reference／hazard 同屬非分類的固定角色，不參與色票驗證。
   */
  vegetation: VEGETATION_BELTS[2].color,
  /**
   * 板塊面。暖褐——同屬非分類的固定角色，見 PLATE_COLOR。
   * 三種板塊邊界的分類色不走這裡，它們是 `items.palette`（PLATE_BOUNDARY_COLORS）。
   */
  plate: PLATE_COLOR,
  /** 大洲分區（面）。梅紫——同屬非分類的固定角色，見 CONTINENT_COLOR。 */
  continent: CONTINENT_COLOR,
  /** 全球活火山（點）。洋紅——暖色端被張裂型邊界佔走了，見 VOLCANO_COLOR。 */
  volcano: VOLCANO_COLOR,
};
